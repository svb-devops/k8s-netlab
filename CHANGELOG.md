# Changelog

所有版本变更记录。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [Unreleased]

### Added
- feat(labgen): Linux Golden Lab #1 Production — "chmod 权限位正确，为什么还是 Permission Denied？" 完成安全预检 + 实验建设 + 真实演练 + 非 admin smoke，状态 `READY_FOR_OWNER_DOGFOOD`。安全预检发现并修复一个真实缺口：`linux_command_executor.py` 的 `_check_path_arg()` 对以 `-` 开头的参数不做检查，导致 `find` 的 `-exec`/`-execdir`/`-ok`/`-okdir`（尤其 `+` 终止符形式，不含任何 shell 元字符，绕过既有检查）和 `chmod --reference` 从未被拦截——已修复为显式拦截（`find_indirect_execution_denied`/`chmod_reference_denied`），并同步更新 `article_lab_prompt_builder.py` 的 Linux 命令生成约束文案，均补齐回归测试。新增 `path_access_condition` verifier 类型（`backend/labgen/models.py`/`static_validator.py`/`linux_verifier_client.py`）——复用与 learner 命令完全相同的 `LinuxCommandExecutor` 降权机制对目标路径发起真实 `cat` 读取，依据真实内核 returncode/stderr 判定 EACCES，而非仅比对 mode 位（`file_mode_matches` 无法区分"位对但仍被拒绝"这一 Golden Topic #1 的核心场景）；无 runner 身份或身份为 root 时 fail closed（`linux.access_check_requires_runner`），ENOENT 与 EACCES 严格区分不混淆。同时修复一个真实 ownership 一致性 bug：`LinuxWorkspaceManager.make_directory()`/`write_file()` 此前在 owner_uid/gid 已设置时仍未 chown 新建路径（与 `create_session()` 已有的 chown 行为不一致），导致真实非 root 进程对自己"应当拥有"的目录执行 chmod 会失败——已修复为创建后逐级 chown 到 workspace 根。真实内部演练（生产 `LinuxRehearsalService`，4/4 步骤，`LAB_CLOSED`/`cleanup_verified=true`）与真实非 admin 学员 smoke（全新注册普通账号 `lnx-cpd-smoke01`，走真实 WS 终端命令执行路径 `_run_linux_cmd`，非 admin bypass，4/4 步骤全过）均验证：Step 1/2 复现真实 EACCES，Step 4 修复父目录 execute 位后真实读取恢复成功。产出 Topic Brief、Lab Design Brief、Lab Draft（`publish_status=draft`，未加入任何公开 allowlist）、Official Site Article 终稿（`ready_to_publish_draft`，未创建 Directus 记录，未发布）。同步更新 `LINUX_GROWTH_SERIES_PLAN_v0.1.md`/`FIRST_GOLDEN_TOPIC_DECISION_v0.1.md`/`PROJECT_NORTH_STAR_v0.1.md` 状态。53+ 项新增定向测试全过，全量测试通过，mypy 0 错误，K8s/既有 Linux lab/公开可见性均无变化。
- feat(labgen): Linux Sandbox Privilege Separation v0.1 — 修复 `LINUX_GOLDEN_TOPIC_1_RUNTIME_BLOCKED_v0.1.md` 记录的 runtime 阻塞：新增专用非特权系统账号 `labgen-linux-runner`（UID/GID 997，无登录 shell，无密码，无附属组，`scripts/install-labgen-linux-runner.sh` 幂等安装），`backend/labgen/linux_command_executor.py` 通过原生 `subprocess.Popen(user=/group=/extra_groups=/umask=)` 参数（Python 3.9+，非 `preexec_fn`——因为该 executor 经 `run_in_executor` 在线程池中调用，`preexec_fn` 在多线程场景下有文档记载的 fork 死锁风险，本项目里是真实风险点，非泛泛而谈）让 learner 命令以该非特权身份执行，取代此前直接继承 root API 进程身份的行为。`backend/labgen/linux_workspace.py` 新增 workspace 目录创建后 chown 给 runner。`backend/labgen/routes.py` 的 `get_linux_runtime_adapter()`（学习者入口）与 `get_linux_rehearsal_service()`（管理员演练入口）均改为先解析并校验 runner 身份，解析失败 fail closed（返回 None / 503），绝不回退为 root。`backend/api_routes.py` 健康检查新增 `linux_runtime` 块（runner_uid/gid/is_root/ready，UID/GID 不作为密钥对待，刻意暴露）。真实复现验证（非模拟）：用生产同款 executor 代码路径证明——目录缺 execute 位时 `cat` 返回真实 EACCES，恢复执行位后真实读取成功。验证过程中发现并修复一个真实 bug：`lab_kubectl_ws.py` 的 `>` 输出重定向此前由 root 主进程直接 `write_text()` 写入，从未经过降权 executor，导致重定向创建的文件仍归 root 所有，与该文件后续以 runner 身份执行的 `chmod` 冲突返回 `Operation not permitted`——已修复为写入后立即 chown 给 runner（同一条已校验的 workspace 内路径，非任意路径 chown），补回归测试锁定该行为。现有 Linux lab（`Linux Files and Permissions Basics`，`6c439064`）在新 runner 下重新走通 4/4 内部演练 + 4/4 非 admin 学员 smoke（全新注册普通账号，非 admin bypass），`LAB_CLOSED`/`cleanup_verified=true`，公开访问已恢复。Safety-reviewer（A类强制审查，涉及安全模型变更）发现 1 BLOCKER + 1 HIGH 并已修复：CI workflow 此前不安装 runner 账号，会导致下次 push 起 pytest collection 直接崩溃（已在 `.github/workflows/test.yml` 补装步骤）；`LinuxRehearsalService` 默认构造路径（`adapter=None`）此前会静默构造以 root 身份运行的 adapter，与本轮"绝不静默回退 root"的安全契约矛盾（已改为默认路径强制要求显式传入 `runner_uid`/`runner_gid`，否则报错，不再存在裸跑 root 的默认路径）。33 项新增定向测试全过，全量测试 5214 passed，mypy 0 错误，K8s/既有 Linux lab 内容/公开可见性均无变化。
- docs(labgen): Linux Golden Lab #1 硬性可行性 Gate — 结果 RUNTIME_BLOCKED，未创建任何 lab/verifier/文章。经真实复现验证（在隔离 scratch 目录、非生产路径）：`k8s-netlab.service` 以 `User=root` 运行，`linux_command_executor.py` 的 subprocess 未做任何降权（无 setuid/seccomp/namespace），移除目录 execute 位后 `cat` 仍以 returncode 0 读取成功——root 绕过了 DAC 目录遍历检查，Golden Topic #1（chmod 权限报错排查）的故障模型在当前 runtime 下无法真实复现，非假设推理。同步修正三份既有规划文档：`LINUX_GROWTH_SERIES_PLAN_v0.1.md`（既有 Linux lab 改为 prerequisite/legacy 资产、不计入六主题系列；仅 Golden Topic #1 正式绑定，其余五环标记 provisional）、`LINUX_TRACK_CONTRACT_v0.1.md`（新增 Runtime Profile 拆分：Sandbox Profile 当前实际以 root 运行，与任务书假设的"无 root"矛盾，已如实更正而非掩盖）、`FIRST_GOLDEN_TOPIC_DECISION_v0.1.md`（标注 gate 结果）。提供 4 个隔离身份修复选项供 Owner 决策，未替 Owner 预先选定方案。本轮未改任何生产代码，K8s/既有 Linux lab/公开可见性均无变化，204 项 Linux 定向测试全过。
- docs(labgen): Linux Growth-First Track Foundation v0.1 — CEO/CTO 决策移除"第二位真实读者测试"发布门槛，Linux 线路从纯技术验证转向内容驱动增长漏斗模型。产出：现有 Linux 资产审计（分类 upgrade_and_include，发现无公开文章/CTA、12 个契约级 verifier 类型仅 2 个存在、当前沙箱运行时零 root/systemd/网络能力）、10 个候选主题的 Topic Radar（含真实 WebSearch 信号，2 个 P0 均零平台缺口）、7 条目 Series Plan（Order 0 既有资产升级 + Order 1-6 新主题，4 个主题因需要运行时投入被明确延后）、Linux Track Contract（runtime/fault injection/verifier/cleanup/发布顺序/增长漏斗埋点边界）、第一个黄金样板主题决策（`linux-chmod-permission-denied-despite-correct-mode`，零平台缺口）。本轮仅产出文档，未新建 lab/文章/账号，未修改任何生产代码，K8s/既有 Linux lab/公开可见性均无变化，health healthy。详见 `docs/labgen/linux/`
- feat(frontend): 网络实验室（/app）与 LabGen 故障排查练习之间的身份/导航统一 — 抽取共享模块 `frontend/js/nav-auth.js`，`landing.js`/`article.js` 不再各自重复 `/api/auth/me` 渲染逻辑；`labgen-session.html` 完成实验（`LAB_COMPLETED`/`LAB_CLOSED`）后新增转化 CTA 指回 `/app`；`index.html`/`labgen-catalog.html` 术语统一为「网络实验室」/「故障排查练习」
- feat(labgen): Pod Pending Minimal Publish — Phase 1《Kubernetes 高频故障排查实战系列》正式收官，六篇全部公开发布 — CEO/CTO 决策正式发布 Pod Pending 作为系列第六篇，发布完成后正式收口 Phase 1。Directus 新建正式 article（`pod-pending-events-not-logs`，`status=published`，公开可访问 200，正文取自内部 final draft 去除 `internal_preview_version` 预览尾注），`lab_id=dcddf681-f906-491c-b126-efee40f3621c` 加入 `LABGEN_ENABLED_LAB_IDS`/`LABGEN_AUTO_VM_PROVISION_LAB_IDS`，`cta_enabled=true`，无 invite 限制，访问模型与前五篇一致。前五篇已发布文章（CrashLoopBackOff/Service No Endpoints/ImagePullBackOff/ConfigMap/DNS Service Discovery）"系列文章"段落追加指向 Pod Pending 的链接，Pod Pending 文章本身已含指向前五篇的链接，6 个 slug 全部实测 API+页面 200，broken_links=0。发布前确认了同日更早修复的两个发布阻塞问题（终端重连输出丢失 + session TTL 30/90 环境变量覆盖漂移，commit `0962703`）已在生产生效：`session_ttl_minutes=90`、`ops_smoke_check.sh` 新增的 TTL drift 检查通过。发布后用全新注册、非 admin、无预分配 VM 的账号（`pp-publish-e2e-01`）走完整公开路径：文章 → CTA → Start Lab → 自动 provisioning → `LAB_ACTIVE` → 5/5 步骤全部通过（含真实浏览器验证终端重连后命令回显/输出正常渲染，零丢失）→ cleanup → `LAB_CLOSED` → `cleanup_verified=true`。全系列状态确认：6 个 lab 全部 `is_startable=true`，6 篇文章全部 `published`，6 个 CTA 与各自 lab 一一对应，其余系列 lab 行为不受影响，health 全程 healthy，`active_sessions=0`/`provisioning_jobs=0`/`tainted_vms=0`，无新增错误日志。本次任务范围严格限定于 Pod Pending 发布 + 系列收官确认，未顺便修复 abort cleanup retry budget（约 28 秒，独立 backlog）、未处理 zombie drafts/`lab_review_diffs.json` 积压（独立 backlog）、未开启新系列/Phase 2/Growth、未修改前五个已发布 lab 的教学内容。新增 `docs/labgen/PHASE1_KUBERNETES_SERIES_CLOSURE_v1.0.md`（Phase 1 收官报告：六篇文章/lab 状态表、可复用 verifier 资产清单、新用户体验验证记录、当前运行边界），更新 `docs/labgen/PHASE1_SERIES_ALIGNMENT_v0.1.md`（状态表 Pod Pending 行改为"已正式公开发布"，second_wave 第 6 项标注系列收官）
- feat(labgen): Second Wave Sprint #3 — 生产"Pod 一直 Pending"排查实验，新增 pod_phase_equals/pod_scheduling_unschedulable 两个最小可复用只读 verifier，未触发任何新的 verifier RBAC 缺口 — CEO/CTO 决策 Second Wave 第三个主题聚焦"调度阶段失败、容器从未创建"，与前五个主题（Pod 状态类/配置类/namespace 隔离类故障）互补。场景：给一个正常运行的 Deployment 用非交互式 `kubectl patch --type=merge` 注入一个集群里不存在的 `nodeSelector`，触发新 Pod 停留在 `Pending`；确认调度失败原因用 `status.conditions`（`PodScheduled=False`/`reason=Unschedulable`）而非 Events 文本（Events 有 TTL 会过期，condition 是当前状态的权威来源）；用 `kubectl patch --type=json` 的 `remove` 操作非交互式移除错误配置（不用 `kubectl edit`）。全程已在真实 K3s（VM 401）实测验证：patch 注入 nodeSelector 后触发新 ReplicaSet，新 Pod 与仍在 Running 的旧 Pod 短暂共存（新 Pod 一直 Pending）；`status.conditions` 实测确认 `message` 为 `"...didn't match Pod's node affinity/selector..."`；移除 nodeSelector 后原 ReplicaSet 直接缩容回 1（未产生第三个 ReplicaSet），`rollout status` 成功。新增 `backend/labgen/verifier.py`（K8sVerifierClientPort 2 个抽象方法 + FakeK8sVerifierClient 实现 + `_SUPPORTED_TYPES`/`_make_detail`/`_dispatch` 各加 2 个分支，`pod_phase_equals` 对 `expected_phase=None` fail-closed）、`backend/labgen/k8s_verifier_client.py`（真实实现：`pod_phase_equals`/`pod_scheduling_unschedulable` 均复用既有 `list_namespaced_pod` + `_find_pods` helper 的"任一匹配即算"语义，与 `pod_running`/`pod_succeeded` 保持一致——同一 label selector 下新旧 Pod 共存时必须挑对目标而非只看第一个；`pod_scheduling_unschedulable` 读 `status.conditions` 而非 Events，`message_contains` 是真正可选字段，为 None 时跳过消息子串比对而非当作永远匹配）、`backend/labgen/models.py`（VerifyType 加 2 个枚举值，VerifyTemplate 新增 `expected_phase`/`message_contains` 两个 Optional 字段）、`backend/labgen/static_validator.py`（新增 `verify.pod_phase_equals_fields` 检查）。safety-reviewer 审查：no blocking issues found，两处主动标注的风险点（"任一匹配"语义的适用边界、`message_contains` 为 None 时的行为与 `log_contains` fail-closed 设计的差异）均确认是既有一致的设计约定，非本次引入的缺陷。顺带补齐了 DNS Sprint 遗留的一处测试缺口：`pod_log_contains_fields` 此前没有专门的 static_validator 回归测试，本轮一并补上（新增 `TestVerifyPodLogContainsFields`）。两轮独立的真实 K3s rehearsal（VM 401，session `f70473e3-...`/`04303ff8-...`）全部 5 步通过，`cleanup_verified=true`，namespace 均确认真实回收。`lab_id=dcddf681-f906-491c-b126-efee40f3621c`，`publish_status=published`（internal soft launch，**不**加入 `LABGEN_ENABLED_LAB_IDS`/`LABGEN_AUTO_VM_PROVISION_LAB_IDS`；已用非 admin 账号验证确认被 `LAB_NOT_ENABLED` 正确拦截）。官方文章草稿已撰写（`article_status=ready_to_publish_draft`，未创建 Directus 记录、未发布）。已发布的五个 First Wave/ConfigMap/DNS lab 与文章、access policy、health 全程未受影响。至此《Kubernetes 高频故障排查实战系列》六个主题全部完成生产。全量测试 5142 passed，92.27% coverage，mypy 0 error。新增 `docs/labgen/POD_PENDING_TOPIC_BRIEF_v0.1.md`/`POD_PENDING_LAB_DESIGN_BRIEF_v0.1.md`/`articles/POD_PENDING_OFFICIAL_ARTICLE_FINAL_DRAFT_v1.0.md`，更新 `PHASE1_SERIES_ALIGNMENT_v0.1.md`
- feat(labgen): Second Wave #2 Minimal Publish — DNS Service Discovery 正式面向公开读者发布，作为《Kubernetes 高频故障排查实战系列》第五篇 — CEO/CTO 决策正式发布。Directus 新建正式 article（`dns-service-discovery-namespace-fqdn`，`status=published`，公开可访问 200，正文取自 `ready_to_publish_draft` 去除内部预览尾注），`lab_id=39b87766-a7eb-460d-a8d3-ac5a31319d4a` 同步加入 `LABGEN_ENABLED_LAB_IDS`/`LABGEN_AUTO_VM_PROVISION_LAB_IDS`，`cta_enabled=true`，无 invite 限制，访问模型与前四篇完全一致。发布前确认了上一轮"kubectl 终端粘贴换行符"前端 bug 修复已部署（生产 `/js/labgen-kubectl-terminal.js` 与已测试提交的源码逐字节一致）。发布后用全新注册、非 admin、无预分配 VM 的账号（`dns-e2e-fresh-01`）走完整公开路径：文章→CTA→Start Lab→自动 provisioning→`LAB_ACTIVE`→4/4 步骤通过（`kubectl logs -l app=dns-check` 正是此前触发 bug 的同类操作，此次顺畅执行）→cleanup→`LAB_CLOSED`→`cleanup_verified=true`；`kubectl logs` 机器验证确认 `SHORT_NAME_FAILED_AS_EXPECTED`/`SERVICE_FQDN_RESOLVED` 两个核心教学标记。五篇已发布文章互相补齐了最小系列导航（本文新增指向前四篇的链接，前四篇也各自追加了指向本文的链接），5 个 slug 全部实测 200，无死链，未新建独立 series page。测试账号与 VM 已清理，5 个系列 lab 全部确认 `is_startable=true`，health 全程 healthy，无新增错误日志。本次纯内容发布 + 配置开关，无代码改动。新增 `docs/labgen/DNS_MINIMAL_PUBLISH_RESULT_v0.1.md`，更新 `PHASE1_SERIES_ALIGNMENT_v0.1.md`
- feat(labgen): Second Wave Sprint #2 — 生产"Pod 里解析不到 Service"DNS 服务发现失败排查实验，新增 pod_succeeded/pod_log_contains 两个最小可复用只读 verifier，并修复一个影响这两个新类型的 verifier RBAC BLOCKER — CEO/CTO 决策 Second Wave 第二个主题聚焦"namespace 隔离导致服务发现失败"，与 First Wave 三个"Pod 状态类"故障、ConfigMap 的"配置类"故障互补。设计相对任务书原始方案有一处必要偏差：读 `backend/labgen/learner_credentials.py`（`LEARNER_ALLOWED_PERMISSIONS`）确认学员 RBAC 完全不含 `namespaces`/`pods`/`batch/jobs` 的 create 权限——学员既不能自己创建第二个 namespace，也不能直接建 Pod/Job，只能创建 `deployments`。改为用平台预先建好的共享 namespace `labgen-dns-target`（`api` Service + Deployment，性质类似已有的 registry mirror/kube-dns，运维一次性维护、不随会话生命周期清理）作为跨 namespace 目标；DNS 解析本身是网络层查询、不需要跨 namespace RBAC，学员侧只需在自己的 namespace 内创建一个诊断 Deployment 发起 nslookup（容器启动命令一次性跑短名称+FQDN 两条 nslookup，打印稳定标记后 `sleep 3600` 保活，避免依赖被禁止的 `kubectl exec`）。已在真实 K3s（VM 401）验证：短名称查询 NXDOMAIN、FQDN 正确解析到目标 Service ClusterIP。新增 `backend/labgen/verifier.py`（K8sVerifierClientPort 2 个抽象方法 + FakeK8sVerifierClient 实现 + `_SUPPORTED_TYPES`/`_make_detail`/`_dispatch` 各加 2 个分支）、`backend/labgen/k8s_verifier_client.py`（真实实现：`pod_succeeded` 读 `list_namespaced_pod` 判断 phase==Succeeded，`pod_log_contains` 读 `read_namespaced_pod_log` 判断子串是否存在，均非 `kubectl exec`）、`backend/labgen/models.py`（VerifyType 加 2 个枚举值，VerifyTemplate 新增 `log_contains` 仅 pod_log_contains 使用的 Optional 字段）、`backend/labgen/static_validator.py`（新增 `verify.pod_log_contains_fields` 检查，防止 log_contains 缺失导致草稿通过 schema 校验但在 rehearsal 时永远失败）。**BLOCKER**：首轮真实 rehearsal 中 `pod_log_contains` 调用因 `lab-verifier-namespace-readonly` ClusterRole 缺少 `pods/log:get` 权限而 500（`Forbidden`），根因是这是该 ClusterRole 第一次需要日志读取能力（此前所有 verify 类型都只需 list+field_selector）；在 `backend/labgen/verifier_credentials.py` 的 `_CLUSTER_ROLE_MANIFEST`（YAML 字符串）和 `PlatformVerifierInitializer.ensure_verifier_identity`（`V1PolicyRule` SDK 对象）两处一致补齐 `pods/log: [get]`（日志读取没有 list 语义等价物，是唯一允许 `get` verb 的例外），对 VM 401 重跑既有的幂等 `initialize_verifier_for_vm_host_side` 使其生效；更新既有回归测试 `test_labgen_verifier_credentials.py`/`test_labgen_platform_verifier_init.py` 中"ClusterRole 绝不允许 get"的断言，改为显式排除 `pods/log` 并新增其"只允许 get、不多不少"的校验，`test_sdk_object_matches_manifest_string` 交叉校验两处定义未来不会静默分叉。两轮独立的真实 K3s rehearsal（VM 401，session `a405bfe3-...`/`7751e825-...`）全部 4 步通过，`cleanup_verified=true`，namespace 均确认真实回收（`kubectl get ns` NotFound）。`lab_id=39b87766-a7eb-460d-a8d3-ac5a31319d4a`，`publish_status=draft`（internal soft launch，**不**加入 `LABGEN_ENABLED_LAB_IDS`/`LABGEN_AUTO_VM_PROVISION_LAB_IDS`）。官方文章草稿已撰写（`article_status=ready_to_publish_draft`，未创建 Directus 记录、未发布）。已发布的四个 First Wave/ConfigMap lab 与文章、access policy、health 全程未受影响。safety-reviewer 审查抓到 2 个真实问题并已修复：`_dispatch` 里 `template.log_contains or ""` 在字段缺失时会退化成空字符串，而空字符串是任意字符串的子串，导致本应"fail closed"的校验反而"fail open"（静默永远通过）——已改为显式判空并直接返回 False，不再依赖 StaticValidator 的发布前检查作为唯一防线；`pod_succeeded`/`pod_log_contains` 共享的 pod 查找逻辑在 label_selector 命中多个 Pod 时（例如 rollout 过程中新旧 Pod 并存）只看 `items[0]`，与既有 `pod_running` 的"任一匹配即算"语义不一致，可能读到错误 Pod 的状态/日志——已改为遍历所有匹配 Pod。均先写失败测试复现（`test_pod_log_contains_missing_log_contains_fails_closed`/`test_label_selector_true_if_any_pod_succeeded`/`test_label_selector_true_if_any_matching_pod_log_contains`），修复后全部通过。新增 `docs/labgen/DNS_SERVICE_DISCOVERY_TOPIC_BRIEF_v0.1.md`/`DNS_SERVICE_DISCOVERY_LAB_DESIGN_BRIEF_v0.1.md`/`articles/DNS_SERVICE_DISCOVERY_OFFICIAL_ARTICLE_FINAL_DRAFT_v1.0.md`，更新 `PHASE1_SERIES_ALIGNMENT_v0.1.md`
- feat(labgen): Second Wave #1 Minimal Publish — ConfigMap 修改后不生效实验正式面向公开读者发布，并借此完成系列四个已发布 lab 的 verifier 关键路径回归 — CEO/CTO 决策正式发布，作为 Kubernetes 高频故障排查实战系列第四篇。Directus 新建正式 article（`configmap-not-effective-rollout-restart`，`status=published`，公开可访问 200，加入系列互链），`lab_id` 同步加入 `LABGEN_ENABLED_LAB_IDS`/`LABGEN_AUTO_VM_PROVISION_LAB_IDS`，`cta_enabled=true`，访问模型与另外三个已发布 lab 完全一致（无 invite 限制）。发布后用全新注册、非 admin、无预分配 VM 的账号（`cm-e2e-fresh-01`）走完整公开路径：文章→CTA→Start Lab→自动 provisioning→`LAB_ACTIVE`→8/8 步骤通过→cleanup→`LAB_CLOSED`→`cleanup_verified=true`；额外用平台管理员权限（非学员/verifier 路径，仅本次验证用）直接 `kubectl exec` 交叉核对三个核心教学语义在真实 Pod 进程里确实成立（重启前读 old、ConfigMap 改 new 后运行中 Pod 仍读 old、rollout restart 后新 Pod 读 new）。同一账号依次对 First Wave 三个已发布 lab 做代表性 verifier 回归（CrashLoopBackOff 的 `deployment_unavailable`、Service No Endpoints 修复后的 `service_has_endpoints`、ImagePullBackOff 的 `deployment_unavailable`），均 PASS + `cleanup_verified=true`，确认上一轮 `LABGEN_K8S_VERIFIER_VM_ID` pin 修复对整个系列都生效而非只对单个 lab。测试账号与 VM 已清理，4 个 namespace 全部确认回收，health 全程 healthy，无新增错误日志。本次纯内容发布 + 配置开关，无代码改动。新增 `docs/labgen/CONFIGMAP_MINIMAL_PUBLISH_RESULT_v0.1.md`，更新 `PHASE1_SERIES_ALIGNMENT_v0.1.md`
- feat(labgen): Second Wave Sprint #1 — 生产"ConfigMap 修改后不生效"排查实验，新增 configmap_value_equals/deployment_restart_triggered/deployment_restart_not_triggered 三个最小可复用 verifier — CEO/CTO 决策 Second Wave 首个主题聚焦"配置类"故障，与 First Wave 三个"Pod 状态类"故障（CrashLoopBackOff/Service 无 Endpoints/ImagePullBackOff）互补。核心工程冲突：`kubectl_executor.py` 对学员和 verifier 均禁止 `exec`，无法直接读取运行中容器进程的实际环境变量值，而 Pod 对象的 `envFrom`/`valueFrom.configMapKeyRef` 存储的始终是对 ConfigMap key 的引用而非 kubelet 解析出的字面值，查 Pod API 无法判断"这个容器进程里的值是新是旧"。改用逻辑等价、可通过 K8s API 直接机器化验证的信号：Deployment 的 `spec.template.metadata.annotations` 是否含 `kubectl rollout restart` 写入的 `kubectl.kubernetes.io/restartedAt` 注解——已在 VM 401 真实 K3s 集群实测验证，仅 `kubectl patch configmap` 不会触发这个注解或创建新 Pod，只有 rollout restart 才会。新增 `backend/labgen/verifier.py`（K8sVerifierClientPort 3 个抽象方法 + FakeK8sVerifierClient 实现 + `_SUPPORTED_TYPES`/`_make_detail`/`_dispatch` 各加 3 个分支）、`backend/labgen/k8s_verifier_client.py`（真实实现，均用 `list_namespaced_*` + field_selector 最小 RBAC、无写操作）、`backend/labgen/models.py`（VerifyType 加 3 个枚举值，VerifyTemplate 新增 `config_key`/`expected_value` 两个仅 configmap_value_equals 使用的 Optional 字段）、`backend/labgen/static_validator.py`（新增 `verify.configmap_value_equals_fields` 检查，防止 config_key/expected_value 缺失导致草稿通过 schema 校验但在 rehearsal 时永远失败）。safety-reviewer 审查抓到 2 个真实问题并已修复：`_check_verify_configmap_value_equals_fields` 用 `not vt.expected_value` 错误地把合法的空字符串值当作缺失字段拒绝（改为显式 `is None` 判断）；`deployment_restart_not_triggered` 对同一个 Deployment 调用了两次 `list_namespaced_deployment`，存在窄 TOCTOU 窗口（重构为单次调用 + 三态 `Optional[bool]` 返回值消除该窗口，补充回归测试锁定"只调用一次"）。新增/更新 4 个测试文件（`test_labgen_k8s_verifier_client.py`/`test_labgen_verifier.py`/`test_labgen_static_validator.py`/`test_labgen_models.py`），全量 5077 passed，92.17% coverage，mypy 0 error。Lab 生产：`article-drafts`/`generate-lab` 拿 stub 骨架 → 人工 PATCH 真实内容（8 步，6 步机器化 verify）→ static validation 21/21 PASS → 真实 K3s rehearsal（VM 401，session `3bd9d4ce-...`，8/8 步骤 + `cleanup_verified=true`）→ `publish_status=published`（internal soft launch，**不**加入 `LABGEN_ENABLED_LAB_IDS`）→ owner-as-learner smoke（session `47e93449-...`，8/8 步骤 + `cleanup_verified=true`）→ 官方文章草稿（`article_status=ready_to_publish_draft`，未创建 Directus 记录）。Rehearsal 过程中发现并修复一个与本次代码无关的真实基础设施 bug：VM 401 的 verifier 凭证（`/var/lib/labgen-staging/verifier-credentials/401/`，与仓库内 `creds/vm_creds/` 是两个不同目录）保存的 kubeconfig 指向 VM 401 的旧 IP（172.16.100.153），实际当前 IP 是 172.16.100.140，用既有的 `scripts/provision_verifier_credentials.py --vm-id 401` 幂等重新 provision 修复（`credential_generation` 1→2），已记入生产结果报告的 backlog（health 端点目前只检查凭证文件存在，不检查 endpoint 连通性，IP 漂移会静默失效直到真正跑 rehearsal 才暴露）。First Wave 三个 lab、`LABGEN_ENABLED_LAB_IDS`/`LABGEN_AUTO_VM_PROVISION_LAB_IDS`、Directus backup/credentials 状态全程未受影响，生产 health 全程 healthy。新增 `docs/labgen/CONFIGMAP_NOT_EFFECTIVE_TOPIC_BRIEF_v0.1.md`/`CONFIGMAP_NOT_EFFECTIVE_LAB_DESIGN_BRIEF_v0.1.md`/`CONFIGMAP_NOT_EFFECTIVE_LAB_PRODUCTION_RESULT_v0.1.md`/`articles/CONFIGMAP_NOT_EFFECTIVE_OFFICIAL_ARTICLE_FINAL_DRAFT_v1.0.md`，更新 `PHASE1_SERIES_ALIGNMENT_v0.1.md`
- feat(ops): P0 Backup Hardening — 补齐 Directus PostgreSQL / uploads / extensions 的自动备份缺口 — 排查发现既有 `scripts/backup-data.sh` 每日只备份 `data/*.json`，Directus 使用的 PostgreSQL 16 容器（承载 CMS 文章 + 公开用户认证数据）以及 `data/directus/uploads`/`extensions` 从未被任何自动化机制覆盖，也从未做过恢复演练。新增 `scripts/backup-directus.sh`（`docker exec pg_dump -Fc` 生成 custom format dump，可用 `pg_restore` 恢复；`tar czf` 归档 uploads/extensions；密码从 `.env.directus` 读取，全程不写入日志/备份产物）+ `scripts/backup-full-state.sh`（编排入口：flock 独占锁防并发、staging 临时目录内完成全部产出+SHA-256 checksum 校验后才原子 `mv` 到正式备份目录，任一步骤失败即整体非零退出且不留半成品、不产出到正式目录；目录权限700/文件权限600；生成 `manifest.json` 记录时间戳/文件名/大小/SHA-256/Postgres版本；保留最近30天且不清理本次刚生成的备份）；`backup-data.sh` 增加可选 `DEST_DIR` 参数（向后兼容，独立 cron 调用行为不变）供编排脚本复用。crontab 新增 `30 3 * * *` 全量备份任务，与原有 `0 3 * * *` 的 data-only 备份错开。生产环境实际执行验证：真实 pg_dump（323KB，13 users/5 articles/16 experiments）+ uploads/extensions 归档 + 15 个文件 checksum 全部通过，全程未中断 Directus/PostgreSQL 生产容器，health 保持 healthy。隔离环境恢复演练：一次性 `postgres:16-alpine` 临时容器（随机密码，非生产密码）`pg_restore` 后关键表记录数（`directus_users`/`articles`/`directus_files`/`comments`/`experiments`）与生产完全一致，uploads/extensions 归档 `diff -rq` 解压比对生产目录无差异，演练结束后 `docker rm -f` 清理临时容器，生产容器/数据全程未受影响。当前**未配置异地备份**（未发现 rclone/restic 配置、未发现 age/GPG 密钥），本地备份与生产数据同处一台物理机不构成灾难恢复能力，已在文档中明确标注 `OFFSITE_BACKUP_NOT_CONFIGURED` 并列出 owner 决策项，不自行生成/托管新密钥。新增 `docs/ops/DIRECTUS_BACKUP_RESTORE_RUNBOOK.md` + `RUNBOOK.md` 场景九引用。提交时 pre-commit hook 明文密码检测在本次多处误报（`PGPASSWORD="$DRILL_PW"`/`DIRECTUS_DB_PASSWORD="$(grep ... "$ENV_FILE")"` 这类 shell 变量引用/命令替换被正则 `="[^"]{4,}"` 误判为硬编码密码字面量）；核实这些位置全部是运行时从环境变量/`.env.directus` 文件动态取值，无一处硬编码，据此给 `scripts/pre-commit-security-check.sh` 明文密码检测新增排除模式 `="\$`（值以 `$` 开头即变量引用/命令替换，不可能是字面量密码），真实硬编码密码检测能力不受影响；`scripts/pre-push-security-check.sh` 同一处正则同步修复
- feat(ops): P0 Directus Credential Rotation — 安全轮换 DIRECTUS_DB_PASSWORD / DIRECTUS_SECRET / DIRECTUS_ADMIN_PASSWORD 三个生产密钥并全部回归验证通过 — 起因是上一次备份加固任务里 Codex review 的独立调查过程读取了 `.env.directus`，其明文内容作为工具输出出现在了 Claude Code 会话记录里，出于预防原则整轮轮换这三个凭证。轮换前先跑一次 `backup-full-state.sh` 确认 checksum 全通过、备份当前 `.env.directus`（600 权限）作为可执行回滚方案。三个密钥全程只通过 stdin 或环境变量传递，不作为任何 `docker exec`/CLI 命令的位置参数或 `-e`/`--password` 类选项值出现（避免落入 `ps auxww`/`/proc/<pid>/cmdline` 可见的进程 argv）：Postgres 角色密码轮换通过把认证用的旧密码和 `ALTER ROLE` 语句一起经 stdin 送进容器内 `psql` 脚本模式执行；DIRECTUS_SECRET 直接写入 `.env.directus` 后仅重建 `directus` 容器（不重启 `database`，因为 `ALTER ROLE` 立即生效、Postgres 官方镜像的 `POSTGRES_PASSWORD` 环境变量只在数据卷首次初始化时读取，之后重启对已有角色密码没有任何作用）；DIRECTUS_ADMIN_PASSWORD 因 Directus CLI 的 `users passwd --password` 会把新密码暴露进程 argv 而改用 REST API（旧密码 `POST /auth/login` 换 token，再 `PATCH /users/me`，请求体由 Python `urllib`/`json.dumps` 构造，不落地文件、不经会打印到 argv 的 curl 参数）。回归验证：旧 admin 密码登录返回 401、新密码返回 200；公开只读端点（`/items/articles`、`/items/experiments`）全程 200 不受 SECRET 轮换影响；无专门的 Directus 非 admin 测试账号密码可用，用"对一个真实 learner 邮箱故意传错密码探测确认 401"+"admin 完整走通登录鉴权同一套代码路径"两者合起来作为普通用户登录路径未受影响的证据链；health 全程 healthy，13 个 `directus_users`、5 篇文章（含三个 First Wave 全部仍 published）、16 个 experiments 记录数轮换前后完全一致，`journalctl`/`docker logs` 无新增 error；轮换后用新凭证跑一次 `backup-full-state.sh`（15 文件 checksum 全过）+ 隔离临时容器 `pg_restore` 演练（关键表记录数与生产一致，已清理临时容器），确认新密钥下备份链路完整可用；用 `ps auxww` 扫描全程未发现任何新密钥残留。`docs/ops/DIRECTUS_BACKUP_RESTORE_RUNBOOK.md` 新增"凭证轮换"章节记录方法论与已验证的操作顺序/坑，不含任何密钥明文。`LABGEN_ENABLED_LAB_IDS`/`LABGEN_AUTO_VM_PROVISION_LAB_IDS`、文章正文、lab 内容均未触碰。`.env.directus` 本身在 `.gitignore` 中，本次未提交任何密钥到 git

### Fixed
- fix(labgen): Phase 1 Limited-Availability Ops Closure — 修复 abort/cleanup namespace 删除误判根因，非 24×7 宿主机下的关键数据持久化任务改为 systemd Persistent timer — CEO 任务简报，目的是让备份/数据保留/实验清理在"宿主机每天只开机约 8 小时"的条件下依然可靠，并消除已知的 cleanup 误报。**根因修复（真实生产问题，非本次改动引入）**：`LabSessionService._do_cleanup()` 删除学员 K8s namespace 时，此前用固定"15 次重试 × 2 秒间隔 = 30 秒硬预算"轮询确认，2026-07-18/19 两次真实生产事故里，K3s 的真实异步删除都在预算耗尽后几秒内完成（用 kubectl 核实过 namespace 确实已删除），却被这个死板预算误判为 `LAB_CLEANUP_FAILED` 并把 VM 标记 `tainted`。改为指数退避（`LABGEN_NS_DELETE_INITIAL_INTERVAL_S` 起始 1 秒，`LABGEN_NS_DELETE_BACKOFF_FACTOR` 1.6 倍增长，`LABGEN_NS_DELETE_MAX_INTERVAL_S` 封顶 10 秒）+ 可配置总等待预算（`LABGEN_NS_DELETE_MAX_WAIT_SECONDS`，生产默认 100 秒，CEO 简报建议 90-120 秒区间内），并新增幂等预检查（`_do_cleanup` 发起删除请求前先查一次 `is_namespace_deleted()`，已经删除则直接判定成功，不重复发请求——覆盖"上一次 cleanup 已经把删除请求发出去、这次是进程重启后的重试"这类场景）。`LabSessionService.__init__` 的 4 个构造参数改名（`ns_delete_poll_interval`/`ns_delete_max_retries` → `ns_delete_max_wait_seconds`/`ns_delete_initial_interval_s`/`ns_delete_backoff_factor`/`ns_delete_max_interval_s`），类默认值刻意保持很小（5 秒量级，延续旧代码"类默认小、生产靠 `routes.py::get_session_service()` 显式传入 config 大值覆盖"的既有约定，保证不传参的测试/脚本调用方仍然跑得快）。新增 5 个回归测试覆盖 CEO 简报要求的全部场景：快删在预算内成功、慢删（真实 wall-clock 多轮轮询、小预算参数保证测试仍快）在预算内成功、预算耗尽后确认仍未删除时正确失败（不会被退避机制掩盖真失败）、namespace 已提前删除时幂等跳过重复删除请求、`_do_cleanup` 直接连续调用两次的幂等性（验证第二次不重复发起删除请求）；顺带修复了一个牵连的测试基础设施 bug——`StubNamespaceLifecycleAdapter.is_namespace_deleted()` 之前无条件返回构造参数值，不管 `delete_namespace()` 有没有被调用过，导致新的幂等预检查在测试里被这个 stub 的默认值提前短路，已改为按每个 namespace 分别追踪"是否已调用过删除"的真实状态。Codex review 抓到 2 个真问题并已修复：① `deploy/systemd/k8s-netlab-data-retention.service` 引用的 `scripts/run-data-retention-cron.sh` 提交时是 `100644`（不可执行），systemd 启动会直接失败——已用 `git update-index --chmod=+x` 修正为 `100755`；② 退避循环若 `LABGEN_NS_DELETE_INITIAL_INTERVAL_S` 被配置为 0/负数会让 `sleep_for` 永远是 0、`elapsed` 永不增长，变成不会退出的忙轮询——已在 `config.py` 加 fail-fast 校验（启动时直接拒绝非法值，仿照既有 `LABGEN_K8S_VERIFIER_VM_ID`/`LABGEN_LAB_SESSION_TTL_MINUTES` 的 fail-closed 模式）+ 在 `_wait_for_namespace_deleted()` 内部加运行时下限兜底（双重防护），新增 4 个回归测试锁定这两类非法配置在启动时就报错而非运行时才炸。生产环境真实非 admin 账号（`pp-ops-e2e-01`）走完整 `LAB_ACTIVE` → 中止实验 → `LAB_CLOSED` 路径验证：`cleanup_verified=true`，无 `LAB_CLEANUP_FAILED`，无 VM taint，用 kubectl 独立核实 namespace 确实被删除，VM/tracker/credentials 状态一致。**调度基础设施**：新增两个 systemd service+timer（`deploy/systemd/`，`Persistent=true`）替代原有 cron——`k8s-netlab-backup-full-state`（全量一致性备份，原 cron `30 3 * * *` 已从 crontab 移除，避免重复执行；每天 03:30）、`k8s-netlab-data-retention`（DataRetentionService 清理，此前从未有过自动调度，历史上全靠人工偶尔手动跑；每周日 04:00，新增 `scripts/run_data_retention.py` CLI 入口 + `scripts/run-data-retention-cron.sh` 调度包装脚本，内部 flock 互斥）；其余 4 条 cron（`backup-data.sh`/`cleanup-orphan-vms.sh`/`stability_watch.sh`/`labgen_expire_sessions_cron.sh`）本次未触碰。`Persistent=true` 补跑机制用一次性 scratch timer（每分钟触发，`stop`跨越一个计划分钟后`start`）实测验证：任务在重启后几秒内立即补跑，而不是等到下一个自然触发时刻，验证完已清理 scratch 单元。`flock` 互斥同样实测：故意占住锁后运行 `backup-full-state.sh`，脚本立即 `exit 1` 并打印 FATAL，不会阻塞等待或并发执行。真实跑通一次 `k8s-netlab-backup-full-state.service`：15 个文件全部产出，`manifest.json` 记录的 SHA-256 独立重新计算全部匹配。**DataRetentionService 正式执行**（backup→dry-run→确认保护项→正式执行的顺序）：dry-run 确认 8 个 protected_records 精确覆盖 6 个已发布 K8s lab + Linux lab + 1 个 rehearsal_completed 的草稿，正式执行后 `zombie_draft_count` 22→0，`lab_review_diffs.json` 1.68MB→0.25MB，health 的 `sessions.status` 由 `degraded` 变回 `ok`；执行后确认 6 个 lab 仍 `is_startable=true`、6 篇文章仍 `published`、CTA 互链不受影响。新增 `docs/ops/LIMITED_AVAILABILITY_OPERATING_MODE_v1.0.md`（非 24×7 运行条件下的调度/备份查看/健康检查/关机前置检查清单）。全量测试 5157 passed，92.51% coverage，mypy 0 error。safety-reviewer + Codex 双重审查均已过（Codex 的 2 条真实发现已修复并回归验证）。本次任务范围严格限定于可靠性与调度基础设施：未修改 6 个已发布 lab 的教学内容、未修改 6 篇文章正文、未修改公开 allowlist、未新开 lab、未做任何迁移、未做 Growth
- fix(frontend): 老平台"网络实验"页面（`index.html`，服务 11 个网络实验）的实验文档抽屉遮挡终端问题 — 用户对比刚修完的 LabGen 终端问题时发现，`index.html` 的 `#doc-drawer` 一直是 `position: fixed` 的悬浮遮罩，展开时直接盖住终端内容左侧一截（终端本身不重排，只是被遮住），跟本次会话前面修的 LabGen 终端重连丢内容是完全不同的根因（纯 UI 层叠问题，不是 buffer 被 reflow 冲掉）。改为推挤式布局，与 LabGen 桌面端已验证过的模式（`labgen-session.html` 的 `#lab-drawer`）保持一致：把 `#doc-drawer`（连同其"网络实验/部署案例"tab、AI 助教聊天面板等全部子内容，逐字未改）从页面末尾的独立 fixed 元素，移动到 `.lab-panel` 内部，与终端列一起放进新的 flex row 容器（`.lab-body`），使其成为终端的 flex 兄弟节点——抽屉展开时终端真正收窄腾出空间，而不是被盖住。仅桌面宽度（>=769px）生效，移动端（<=768px）保留原有全屏遮罩行为不变（`app.css` 新增 `#doc-drawer`/`#doc-drawer-backdrop` 的 ID 选择器覆盖规则，用 ID 特异性精确覆盖共享的 `.doc-drawer` 类默认样式，不影响 `labgen-session.html` 自己的 `#lab-drawer`）。抽屉从"从不真正 resize 终端"变成"真的会 resize"后，顺带移植了 LabGen 那边已经踩过并修复的 xterm.js + fit-addon 已知坑：`fitAddon.fit()` 的 reflow 不会自动把视口滚动跟随光标，`terminal.js` 的 `fit()` 之前没处理这个（因为之前从不需要处理，抽屉从不真正 resize），现已照抄 LabGen 已验证的修法在 `fitAddon.fit()` 后调用 `terminal.scrollToBottom()`。新增 `tests/frontend/test_terminal_manager_fit.mjs`（4 用例：fit() 调用 fitAddon.fit()、fit() 在 fitAddon.fit() 之后调用 scrollToBottom()、fitAddon 未设置时不抛错、terminal 为 null 时不抛错），先确认"scrollToBottom 未被调用"这条在修复前失败（`git stash` 验证），修复后 4/4 通过；`terminal.js` 是非 ES module 的经典 script（`<script src>` 加载，顶层 `class TerminalManager` 声明），测试用 Node `vm` 模块加载并显式挂载到 sandbox 全局，沿用 `test_article_terminal_highlight.mjs` 已建立的同类模式。safety-reviewer 审查：no blocking issues found（重点核对了 DOM 挪动是否影响依赖 `closest()`/DOM 树位置的现有 JS 逻辑——`drawer.js`/`terminal.js`/`ai-tutor.js` 均用 `document.getElementById()` 查找、不依赖祖先链，无耦合风险；`#doc-drawer-backdrop` 留在原位置未随 `#doc-drawer` 挪动，因其自身 `position:fixed` 不受 DOM 位置影响，移动端仍可正确覆盖整个视口）。生产环境用 `smoke-admin` 账号真实创建 VM（501）+ 连接终端 + 展开抽屉，桌面宽度（1600px）截图确认抽屉与终端并排、终端完整可见不被遮挡；移动宽度（390px）截图确认仍是全屏遮罩、行为不变；测试 VM 已删除，health 全程 healthy，无新增错误日志
- fix(labgen): Pod Pending 发布前稳定性修复 — 修复终端重连后命令回显丢失的根因（不是仅加 loading 提示），并定位/修复 VM 过期清理误删活跃 LabGen 会话 VM 的真实根因 — CEO 任务简报，owner dogfood 5/5 通过后发现两个发布阻塞问题。**问题1（前端根因，已修复）**：每次终端重连（页面刷新，或 WS 空闲超时后重开——本项目无自动重连，"重连"等价于整页重新加载）后，`labgen-session-init.js` 首次激活时自动展开的 drawer 会触发一次延迟约 260ms 的 `fit()`（`_refitAfterDrawerTransition`），把此时已经写入终端缓冲区的 ready banner / 命令回显 / 输出静默冲掉——服务端日志证实命令每次都正确执行（`exit=0`），问题只在浏览器端渲染。定位到 `frontend/js/labgen-kubectl-terminal.js::connect()`：WebSocket（以及它能触发的所有写入）在 drawer 展开动画开始前就已经同步打开，写入时机与延迟 refit 产生竞态。修复：`connect(settleDelayMs)` 让 WebSocket 的创建延迟到该布局 transition 结算之后才发生，缓冲区在 refit 发生时还是空的，无内容可冲；`labgen-session-init.js` 在决定即将自动展开 drawer 时把 `DRAWER_TRANSITION_MS + 10` 传给 `connect()`。新增 `_connecting`/`_settleTimer` 两个实例字段防止延迟窗口内重复连接、`disconnect()` 取消未触发的定时器防止操作已析构的终端实例。真实生产环境验证：用非 admin 全新账号（`pp-stab-e2e-01`）走完整公开路径，页面 reload 后立即（0.3s 内）粘贴命令验证了"过早输入静默无反应"这一行为在改动前后完全一致（不是本次改动引入的新问题——`_handleInput()` 本身未改动，其入口保护条件 `!this._active` 在两版代码里都会吞掉过早输入，只是丢弃机制不同，对真实用户无感知差异，因为读步骤说明+复制+点击终端+粘贴的真实操作耗时远超这个窗口），随后按真实节奏连续执行 3 条命令（`get pods`/`patch`/`describe`）截图确认全部正确回显和渲染，零丢失。新增 `tests/frontend/test_labgen_kubectl_terminal_reconnect_race.mjs`（4 用例：无延迟时 WebSocket 同步打开、有延迟时 WebSocket 在延迟结束前不打开、延迟窗口内重复 `connect()` 不产生重复连接、`disconnect()` 能取消待触发的延迟定时器且不留下卡死状态）。**问题2（根因和 CEO 简报设想的不一样，已修复）**：CEO 简报假设是"老平台 30 分钟 VM 过期清理缺少活跃会话判断"，排查后发现 `backend/main.py::auto_cleanup_task` 的 `has_active_session_for_vm` 检查其实早已存在且工作正常（生产日志显示 VM 501 连续 8 分钟被正确跳过）。真实根因是生产环境 `/etc/labgen/home_lab_mvp.env`（systemd drop-in `EnvironmentFile`，加载顺序在主 `.env` 之后，同名变量会静默覆盖，见 `k8s-netlab.service.d/10-labgen-home.conf`）里有一行过时的 `LABGEN_LAB_SESSION_TTL_MINUTES=30`，覆盖了主 `.env` 里本应生效的 `90`（`/proc/<pid>/environ` 实测证实运行进程拿到的确实是 30 而非 90）。导致真实学员 session 在约 30 分钟（而非设计的 90 分钟）就被 `/api/labgen/runtime/expire-sessions`（10 分钟一次的 cron）标记为 `LAB_TIMEOUT`，才让本身工作正常的老平台 30 分钟 VM 存活清理任务顺理成章地删除了"看起来已结束但实际还在用"的 VM——两次真实生产事故（session `165ae671`/`c71ca82c`，均在 30-38 分钟被误判超时，VM 501/500 被删并 tainted）在本次排查过程中被发现和记录。修复是纯运维配置改动，**未修改任何 Python 代码逻辑**：删除该 env 文件里的重复声明，改为注释警告（不得在此文件重复声明该变量，说明 systemd 覆盖顺序和 2026-07-18 事故）；重启后 `/proc/<pid>/environ` 实测确认生效值变回 90。新增回归测试锁定既有代码路径的正确行为（`tests/test_auto_cleanup_permission_denied.py::TestAutoCleanupActiveLabGenSession` 4 个用例覆盖"过期+活跃会话不删"/"过期+会话已结束正常删"/"无会话的老平台 VM 行为不变"/"连续两次清理幂等"；`tests/test_labgen_vm_expiry.py::TestGetExpiryServiceWiring` 锁定 `get_expiry_service()` 确实把 `config.LABGEN_LAB_SESSION_TTL_MINUTES` 传给 `VMExpiryService` 而非静默退化为类默认值 30）；新增 `backend/api_routes.py` health 端点字段 `sessions.session_ttl_minutes`（暴露运行进程的**实际生效值**供人工核对，不用 SSH 进去读 `/proc/<pid>/environ`）+ 对应回归测试；`scripts/ops_smoke_check.sh` 新增检查项对比 `.env` 声明值与运行进程实际环境变量，检测任何同类 drop-in 覆盖漂移（safety-reviewer 抓到一个真实 HIGH：新检查在 `set -euo pipefail` 下用裸 `grep | cut` 且无 `|| true` 兜底，恰好在它要检测的"变量缺失/被覆盖"场景下会触发 `pipefail` 导致整个脚本静默中止、连后面的 mypy 检查都不会跑——已修复补齐 `|| true`，验证修复后脚本在故意制造缺失场景下能正常走完全部 12 项检查）。全量测试 5149 passed，92.52% coverage，mypy 0 error。safety-reviewer 审查（1 个 HIGH 已修复，见上）+ Codex review 均无阻塞问题。生产验证全程使用非 admin 全新测试账号（`pp-stab-e2e-01`），验证结束后已清理：dogfood 期间为验证临时加入的 `LABGEN_ENABLED_LAB_IDS`/`LABGEN_AUTO_VM_PROVISION_LAB_IDS`/`labgen_invites.json` 全部撤销回滚，验证产生的 `LAB_CLEANUP_FAILED` session（触发的是已有独立 backlog 的 abort 重试预算过短问题，非本次改动引入，已用 kubectl 核实 namespace 确已删除后标记 closed/verified）与 `tainted_vms.json` 均已清理，5 个已发布 lab/文章、`LABGEN_ENABLED_LAB_IDS`、provisioning、verifier 全程未受影响，health 全程 healthy，git 工作区干净（.env 与 `/etc/labgen/home_lab_mvp.env` 均不入库，改动记录仅存在于本条 CHANGELOG）。Pod Pending 是否/何时正式发布仍需 owner 另行决策，本次任务范围仅为清除两个发布阻塞问题，不做任何发布/新主题/Growth/教学内容改动
- fix(frontend): DNS Service Discovery lab owner dogfood 中"输入命令闪一下、无输出、光标停在原地"的问题复现 — 与之前 ConfigMap dogfood 排查出的 verifier vm_id 凭证 BLOCKER 是完全不同的根因，这次问题出在浏览器端。查生产日志确认：`lab_kubectl WS` 连接全程未断开、只成功执行了一条 `kubectl create deployment` 命令后再无任何 `Learner command` 日志——说明第二条命令根本没有作为 WebSocket 消息发出去，是前端问题而非后端/verifier 问题。定位到 `frontend/js/labgen-kubectl-terminal.js::_handleInput()`：只把 `code === 13`（CR）识别为 Enter，从 lab 步骤"建议命令" `<code>` 块里选中复制的文本，剪贴板内容通常以裸 `\n`（LF，code 10）结尾而非 `\r\n`——这个 LF 落进"忽略其它控制字符"分支被静默丢弃，既不触发发送、也不重置输入缓冲区，于是命令被逐字符回显到屏幕上（看起来像打完了），但从未真正调用 `ws.send()`，与用户描述的"闪一下、没输出、光标还在"完全吻合。**这不是 P0/ConfigMap 那次已修复问题的复发**，是同一症状描述下的另一个独立根因，此前从未被发现或修复过。项目缺少前端 JS 的浏览器/DOM 测试环境（本项目 `tests/frontend/*.mjs` 一贯只测纯函数，不搭 jsdom），但 `LabKubectlTerminal` 的构造函数和 `_handleInput()` 本身不直接依赖 DOM/xterm，可以在 stub 掉 `WebSocket.OPEN`/`this._ws` 后直接实例化调用——新增 `tests/frontend/test_labgen_kubectl_terminal_paste.mjs`（4 个用例：真实敲键盘 CR 正常发送、LF 结尾的粘贴此前会被吞掉现在必须发送、CRLF 粘贴不能发送两次、单独一个 LF 且缓冲区为空不发送空命令），先确认"LF 结尾粘贴不发送"这条在修复前失败，再修复 `_handleInput` 把 `code === 10` 与 `code === 13` 一并识别为 Enter（CRLF 场景下 CR 先触发一次真实发送，紧随其后的 LF 因缓冲区已清空而只是无害地重新显示提示符，不会发送第二次空命令）。修复后 4/4 新测试通过，`tests/frontend/` 全量 171 个用例里唯一失败的 1 个（`test_llm_provider_dev_view.mjs`）经 `git stash` 验证是修复前主干就已存在的、与本次改动无关的既有失败，不在本次范围内处理。静态文件走 `NoCacheStaticFiles`（`no-store`），修复对生产环境是免重启的，`curl` 实测确认新代码已生效。safety-reviewer 审查发现 1 个 LOW：`code === 13 || code === 10` 让 CRLF 结尾粘贴时 CR 和 LF 各自触发一次 Enter 分支（虽然 LF 触发时缓冲区已空、不会二次 `ws.send()`，但仍会多写一次空行并提前打印一次多余的提示符）——已修复为在遇到"紧跟在 CR 后面的 LF"时整体跳过（标准 CRLF 折叠处理），新增回归测试锁定"CRLF 只触发一次换行/提示符"。修复后 5/5 新测试通过
- fix(labgen): BLOCKER — verifier 凭证按学员自己的配额 VM 号查找，导致任何 vm_id 不是共享平台 VM（401/299）的会话 verify 一律 credential_missing — Owner 亲自 dogfood ConfigMap lab 时报告"进了终端敲命令没反应"，排查后定位到与终端表面症状完全无关的根因：`lab_kubectl_ws.py` 的学员 kubectl 终端始终连接同一个全局固定的共享 K3s 集群（`LABGEN_K8S_PLATFORM_KUBECONFIG_PATH`，完全不看 `session.vm_id`），但 `VerifierService.check()` 查找 verify 凭证时用的是 `session.vm_id`——学员在老平台 500-599 配额池里分到的记账 VM 号，而全平台只有 401/299 两个 VM 真正配置了 verifier 凭证。结果：只要学员的 `vm_id` 不是 401/299（新用户走自动建号或手动去 `/app` 建号后几乎必然如此），kubectl 命令在共享集群上明明执行成功了，verify 检查却一律悄悄返回 `credential_missing`，页面卡在当前步骤不推进，表现成"敲了命令没反应"。**这不是本次 ConfigMap sprint 引入的新 bug，是 LabGen K8s 学员会话自诞生以来就存在的架构缺陷**：此前"P0 Reader Path Repair"（HIGH-01）把"新用户没有 VM"这道更早的拦截打开后，新用户第一次能走到真正调用 verify 的这一步，才第一次实际触发这个问题；P0 当时的验证只覆盖了"建号→session创建→abort→cleanup"，没有真正跑一次 verify，所以没测出来。修复：新增配置 `LABGEN_K8S_VERIFIER_VM_ID`（生产 `/etc/labgen/home_lab_mvp.env` 设为 `401`，与学员终端连接的共享集群保持一致），`VerifierService` 新增可选构造参数 `verifier_vm_id`，`check()` 里 `credential_vm_id = self._verifier_vm_id or session.vm_id`，凭证查找按这个而非 `session.vm_id`，未设置时保留原有按 session.vm_id 查找的行为（向后兼容）。已在生产环境用真实 session（`vm_id=500`，完整复现 Owner 遇到的场景）验证修复前后行为差异：修复前 verify 返回 `credential_missing`，修复后 `all_passed=true`。safety-reviewer 审查发现 1 个 MEDIUM：`LABGEN_K8S_VERIFIER_VM_ID` 若被误配成非数字值，会在 `VerifierCredentialStore._validate_vm_id()` 深处抛出未捕获的 `ValueError`，导致所有 K8s-domain 会话 verify 变成 500 而非既有的结构化 `credential_missing` 失败——已修复为在 config 加载阶段做数字格式校验（参照既有 `LABGEN_LAB_SESSION_TTL_MINUTES` 的 fail-fast-at-startup 模式），配置错误时服务直接拒绝启动而非在学员会话中静默产生 500。排查过程中顺带发现并修复两个独立的运维事故：① 我此前为跑 rehearsal 手动把共享 VM 401 track 进了老平台的 VM 过期追踪表，导致自动清理任务每分钟打一条"VM 401 expired"日志（好在 `VM_CLEANUP_EXEMPT_IDS` 保护它没被真删）——已 untrack 撤销；② 两个学员 session 因 `lab_timeout` 自动过期时清理调用超时（约28秒），K8s 侧命名空间其实已经删除成功，但系统误判为 `cleanup_failed` 并把共享 VM 401 标记 `tainted`，导致平台一度无法在 VM 401 上新建任何 session/rehearsal——已核实命名空间确实不存在后清空 `tainted_vms.json`，并对遗留的 `LAB_CLEANUP_FAILED` session 按 RUNBOOK 场景七走 `force-close`（`residual_risk=false`）恢复，这两点均为本次 dogfood 暴露的真实生产问题，非本次代码改动引入，记入待办（超时清理误判连带污染共享 VM 的问题尚未修复，值得作为后续独立 sprint 处理）。新增 4 个回归测试（`test_labgen_verifier.py::TestVerifierServicePinnedVmId` 3 个 + `test_labgen_production_preflight.py::TestConfigModuleVerifierVmIdFailClosed` 3 个），全量 5083 passed，92.17% coverage，mypy 0 error
- fix(test): 缓解 `test_concurrent_login_rate_limiter_thread_safe` 在全量测试套件中间歇性失败的问题 — 推送一批纯文档改动时被 pre-push hook 的全量测试拦下，`AssertionError: 并发登录出现意外状态码: {200, 404}`，单独跑该测试文件从不复现，只在全量套件里连续 4 次必现。**诚实记录一次两轮才收敛的排查**：经过两轮 safety-reviewer 审查，前两版诊断都把"根因"写成了确定性断言但缺乏代码依据（第一版猜测是 starlette TestClient 的 ASGI portal 惰性初始化竞态，被指出该测试 fixture 未使用 `with TestClient(app) as client:`、每次请求本就各自独立创建 portal，这个诊断站不住脚；第二版改为 `Starlette.__call__` 里 `middleware_stack` 惰性构建的无锁 check-then-set 竞态，还写错了 venv 实际的 starlette 版本号——实际是 1.3.1，不是最初写的 1.0.0，被再次指出"从竞态到 404 的具体因果路径"没有代码证据，`build_middleware_stack()` 每次重建的中间件链在功能上应当等价，理论上不应产生导致路由 404 的中间态）。最终结论：**这是经验性缓解，不是已定位根因的修复**。已核实的事实是该 fixture 确实让 10 个线程各自真并发（独立事件循环），`middleware_stack` 的 check-then-set 确实无锁，但竞态具体如何转化为 404 仍未证实，也不排除是其它原因（其它测试文件对共享 router 单例的影响、httpx 连接池多线程行为等）。缓解方式：在开线程池前先用同一个 client 做一次同步登录预热请求（对被测逻辑无副作用），修复后单独重跑 5 次 + 全量套件重跑 1 次（`pytest tests/ -x -q --tb=short`，与 pre-push hook 完全一致的调用方式）均稳定通过，但如果这个 flake 未来重新出现，说明预热只是缩小了触发窗口而非根治，需要继续排查
- fix(deploy): 修复 Cloudflare 边缘节点无视 origin `Cache-Control` 头、把 `/js/*`/`/css/*` 浏览器缓存策略强行改成 4 小时的问题 — owner 反馈"看不到 P3 改版效果"，排查后发现根因和之前以为已经修好的一个既有 bug 是同一个：`backend/main.py::NoCacheStaticFiles` 此前设置 `Cache-Control: no-cache, must-revalidate`（`tests/test_static_no_cache_headers.py` docstring 记录这是修复"Real Human Re-validation for Labs 2-4 v0.1"期间发现的 Cloudflare 4 小时边缘缓存问题时加的），单元测试（`TestClient`，不经过 Cloudflare）一直显示通过，但今天用 `curl -I` 走真实域名验证时发现 Cloudflare 边缘节点会把响应头**改写**成 `max-age=14400`——`no-cache` 语义上仍然"允许缓存，只是复用前必须先重新验证"，Cloudflare 的默认 Browser Cache TTL 规则会覆盖这类较弱的指令；`no-store`（彻底禁止缓存，语义更强）Cloudflare 才会照办。**这暴露了一个更值得记住的教训**：`tests/test_static_no_cache_headers.py` 这类只测 origin 响应头的单元测试，永远无法发现 CDN 层对响应头的篡改——必须走真实域名 `curl -I` 才能验证 CDN 是否真的按预期转发。修复：`NoCacheStaticFiles` 改为设置 `no-store`；生产环境用 `curl -sI https://lab.cloudnetops.tech/js/...` 实测确认 `cache-control: no-store` + `cf-cache-status: BYPASS`，问题解决。更新 2 个既有测试的期望值（`no-cache, must-revalidate` → `no-store`），先 fail 后 fix 验证过。这条修复解释了本次 dogfooding 过程中至少两次"代码明明改了、页面却看不到效果"的现象（一次是我自己验证 P1/P2 时用全新浏览器 profile 绕过去的，未深究；这次 owner 反馈 P3 时才彻底定位到根因），此前均被误判为"浏览器缓存，硬刷新即可"，实际是同一个 CDN 层问题的重复发作

### Changed
- feat(labgen): P0 Reader Path Repair — First Wave 三个 lab（CrashLoopBackOff/Service No Endpoints/ImagePullBackOff）的 Start Lab 流程支持自动 VM provisioning，解决生产核验发现的 HIGH-01：全新注册、从未用过老 VM 平台的读者点文章 CTA 进 lab 页点 Start Lab 会撞上 `no_vm_assigned` 422，被要求手动跳转 `/app` 建 VM，破坏"读了文章马上能做实验"的产品承诺。CEO/CTO 决策后实现：检测到用户无 VM 且 lab_id 在新配置白名单 `LABGEN_AUTO_VM_PROVISION_LAB_IDS`（默认空，安全默认）内时，后台自动触发 VM 创建（复用现有 Proxmox provisioning，不重造 runtime——调查确认 LabGen 的 K8s 实验本就跑在共享平台集群，`vm_id` 只是配额记账字段），不再要求用户跳转 `/app`；非白名单 lab 与已有 VM 的用户路径完全不变。新增 `backend/labgen/vm_provisioning.py`（幂等 job 服务，`get_or_start_provisioning()` 的"查 in-flight + 写入新 job"整体在 `safe_update_json` 的一次持锁周期内完成，防止并发点击 Start Lab 建出多台 VM；stale job（服务重启导致内存态丢失）超过 600s 允许重新触发，用新 vm_id，不覆盖任何残留资源）；重构抽取 `backend/vm_manager.py::provision_vm_for_user()` 作为 `POST /api/vms/create` 和 LabGen 自动 provisioning 共用的核心创建逻辑（reconcile→配额→限流→分配→create_vm→track_vm），通过关键字参数依赖注入保持 `api_create_vm` 现有全部测试的 mock 路径不变；`create_lab_session` 的 `no_vm_assigned` 分支新增白名单判断，命中时返回同结构 422 但 `code` 换成 `vm_provisioning`/`vm_provisioning_failed`（绝不透传 VMID/Proxmox/原始异常文本）；新增 `GET /api/lab-sessions/vm-provisioning-status` 轮询端点。前端 `labgen-lab-init.js` 新增分支处理新 code，自动轮询（新文件 `labgen-vm-wait.js`）到 ready 后自动重试 `startLab()`，失败/超时只显示通用重试文案；原 `no_vm_assigned` + 跳转 `/app` 分支原样保留服务非白名单 lab。新增 3 个测试文件（`test_labgen_vm_provisioning.py` 24 个含真实多线程并发验证 flock 幂等生效、`test_vm_manager.py` 新增 `TestProvisionVmForUser` 7 个、`test_labgen_lab_auto_provisioning_cta.py` 前端字符串回归 8 个）+ `test_labgen_lab_session.py`/`test_labgen_api_contract_pack.py` 追加回归用例，全量 5046 passed，92.25% coverage，mypy 0 error
- feat(labgen): 学员实验目录页（`labgen-catalog.html`）按领域（Kubernetes / Linux）分区展示，视觉整体优化 — owner 看到实际线上目录页截图后反馈"毫无美感"，并指出如果产品已经把 Linux 和 K8s 当作两条不同的赛道，目录页就该把它们独立分开，而不是混在一个平铺网格里（此前 4 个 lab——1 个 Linux + 3 个 K8s——挤在同一个 grid 里，长标题不做截断导致卡片高度参差不齐、无任何分组线索）。后端 `LearnerLabCatalogItem`（`backend/labgen/learner_catalog.py`）新增 `target_domain: str`（`"k8s"` | `"linux"`，取自 `LabDraft.target_domain`），先写失败测试（`tests/test_labgen_learner_catalog.py::test_catalog_item_exposes_target_domain`）验证 API 返回该字段再补实现。前端 `labgenViews.js::renderLabCatalog()` 按 `target_domain` 分组渲染为两个独立 `<section>`（各自标题+图标+数量+简介），K8s 用品牌蓝 `k8s-blue` 顶部强调条，Linux 用琥珀色 `amber-500`，某个领域没有已发布 lab 时对应分区标题不渲染；缺失/未识别的 `target_domain` 兜底归入 K8s 分区，保证旧数据和既有测试（未传该字段）行为不变。同时改进单张卡片视觉：标题 `line-clamp-2`、简介 `line-clamp-3` 消除参差不齐，加卡片悬浮上浮阴影、底部分隔线+objectives/steps 统计+"开始 →"提示。新增 5 个回归测试（后端 1 个 + 前端 4 个：分区标题存在、lab 落在正确分区、无 lab 的领域不渲染标题、缺失 `target_domain` 兜底为 K8s），全量测试 4994 passed，92.23% coverage。后端字段新增需要重启服务生效
- feat(articles): 文章页加三栏布局——左侧本文目录、右侧更多文章列表 — owner 反馈宽屏下文章正文两侧空得太多，要求左侧做"层次性的文章段落检索"、右侧给文章列表。`article.html` 从单栏 `max-w-3xl` 改为 `max-w-6xl` 三栏网格（`lg:grid-cols-[200px_1fr_260px]`），`lg` 断点以下（窄屏/手机）两侧栏整体隐藏、只保留居中正文，不挤占移动端阅读宽度。`article.js` 新增两个逻辑：① `_buildToc()` 在正文渲染完成后扫描 `.prose h2` 标题，用 `_slugify()` 生成 id（对每个标题的 index 取模，保证同名标题不冲突）赋给标题元素（通过安全的 `.id` IDL 属性赋值，不经过 innerHTML），左侧目录用 `createElement`+`textContent` 构建锚点链接列表，无标题时侧栏整体隐藏（用 `style.display='none'` 强制隐藏，不是 `classList.add('hidden')`——后者因为侧栏本来就带 `hidden lg:block` 两个 class，重复添加 `hidden` 是空操作，在 `lg:block` 生效的桌面宽度下不会真的隐藏，必须用内联样式覆盖）；② `loadRelatedArticles()` 拉取 `/api/articles` 公开列表接口，过滤掉当前文章，右侧渲染其余已发布文章卡片（标题经 `escapeHtml()` 转义）。safety-reviewer 审查发现一处真实 MEDIUM 缺陷：`loadRelatedArticles()` 的 `/api/articles` 请求返回非 2xx 状态码（`!r.ok`）时原本直接 `return`，没有像 fetch 失败/空列表两条分支那样隐藏侧栏，会在接口 500/429 时露出一个"更多文章"标题+空列表的空白框——正是同一个 commit 里刚描述过的"空白框"缺陷模式，在第三条分支上又漏了一次，已修复为三条降级路径一致隐藏侧栏。新增 14 个回归测试（`tests/frontend/test_article_toc_and_related.mjs`：`_slugify` 的中英文标题/空标题兜底/同名标题不冲突，`_buildToc` 只用安全 DOM API 赋值 id，以及 `loadRelatedArticles` 的 4 种路径——happy path 转义校验+当前文章过滤、非 2xx、fetch 拒绝、空列表，后三种都断言侧栏被隐藏），同步修了一个既有测试文件的隐藏用例（`test_article_terminal_highlight.mjs` 的 fake element stub 缺 `.closest()`，导致新增的 `loadRelatedArticles()` 异步链在测试沙箱里抛出未处理的 rejection）。用 Playwright 截图生产环境真实文章页验证：桌面宽屏三栏正常（左侧目录含全部 8 个小标题锚点、右侧列出其余 4 篇已发布文章）、手机窄屏正确收缩为单栏、无空白浪费。全量测试通过。纯前端改动，无需重启服务即生效
- feat(articles): 文章页代码块加终端窗口样式 + 关键诊断词高亮，对齐 Artifact 预览稿视觉 — owner 把此前的深色 Artifact mockup（手工做了三色圆点终端标题栏 + 关键词高亮）和刚上线的真实 `article.html`（marked.js 渲染的普通代码块，无标题栏、无高亮）放在一起对比，问"还是有区别"。在 `article.js` 新增 `_enhanceCodeBlocks()`：渲染完 Markdown 正文后，对每个 `<pre>` 用 DOM API（`createElement`/`appendChild`，非 innerHTML 拼接）包一层终端窗口外壳（三色圆点 + 标签），依据 marked.js 生成的 `language-bash` class 区分"命令"（标签 `$ kubectl`）与"输出"（标签 `output`）两种代码块。输出类代码块额外过 `_highlightDiagnosticText()`：先对原始文本整体 `escapeHtml()`，再在已转义的字符串上做固定关键词（`CrashLoopBackOff`/`ImagePullBackOff`/`ErrImagePull`/`not found`/`Failed`/`Error`/`Exit Code:` 后的数字）正则替换包 `<span class="hl-bad">`——顺序是"先转义、后高亮"，高亮词表是硬编码的纯 ASCII 词，不含 HTML 特殊字符，因此不会因为高亮逻辑重新引入未转义内容，即使 Directus 文章正文里混入恶意标签也不会绕过转义生效。新增 5 个回归测试（`tests/frontend/test_article_terminal_highlight.mjs`，用 Node `vm` 模块把 `article.js` 加载进隔离 sandbox 单测这两个纯函数），其中一条专门验证"转义在高亮之前发生"这一 XSS 安全性质（构造同时含恶意 `<img onerror=...>` 和合法关键词 `not found` 的输入，断言原始标签只以转义形式存在、关键词高亮仍正常生效）。用 Playwright 直接截图生产环境正在使用的 CrashLoopBackOff 真实文章页确认效果，与 Artifact 预览稿视觉一致。全量测试通过（Python 4993 passed + 前端全部 `.mjs` 测试套件，含新增 5 个）。纯前端改动，无需重启服务即生效
- feat(articles): 公开文章页（`article.html`）改为深色 DevOps 风格，与学员实验流程三页统一 — First Wave 三篇文章刚真实上线后，owner 用 Artifact 深色 mockup 和线上文章页浅色实际效果对比，问"风格怎么变了"。经确认：Artifact 只是此前的本地预览稿，从未应用到 `article.html`；`article.html` 从建站起就一直是默认浅色 Tailwind 样式，没被之前那次学员流程三页的深色改版覆盖到。复用已有的 `devops-*` token（`frontend/js/tailwind-config.js`，无需新增）和 IBM Plex Sans/JetBrains Mono 字体，把 `article.html` 的 nav/正文 `.prose`（标题/代码块/表格/引用块/链接）与 `article.js` 里动态渲染的登录态导航、评论表单、评论列表、配套实验 CTA 卡片全部改深色。范围显式限定：只改 `article.html`，不动 `landing.html`（首页/文章列表）——用户在方向选择时明确要求最小改动范围。`article.js` 经确认只被 `article.html` 引用，不存在跨页面泄漏风险。补 8 个回归测试（`tests/test_article_dark_devops_theme.py`）锁定深色 token 存在、旧浅色 CTA 卡片/评论卡片 class 字符串不再出现、`landing.html`/`index.html` 不含 `devops-bg`、`article.js` 未被其它页面引用。用 Playwright 直接截图生产环境正在使用的 CrashLoopBackOff 真实文章页（该文章当天已由 owner 授权正式发布），确认深色渲染、代码块/引用块/系列文章互链/配套实验 CTA/评论区全部正常显示，无控制台报错。全量测试 4993 passed，92.23% coverage。纯前端改动，无需重启服务即生效
- feat(labgen): 学员实验流程三页（`labgen-catalog.html`/`labgen-lab.html`/`labgen-session.html`）整体改为深色 DevOps 风格 — owner 反馈上次的 P3 改版"还是太丑，没办法放出去"。调研了 Grafana/GitHub Dark/Kubernetes Dashboard/Vercel 等 DevOps 工具类平台的共同视觉语言（近黑背景 + 等宽字体点缀技术标签 + 克制的语义色），落地为近黑背景（`#0B0F17`）+ 卡片面（`#121826`/`#1A2233`）+ K8s 品牌蓝（`#326CE5`，不变）+ 半透明语义色徽标（绿/琥珀/红），正文字体 IBM Plex Sans、步骤编号/命令/badge 用 JetBrains Mono。改动方式：`frontend/js/tailwind-config.js` 新增 `devops-*` 颜色 token 和 `font-plex`/`font-plex-mono` 字体族（纯新增，不改 `k8s-blue`/`k8s-dark` 等既有 token，其它页面零影响）；`labgenViews.js` 中仅 `renderLabCatalog`/`renderLabDetail`/`renderSessionView`/`_statusBadge`/`_alertBox`/`_renderCheckSummary` 直接改深色（这几个函数只被这三个页面调用，不影响 admin/dev 页面）；`renderLoading`/`renderErrorState`/`renderNotFound` 三个和 admin/dev 页面共用的函数改为 `theme` 可选参数（默认 `'light'` 保持原有调用点行为不变，三个学员页面的 init.js 显式传 `'dark'`）；`app.css` 新增 `.theme-dark` 作用域下的 `.doc-drawer`/`.step-pill` 深色覆盖规则（`index.html` 共用同一份 CSS 但不设置 `theme-dark` class，样式不受影响）。补 26 个前端回归测试（`tests/frontend/test_dark_devops_theme.mjs` 11 个 + `tests/test_labgen_dark_devops_theme.py` 15 个）锁定深色 token 存在、验证改动范围确实只限三个页面（`index.html`/`login.html`/`admin.html` 不含 `devops-bg`）、验证共享函数的 `theme` 参数默认值未破坏 admin/dev 调用点；同步修正 1 个既有测试断言字符串（`text-red-600` → `text-red-400`）。用 Playwright（复用已安装的 Chromium，未走 gstack skill 完整工作流）在本地 dev server（`127.0.0.1:8123`，独立于生产）登录 `smoke-admin` 截图验证目录页与详情页真实渲染效果；session 页面因涉及真实创建 K8s 实验环境，改用 pure 函数 mock 数据本地渲染截图自测（未触碰任何真实 session），三页视觉统一、控制台无报错。全量测试 4986 passed，92.23% coverage。纯前端改动，无需重启服务即生效
- feat(labgen): Lab 详情页（`labgen-lab.html`）视觉改版 — 此前内容区排版过于简陋（标题+摘要+纯色块警示框+无层次的项目符号列表）。改版内容：标题下新增步骤数/难度徽标；Background/学习目标/实验步骤三个区块统一卡片化，学习目标用勾选图标列表，实验步骤改为带连接线的圆形编号纵向时间轴（每步展示 `learner_goal` 摘要 + 自动校验项数）；"运行环境检查已延迟"提示和"暂不满足开始条件"错误框统一改为图标+标题+正文的 `_alertBox()` 组件（对应现有 amber/red 配色语义），文案改为中文（与站内其它页面一致，此前唯独这个页面是英文）。改版过程中发现并修复一个真实数据 bug：`renderLabDetail()` 一直读取 `lab?.steps`，但后端 `LearnerLabDetail` 模型（`backend/labgen/learner_catalog.py`）真实字段是 `steps_preview`（结构也不同：`step_id`/`title`/`learner_goal`/`instructions_summary`/`expected_outcome_summary`/`check_count`），导致"实验步骤"区块在生产环境自始至终从未渲染过——从 owner 最早发的截图就能验证：Objectives 有内容但完全没有 Steps 区块。既有测试恰好全部用错误字段名 `steps` mock 数据，与这个 bug"完美契合"所以从未被测试捕获。safety-reviewer 审查发现新增的 `_alertBox()` helper 对 `title`/`body` 参数不做内部转义，违反本文件"所有动态值必须先过 `escapeHtml`/`sanitizeDisplayText`"的既定约定（当前两个调用点均安全——分别是纯静态文案和已转义拼装的 HTML 片段，非阻塞发现，但是留给未来调用者的隐患）：已加固，`title` 参数内部强制 `_safe()` 转义（约定为纯文本，调用方不得传入标签），`body` 参数改名为 `bodyHtml` 让"调用方必须自行保证已转义"这个契约在参数名上显式体现。新增 1 个前端回归测试覆盖 `steps_preview` 字段映射（先 fail 后 fix 验证过），另更新 1 个既有测试的断言字符串（英文文案改中文后同步）。用无头浏览器对 pure 函数输出做了可开始/不可开始两种状态的本地渲染截图自测（未登录任何账号，未触碰任何真实 session），确认排版正常、控制台无报错。纯前端改动，无需重启服务即生效

### Fixed
- fix(labgen): 修复终端最上面几行文字排版错位（"Lab terminal ready. Namespace: ..." 和 "Use kubectl commands..." 之间出现一大段错位空白）— owner 完整跑完 Service No Endpoints lab 后仍报告这个问题，确认是与前几条改动（终端安全提示/fit 时序/scrollToBottom）不同的另一个独立根因：WS `ready` 消息的 `msg.msg` 字段用裸 `\n`（Python 字符串字面量）换行，但 xterm.js（`convertEol` 默认 `false`）把裸 `\n` 当作"只换行不归位"（不回到列 0），导致第二行文字从上一行光标结束的那一列开始打印，形成阶梯状错位。同一文件里 `output`/`blocked`/`error` 三个消息类型分支早就用 `.replace(/\r?\n/g, '\r\n')` 做了归一化，唯独 `ready` 分支没做——这是一直存在的既有 bug，只是之前被"服务端和客户端各写一遍安全提示"那次一起清理的文字混杂在一起，没能单独定位出来，这次移除干扰内容后才清楚暴露。修复：`ready` 分支的 `msg.msg` 同样先 `.replace(/\r?\n/g, '\r\n')` 归一化再写入，兜底默认值同步改为走同一条归一化管线（避免"默认值手写 CRLF、真实消息却裸 \n"这种两套换行处理并存的写法继续留着复发风险）。新增 1 个前端回归测试，先 fail 后 fix 验证过。safety-reviewer 审查 no blocking issues。纯前端改动，无需重启服务即生效
- fix(labgen): 修复"敲回车后刚输入的命令行本身消失"的终端 bug — owner 测 Service No Endpoints lab 时报告，排查后定位到 xterm.js + fit-addon 的一个已知交互问题：`fitAddon.fit()` 会把终端缓冲区按新列数 reflow，但不会让视口自动跟随光标滚动——如果 reflow 让当前行在更窄列数下需要占用更多行，当前行会滚到可视区域上方，看起来像"消失了"，实际内容还在缓冲区里。这个交互隐患本身是 xterm.js 的既有特性（不算这个项目独有的 bug），但此前从未真正暴露过，因为此前从没有代码会在页面加载后不久、学员很可能正在输入第一条命令的时间窗口内主动触发一次 resize——上一条改动新增的"抽屉首次自动展开"功能（`_refitAfterDrawerTransition()`，页面加载后 260ms 触发一次 `fit()`）第一次把这条触发路径接了进来，把一个理论风险变成了会真实发生的问题。修复：`LabKubectlTerminal.fit()` 在 `fitAddon.fit()` 之后追加 `terminal.scrollToBottom()`（xterm.js 官方推荐的标准搭配写法）；`connect()` 里的首次 fit 和 `window resize` 监听器原先各自直接调用 `_fitAddon.fit()`，统一改为都调用 `this.fit()`，确保只要是走 fit 的路径都会带上这次修复，不遗漏任何一个调用点。safety-reviewer 审查 no blocking issues，指出 `scrollToBottom()` 在每次 resize 时无条件触发，理论上如果学员正手动往上翻看历史输出、此时恰好发生一次 resize（如移动端软键盘弹出触发的 resize），视口会被拽回底部打断阅读——这是标准修复方式的已知代价，本次予以接受，未做额外的条件判断（避免过度设计）。新增 1 个前端回归测试（`test_labgen_session_layout.mjs`，断言 `fit()` 方法体同时包含 `_fitAddon.fit()` 和 `_terminal.scrollToBottom()`，且不再有绕过 `this.fit()` 直接调用 `_fitAddon.fit()` 的旧写法），先 fail 后 fix 验证过。纯前端改动，无需重启服务即生效
- fix(labgen): 修复"已通过步骤的操作命令永久消失"的产品缺陷 — owner 测 Service No Endpoints lab 时反复卡在步骤2（`kubectl get endpoints web-svc` 持续 NotFound），排查发现根因：`frontend/js/labgenViews.js::renderSessionView()` 的 `stepDo`/`stepCmds`/`stepHint` 只在 `isCurrent` 时渲染——一旦某步骤被标记 `passed`，它的操作说明和命令代码块就从面板里永久消失，没有任何入口能再看到。本次 lab 的步骤1（创建 Deployment + Service）恰好没有配置自动校验条件，点一下"检查当前步骤"就直接 `passed`（不代表命令真的被执行过），用户据此以为已完成，实际上从未在终端里跑过这两条建库命令；进入步骤2后面板上只剩步骤2唯一一条命令可见，学员反复重跑它，而它必然因为对应资源不存在而失败——面板从未再展示过步骤1的命令，学员无从得知需要回头补跑。用 `kubectl get all -n <该 session 的 namespace>` 直接核实该 namespace 确实空无一物，验证了这一诊断。修复：`isPassed` 步骤的操作内容改为默认展开的 `<details open>`（"本步骤命令"，仍可折叠），不再彻底隐藏，`isCurrent` 步骤保持原有展开显示；仅 `locked`/`pending` 步骤维持完全隐藏（尚不可操作）。safety-reviewer 审查 no blocking issues，指出折叠默认收起本身可能延续"发现不了"的问题——鉴于这正是本次真实卡住学员的根因，采纳建议改为默认展开而非默认折叠。新增 1 个前端回归测试（`test_views.mjs`，断言 `passed` 状态步骤的 `step_commands` 仍出现在渲染结果里），先 fail 后 fix 验证过。纯前端改动，无需重启服务即生效

### Fixed
- fix(labgen): 修复上一条改动（实验步骤抽屉从覆盖层改为可伸缩侧栏）引入的真实回归 — owner 测第二个 lab（Service No Endpoints）时报告终端输出仍然错位，排查后发现根因是 `openDrawer()`/`closeDrawer()` 此前在切换抽屉 CSS class 的同一刻**同步**调用 `_terminal.fit()`，但桌面端 `#lab-drawer` 的宽度变化是 250ms 的 CSS transition，不是瞬时的——`fit()` 在过渡进行到一半时测量容器宽度，拿到的是过渡中间态的错误列数，之后每次终端输出都基于这个错误列数换行，导致后续文本相互覆盖而非正常换行（用户截图里长命令回显在中途被输出内容覆盖，`--replicas=1` 消失不见，正是这个症状）。这个问题在抽屉还是覆盖层设计时不存在（因为覆盖层不改变终端容器尺寸，`fit()` 测哪个时间点结果都一样），是本次侧栏重构的直接副作用，只有真实点开抽屉再执行长命令才会暴露，静态测试和 P1 提交前的验证截图都没跑到这条路径。修复：移除 `openDrawer()`/`closeDrawer()` 里同步的 `_terminal.fit()` 调用，改为在 `#lab-drawer` 上监听 `transitionend` 事件（过滤 `propertyName === 'width'`），等宽度过渡真正结束后再 `fit()`。新增 1 个前端回归测试（`test_labgen_session_layout.mjs`，断言 `openDrawer`/`closeDrawer` 函数体内不再包含 `_terminal.fit()`，且存在按 `transitionend`+`width` 过滤的监听器），先 fail 后 fix 验证过。纯前端改动，无需重启服务即生效。排查过程中同时确认"检查当前步骤"按钮点击无反应是另一件事——是我本人为验证 P1/P2 反复用共享的 `smoke-admin` 账号登录，触发后端"每用户仅保留一个活跃 session"的驱逐逻辑，顶掉了 owner 当时的登录状态，导致该请求返回 401，不是代码缺陷，未改动任何代码，仅记录为操作教训（后续验证改用独立账号，避免与正在使用的用户抢占同一账号 session）

### Added
- feat(labgen): owner 亲测 CrashLoopBackOff lab 时发现的 3 处 UI 问题一并修复。① 实验步骤抽屉此前是 `position:fixed` 全屏覆盖层（`.doc-drawer`，z-index 40）+ 全屏半透明 backdrop（z-index 35），打开时整个终端连同背景一起被压暗遮挡；改为桌面端（≥768px）左侧常驻可伸缩侧栏——`labgen-session.html` 新增 `#session-body-row` flex 容器把 `#lab-drawer` 和 `#session-terminal-wrap` 变成同级 flex 子项，`#lab-drawer` 用页面级 ID 选择器覆盖共享的 `.doc-drawer` 样式（`position:static`，`width:0→440px` 过渡），backdrop 在桌面端 `display:none`；移动端（<768px）保留原有全屏覆盖行为（媒体查询显式覆盖）。未改动 `app.css` 里的共享 `.doc-drawer`/`.drawer-backdrop` 类，`index.html`（普通实验文档抽屉）不受影响。② 步骤面板入口此前只在顶栏一个不起眼的按钮，容易被忽略；`labgen-session-init.js` 新增桌面端首次进入实验时自动展开抽屉（`_hasAutoOpenedDrawer` 一次性标记，仅首次触发，不会在用户手动关闭后被后续快照刷新重新打开；`DESKTOP_DRAWER_BREAKPOINT_PX` 门控，避免移动端一进入就被全屏抽屉盖住刚连上的终端）。③ 终端安全提示（"仅限本实验范围使用，勿输入真实密码/Token"）此前分别由客户端 `_showSecurityNotice()` 和服务端 WS `ready` 消息的 `msg` 字段各写一遍 ANSI 转义文本进终端缓冲区，两处都会随终端当前列宽换行，在多数宽度下读起来错位凌乱；现改为 `labgen-session.html` 里终端标题栏下方的一条静态 HTML banner（amber 背景 + 图标），移除客户端 `_showSecurityNotice()` 方法及其调用，`backend/labgen/lab_kubectl_ws.py` 的 kubectl 学员终端和 Linux 学员终端两处 WS `ready` 消息的 `msg` 字段均去掉安全提示行，只保留操作性文本（`Lab terminal ready. Namespace: ...` / `Use kubectl commands...`）。新增前端 5 个断言测试（`tests/frontend/test_labgen_session_layout.mjs`，无 jsdom，断言静态 HTML/CSS/JS 源码而非渲染后 DOM，均先 fail 后 fix 验证过）+ 后端 2 个回归测试（`test_labgen_terminal_coverage.py`/`test_labgen_linux_terminal_ws.py` 各新增一条断言 `ready` 消息不含安全提示文本）。生产验证：用 `browse` headless 浏览器实测登录 `smoke-admin` 打开 CrashLoopBackOff lab 真实 session，确认新布局侧栏与终端并排显示、无遮挡；发现浏览器对未加版本号的 JS 模块有 4 小时 `Cache-Control: max-age=14400` 缓存（`labgen-kubectl-terminal.js` 等非入口文件均未加 `?v=` 查询参数，仅顶层 `<script src="...">` 有），验证时用全新 headless 浏览器 profile 绕过；该缓存问题是本次改动之外的既有架构缺口（下次改动这批文件时应考虑补版本号），本次未处理，记为技术债
- feat(labgen): 接线 LabGen 学员会话超时自动清理 — 后端已有 `POST /api/labgen/runtime/expire-sessions`（admin-only）能按 TTL 识别并清理超时未关闭的会话（namespace + Deployment/Pod/Service），但此前从未被任何调度器调用，学员若做完实验不手动执行清理步骤就直接关闭浏览器，namespace 会永久残留集群里。owner 亲测 CrashLoopBackOff lab 时发现该 gap。新增 `scripts/labgen_expire_sessions_cron.sh`（登录 admin 账号 + 调用该端点，凭证从 `/root/.k8s-netlab-admin-credentials` 读取），接入 crontab 每 10 分钟运行一次。运行首次调用即清理出 2 个此前遗留的僵尸会话，证实泄漏是真实存在的（非假设风险）。因为该接口现在会被定期强制调用，`LABGEN_LAB_SESSION_TTL_MINUTES` 从默认 30 分钟上调至 90 分钟（`.env` 新增），避免学员正在专注做实验时会话被强制判定超时清理

### Fixed
- fix(labgen): 修复 `VMExpiryService._SKIP_STATUSES` 遗漏 `LAB_FORCE_CLOSED` 的问题 — 补回归测试防止重现。接线上面的自动清理 cron 时发现：`LAB_FORCE_CLOSED` 是 admin 专用的终态覆盖（`lab_session_service.py::_ALREADY_ENDED_STATES` 已将其视为终态），但 `vm_expiry.py::_SKIP_STATUSES` 从未包含它，导致 `find_expired_sessions()` 会一直重复选中已经 force-closed 的旧会话，每次 cron 运行都对其调用一次空操作的 `timeout_session()`（因 `_ALREADY_ENDED_STATES` 保护未造成实际破坏，但会造成无意义的重复处理和日志噪音，且会一直"误报"为本次清理成功）。实测在生产环境首次运行 cron 时命中 2 个这样的历史会话。修复：`_SKIP_STATUSES` 补上 `LabSessionStatus.LAB_FORCE_CLOSED`
- fix(labgen): 修复 P0 RC 一致性问题 HIGH-01 — CrashLoopBackOff lab 的 LabDraft CTA 字段与 Directus 文章真实状态不一致，导致对外表现为"文章已发布可读"但实际文章处于 `draft`（`GET /api/articles/{slug}` 返回 404 语义）— 采用保守对齐修复，不擅自变更发布决策。根因确认：Directus `activity`/`revisions` 记录显示 2026-07-01T20:25:36Z 同一 admin 账号对 article id=4 执行了一次显式 `update`（delta `{"status": "draft"}`），即该文章在 2026-06-28 `PUBLISHED_VERIFIED` 锁定之后被人工/脚本改回 draft，但 LabDraft（`bb4fe651-...`）的 `cta_enabled=true`/`article_url` 未同步降级，状态漂移持续到 2026-07-13 RC 检查才被发现（HIGH-01，`PHASE1_FIRST_WAVE_RISK_REGISTER_v0.1.md`）。修复：`PATCH /api/labgen/drafts/bb4fe651-...` 将 `cta_enabled` 降回 `false`，`article_url`/`article_title`/`article_channel`/`article_published_at` 全部清空，与 Service No Endpoints/ImagePullBackOff 两个 lab 完全一致（纯数据修复，`data/*.json` 不入库，无代码改动）。未触碰 Directus 文章状态（是否/何时重新发布仍需 owner 决策）、未修改 `LABGEN_ENABLED_LAB_IDS`、未新增用户、未开放任何公开访问。targeted tests（cta/lab_draft/article_bind 相关）124 passed，生产 health 全绿。同步更新 `PHASE1_FIRST_WAVE_RELEASE_CANDIDATE_v0.1.md`/`PHASE1_FIRST_WAVE_RISK_REGISTER_v0.1.md` 反映修复结果

### Changed
- docs(labgen): Phase 1 First Wave RC Freeze — 三个 first wave lab（CrashLoopBackOff/Service-no-Endpoints/ImagePullBackOff）收口为 Release Candidate 快照，供 owner 决策。新增 `PHASE1_FIRST_WAVE_RELEASE_CANDIDATE_v0.1.md`（三个 lab 完整状态表 + 三篇文章一致性检查 + CTA/Access 策略菜单，推荐 Owner Dogfood，不推荐现在开放外部测试）、`OWNER_DOGFOOD_FIRST_WAVE_RUNBOOK_v0.1.md`（测试顺序/预计耗时/成功标准/PASS 判定标准，本次只交付 runbook 不执行 dogfood）、`PHASE1_FIRST_WAVE_RISK_REGISTER_v0.1.md`（1 HIGH + 2 MEDIUM + 2 LOW，0 BLOCKER）。RC 检查中发现一项此前未记录的真实状态漂移（HIGH-01）：`OWNER_SOFT_LAUNCH_ARTICLE1_PUBLISHED_VERIFIED_v0.1.md`（2026-06-28）记录 CrashLoopBackOff 文章已 `PUBLISHED_VERIFIED`/`cta_enabled=true`，但本次核查 Directus 实际记录（article id=4）显示当前 `status=draft`，公开 API 确认匿名访客当前无法读取该文章，而 LabDraft 层的 `cta_enabled=true`/`article_url` 未同步降级——根因未知（超出本次只读 RC 快照范围），当前无真实流量受影响（lab 本身仍被 `LABGEN_ENABLED_LAB_IDS` 挡住），已记录不处理，留给 owner 决策发布或降级两个方向之一。另确认 `DataRetentionService.run(dry_run=True)` 显示 19 个 zombie draft 可安全归档（三个 first wave lab 因 published/rehearsal_completed 被保护不受影响）——只读确认，未执行归档。本次严格只读：未修改任何 lab/article 发布状态、未修改 `LABGEN_ENABLED_LAB_IDS`、未添加用户、零后端代码改动，全量测试套件未受影响（docs-only diff，本次未重跑 full suite，仅确认 mypy 0 error + 生产 health healthy）
- feat(labgen): Phase 1 First Wave Sprint #3 — 生产 ImagePullBackOff lab（`eb78afaa-f7fb-422e-8eb9-98644f59527f`）全流程完成：Topic Brief → Lab Design Brief → admin curated lab draft（`article-drafts`/`generate-lab` 拿 stub 骨架，人工 PATCH 真实内容，路径与 lab #1/#2 一致）→ static validation（20/20 PASS）→ 两轮真实 K3s rehearsal（VM 401，172.16.100.140，K3s v1.34.4）→ 发布（`publish_status=published`，internal soft launch，未加入 `LABGEN_ENABLED_LAB_IDS`）→ owner-as-learner smoke（`cleanup_verified=true`）→ official article draft。第一轮 rehearsal 发现并修正两处真实内容缺陷（非代码 bug）：Step 3 observe 文本假设的 Events 错误消息（`manifest unknown`）与真实环境返回的实际消息（`... not found`）不符；Step 5 修复命令的容器名假设错误（`kubectl create deployment --image=` 默认用镜像名而非 Deployment 名作为容器名，导致 `kubectl set image` 报 `unable to find container named ...`），修正后第二轮 rehearsal 7/7 步骤全部通过。工程范围为三个 first wave lab 中最小的一个：复用 lab #1 已实现的 `deployment_unavailable`/`deployment_ready` verifier（无需新增 verify 原语）、复用 lab #1 已授予的 `deployments` update/patch RBAC（无需扩大权限面）、`kubectl_executor.py` 无需变更（`set image` 子命令未被现有黑名单拦截）——本次**零后端代码改动**，纯内容生产 + 4 篇文档（Topic Brief/Design Brief/Article Draft/Production Result）+ `PHASE1_SERIES_ALIGNMENT_v0.1.md` 更新（first_wave 三个 lab 全部标记完成）。回归确认：CrashLoopBackOff/Service-no-Endpoints 两个已发布 lab 不受影响；`LABGEN_ENABLED_LAB_IDS`/`data/labgen_invites.json` 全程未变更，真实学生仍 403；生产 health 全程 healthy
- feat(labgen): 实现 LabGen "Controlled Micro Invite" 的按具名用户邀请机制（P0 Access Model Fix，本次只实现机制，不添加任何真实用户）。背景：现有 `LABGEN_ENABLED_LAB_IDS` 只能做 lab 级别开关，一旦某 lab_id 加入白名单，任何已认证非 admin 用户都能访问，无法满足"1-2 位具名用户"的邀测粒度要求。新增 `backend/labgen/invite_registry.py::InviteRegistry`，只读读取 `data/labgen_invites.json`（flock 原子读写，与其它 `data/*.json` 同一约定，gitignored，不写死真实用户名进代码，格式 `{"<lab_id>": ["username1", ...]}`），提供 `requires_invite(lab_id)`（lab_id 是否作为 key 存在——不存在则该 lab 完全不受此机制约束，核心课程不受影响）和 `is_invited(lab_id, username)`（用户名是否在列表内，对非 list 类型的畸形值 fail closed 返回 False）两个方法，每次调用都重新读文件（不缓存），因此新增/移除邀请无需重启服务。`failure_reasons.py` 新增 `PRECHECK_LAB_ACCESS_NOT_INVITED`（纯新增枚举值）；`LabSessionService.run_precheck()` 在原有 `LABGEN_ENABLED_LAB_IDS` 检查之后（顺序经 safety-reviewer 逐行核对）新增邀请名单检查，admin 无条件绕过；`LearnerCatalogService`（目录列表 + 详情页 eligibility）新增完全对齐的同款检查，避免重蹈 HIGH-001 覆辙（目录层与启动层各说各话）——`_compute_is_startable()`/`_evaluate()` 用 `elif` 接在 lab 级别门禁判断之后，只有 lab 级别先通过才检查邀请名单，避免同时报两条互相矛盾的错误。**破坏性重命名**（仅限本次改动范围内）：`EligibilityIssueCode.UNAUTHORIZED` 重命名为 `LAB_NOT_ENABLED`（该字段是上一个 commit 才引入、尚未被任何前端/学生依赖，此时改更准确的名字成本最低），新增 `NOT_INVITED`；两种拒绝情况对学员展示的错误文案刻意保持完全相同，只有 `code` 字段不同，避免学员从提示信息里推断出"这个 lab 到底有没有配置邀请名单"这类内部实现细节。`routes.py` 的 `get_catalog_service()`/`get_session_service()` 两处 DI 工厂均注入同一路径的真实 `InviteRegistry()` 实例（已核实两者读同一个文件，不存在路径不同步风险）。新增 19 个回归测试（`test_labgen_invite_registry.py` 8 个 + `test_labgen_lab_session.py::TestPrecheck` 新增 4 个 + `test_labgen_learner_catalog.py::TestNamedUserInviteGate` 7 个），覆盖：核心课程（未加入 registry）不受影响、已启用+未受邀→拒绝且 code=NOT_INVITED、已启用+受邀→放行、admin 绕过、"未启用"优先于"未受邀"的互斥展示（同一个 lab 若两者都不满足，只报 `LAB_NOT_ENABLED`）、registry 未配置时默认不拦截。同步更新 `docs/labgen/LABGEN_CONTROLLED_INVITE_POLICY_v0.1.md` 第 1 节 allowed_users model（原设计提案是扁平的 `LABGEN_ENABLED_USERNAMES` 环境变量，实现时改为 per-lab 的 `InviteRegistry`，因为任务要求"支持 per-lab invite"——同一批测试账号可能只被邀请到部分 lab，扁平全局用户名单无法表达这种粒度）及 go/no-go checklist。生产验证：`data/labgen_invites.json` 确认当前不存在（该机制目前是纯粹的 no-op），对全部 8 个已发布 lab 用真实学生身份调用 `list_published_labs`/`evaluate_start_eligibility`，行为与本机制上线前完全一致（`is_startable=false`，`code=LAB_NOT_ENABLED`，未因新逻辑产生任何变化）；`LABGEN_ENABLED_LAB_IDS` 本身未被修改，未添加任何真实学生/邀测用户，未开放 Service official article，未启动 ImagePullBackOff 或任何新 lab。safety-reviewer 判定 A 类（触及权限判断逻辑），逐条核对代码后 PASS；pre-push hook 的 Codex 独立审查同步通过；全量 4968 tests passed；mypy 0 error
- fix(labgen): 修复 P0 bug — LabGen learner catalog 的 `is_startable`/start eligibility 从未纳入 `LABGEN_ENABLED_LAB_IDS` 判断，导致已发布但未加入白名单的 lab 在已登录学生的实验目录里显示"可开始"，直到真正点击"开始实验"才收到 403 — 补回归测试防止重现。根因：`LABGEN_ENABLED_LAB_IDS` 网关此前只在 `LabSessionService.run_precheck()`（真正的会话启动调用）里生效，`learner_catalog.py` 的 `LearnerCatalogService._compute_is_startable()`/`_evaluate()` 完全不知道这个网关的存在，只检查 `publish_status`/validation/image 状态，导致目录层与启动层对"能不能开始"这件事各说各话（在 Service Official Article Publish Gate 准备 sprint 的 review 中发现，记为 HIGH-001）。修复：`LearnerCatalogService.__init__()` 新增 `enabled_lab_ids`/`admin_usernames` 参数（默认 `None`/空，向后兼容——`None` 表示网关未配置，不静默拒绝所有 lab，与 `LabSessionService` 现有语义一致）；新增 `_is_access_denied()` 辅助方法，与 `run_precheck()` 的 `PRECHECK_LAB_ACCESS_NOT_ENABLED` 判断逐字对齐（`enabled_lab_ids is not None and lab_id not in enabled_lab_ids and actor_user not in admin_usernames`）；`_compute_is_startable()` 新增该检查（目录列表层）；`_evaluate()` 新增该检查作为第 0 项（详情页 start_eligibility 层），复用既有 `EligibilityIssueCode.UNAUTHORIZED`，被拒绝时不再叠加 `RUNTIME_CHECKS_DEFERRED` 提示（避免"错误状态 + 继续提示"的矛盾信号）；`routes.py::get_catalog_service()` 相应注入 `config.LABGEN_ENABLED_LAB_IDS`/`config.ADMIN_USERNAMES`。新增 7 个回归测试（`TestAccessAllowlistGate`）覆盖：已发布未启用→目录/详情页均 `is_startable=false` 且带 `UNAUTHORIZED` 原因、已启用+已认证学生→`is_startable=true`（同时记录一个诚实发现：代码库当前**没有**独立的按用户邀请名单机制，`LABGEN_ENABLED_LAB_IDS` 是唯一的访问网关，任何非 admin 的已认证用户一旦 lab_id 被加入白名单即被授权）、admin 绕过网关、网关未配置时默认可开始（不静默拒绝一切）、被拒绝状态不泄露 `RUNTIME_CHECKS_DEFERRED` 混合信号。生产验证：真实数据下对全部 8 个已发布 lab 用真实学生身份调用 `list_published_labs`，全部正确返回 `is_startable=false`（因为 `LABGEN_ENABLED_LAB_IDS` 目前为空）；对 CrashLoopBackOff/Service-no-Endpoints 两个 lab 用真实 admin 账号（`smoke-admin`）调用 `evaluate_start_eligibility`，正确绕过网关返回 `is_startable=true`；两个 lab 的 `publish_status` 均保持 `PUBLISHED` 不变；真实 `POST /lab-sessions` 端点的 403 保护未受影响（本次未触碰 `lab_session_service.py`）；`LABGEN_ENABLED_LAB_IDS` 本身未被修改，未开放任何真实访问
- docs(labgen): Service Official Article Publish Gate — 发布决策准备（不执行发布）。审查并更新 `SERVICE_NO_ENDPOINTS_OFFICIAL_ARTICLE_DRAFT_v0.1.md`：逐项核对文章命令/现象/修复路径与真实 lab 一致，完成内容可读性 dry-run review（非完整 learner session E2E），更新 blocker #4 状态为已完成 review（但仍不构成可发布理由）。新增 `SERVICE_ARTICLE_PUBLISH_GATE_PREP_v0.1.md`，输出 CTA 策略草案（用真实 `_build_cta()` 调用生成文案样例）、Directus 发布 checklist（未创建任何记录）、访问策略矩阵（内部关闭 vs Controlled Micro Invite 对比，不推荐执行）。本轮 review 过程中发现一个独立于本次任务范围的 HIGH 级别既有 gap 并记录在文档 blocker #5：`LearnerCatalogService`（`backend/labgen/learner_catalog.py`）的目录可见性/`is_startable` 计算从未纳入 `LABGEN_ENABLED_LAB_IDS` 判断——`LABGEN_ENABLED_LAB_IDS` 网关只在 `LabSessionService` 的真正启动调用里生效，导致任何"已发布但未加入白名单"的 lab（当前正是 Service-no-Endpoints 的状态）在已登录学生的实验目录里会显示为可开始，直到真正点击"开始实验"才收到 403，属于用户体验缺陷而非安全问题（未验证前端如何呈现该 403，403 网关本身仍然生效，未发生任何越权/数据泄露），留作独立 bug-fix 任务处理。本次全程只读操作：未创建 Directus 记录、未修改 `LABGEN_ENABLED_LAB_IDS`、未开放 CTA、未发布任何公开文章、代码零改动（仅 2 个 docs 文件新增/更新），regression 确认 publish_status/CrashLoopBackOff/service_has_endpoints/学生 403/health 均无变化
- feat(labgen): 新增 `service_has_endpoints` verify 原语（Verifier Gap Fix），补齐 Service-no-Endpoints lab 此前唯一未被机器化覆盖的核心验证点——"selector 修复后 Endpoints 是否真的从空变为非空"过去只能靠 admin 人工 observe。实现：`models.py` 新增 `VerifyType.SERVICE_HAS_ENDPOINTS`；`verifier.py` 的 `K8sVerifierClientPort`/`FakeK8sVerifierClient`/`_SUPPORTED_TYPES`/`_dispatch`/`_make_detail` 全部接入；`K8sVerifierClientAdapter.service_has_endpoints()` 用 `list_namespaced_endpoints(namespace, field_selector=...)`（等价于 `kubectl get endpoints`，不要求 `get` RBAC）检查至少一个 subset 有非空 `addresses`，`subsets=None`/空 list/`addresses=None` 均归约为 False，404→False，其它 ApiException 透传，与既有 `service_exists`/`pod_running` 等方法模式完全一致；`verifier.py::_check_verify_type_implemented` 无需改动即自动放行新类型（数据源是 `_SUPPORTED_TYPES`）。刻意不用 `kubectl describe service` 的 Endpoints 字段作为判断依据——该字段在当前 K3s v1.34.4 上有已知不可靠 bug（见下方 RUNBOOK 记录），`service_has_endpoints` 直接查询 core/v1 Endpoints 对象，不受影响。RBAC：verifier 的 ClusterRole（两条独立 apply 路径——`_CLUSTER_ROLE_MANIFEST` 字符串常量给 SSH kubectl apply 用，`PlatformVerifierInitializer.ensure_verifier_identity` 的 `V1PolicyRule` 给平台 Python K8s API 路径用）均新增 `endpoints` 资源，权限仅 `list`+`watch`（不含 `get`，与 pods/services/configmaps 权限模型一致），两处保持同步；`article_lab_prompt_builder.py` 的 K8s domain hint 同步更新，把 `service_has_endpoints` 加入"已实现原语"清单，并明确告诫不要用 `describe service` 的 Endpoints 行作为学员可见证据。新增约 40 个回归测试覆盖 7 个测试文件（verifier/k8s_verifier_client/verifier_credentials/platform_verifier_init/static_validator/prompt_builder/models），其中 2 个此前断言"endpoints 不应授权"的 guardrail 测试（`test_endpoints_not_in_core_rule`/`test_cluster_role_no_endpoints_resource`）随语义变化翻转为断言"endpoints 应授权且仅 list+watch"，防止未来误加 `get` 权限。生产内容：已发布的 Service-no-Endpoints lab（`2dcc7b1f-a52f-4a8f-9e8a-3ee9ff0a5a86`）step-6 追加一条 `service_has_endpoints` verify（web-svc），`publish_status` 保持 `published` 不变，StaticValidator 20/20 通过；step-5 的 verify notes 中"当前无 service_has_endpoints 校验原语"的说明已随之移除。真实 K3s 环境验证（VM 401，172.16.100.140）：重新执行 `ensure_verifier_identity` 后确认线上 ClusterRole 已生效含 `endpoints` list+watch；在临时命名空间创建 selector 不匹配的 Deployment+Service，确认 `service_has_endpoints` 返回 False；按 lab 的 delete+expose 修复路径修复后确认返回 True；已清理临时命名空间。全量 4942 tests passed（含此前偶发的 RateLimiter 并发测试本轮未复现）+ mypy 0 error。未开放任何新学员权限——`LABGEN_ENABLED_LAB_IDS` 未变更，真实学生仍 403；未开放灰度、未发布 Service official article、未创建新 lab，严格限定在本次唯一目标（补齐核心 verifier gap）范围内
- fix(labgen): 修复 P0 bug — 已发布 lab 被 `/drafts/{id}/validate` 静默降级 — 补回归测试防止重现。根因：`backend/labgen/routes.py::_compute_publish_status()` 只返回 `PUBLISH_BLOCKED`/`REVIEW_REQUIRED`/`DRAFT` 三种值，从不返回 `PUBLISHED`，而 `validate_draft` 端点无条件用它覆盖 `draft.publish_status`——任何对已发布 lab 的重新校验（即使内容和 rehearsal 结果完全没变、100% 检查通过）都会把它静默打回 `draft`，这正是上一条 Post-Sprint Stabilization 记录里发现并临时手动恢复的那次事故的根因。修复：`_compute_publish_status()` 新增 `current_status` 参数（默认 `DRAFT`，向后兼容既有调用点），当 `current_status == PUBLISHED` 时只在出现新的 `PUBLISH_BLOCKING` 级别失败时才降级为 `PUBLISH_BLOCKED`，`REVIEW_REQUIRED` 级别失败不再触发降级——这与 `PublishService.publish()` 自身的 gate 定义完全对齐（其文档已明确写"review_required results do not block MVP publish"），保证 `/validate` 与 `/publish` 对"什么算失败"这件事的判断口径一致，不再各说各话。`validate_draft` 调用点相应改为传入 `draft.publish_status` 作为 `current_status`。新增 9 个回归测试：`_compute_publish_status` 单元测试覆盖"已发布+全通过→保持 published"/"已发布+无结果→保持 published"/"已发布+新增 publish_blocking 失败→降级"/"已发布+仅 review_required 失败→不降级"/"非 published 起点行为不变"5 种场景 + 3 个端到端 `POST /validate` 集成测试（`TestValidatePreservesPublishedStatus`）验证真实 HTTP 端点行为。生产验证：对两个已发布 lab（CrashLoopBackOff、Service-no-Endpoints）通过真实 API 调用 `/validate`，确认均保持 `published`；真实学生账号对两者仍 403（该网关由 `LABGEN_ENABLED_LAB_IDS` 单独控制，与本次改动无关，未受影响）；CTA 端点 `is_published=true`/`cta_enabled=false` 不变
- docs(labgen): Post-Sprint Stabilization — Phase 1 系列对齐、Service-no-Endpoints article gate 三件套（Topic/Design Brief/Article Draft）、Controlled Invite Policy 设计、技术债分类，均为纯文档产出，不开放任何真实用户权限（`LABGEN_ENABLED_LAB_IDS` 保持不变）。回归检查中发现并原地恢复一个真实数据状态偏差：Service-no-Endpoints lab（`2dcc7b1f-a52f-4a8f-9e8a-3ee9ff0a5a86`）的 `publish_status` 在上次 sprint 收尾后被静默从 `published` 打回 `draft`，根因是 `backend/labgen/routes.py::_compute_publish_status()` 只返回 `PUBLISH_BLOCKED`/`REVIEW_REQUIRED`/`DRAFT`，而 `/drafts/{lab_id}/validate` 端点无条件用它覆盖 `publish_status`——任何对已发布 lab 的重新校验调用都会把它静默降级，即使内容和 rehearsal 结果未变。已通过正规 `/drafts/{lab_id}/publish` 端点（原子重跑 gate，非绕过）恢复为 `published`，真实学生 403 网关不受影响（该网关由 `LABGEN_ENABLED_LAB_IDS` 单独控制，与 `publish_status` 无关）。**本次只做状态恢复，未修复根因**，根因修复留作独立 bug-fix 任务，详见 `docs/labgen/SERVICE_NO_ENDPOINTS_LAB_PRODUCTION_RESULT_v0.1.md` 附注章节。
- feat(labgen): 新增 `verify.type_implemented` 发布前校验，补齐 schema 与 verifier 运行时之间的一个真实盲区 — 生产第二个 internal soft launch lab（Service-no-Endpoints）时，草稿里用了 `VerifyType.POD_READY`（`models.py` schema 枚举里合法存在的值），静态校验 100% 通过、发布也成功，但真实 rehearsal 时该 step 永久卡死，报错 `verify_type_not_implemented`——因为 `verifier.py::_SUPPORTED_TYPES` 从未实现 `POD_READY`（只有 `POD_RUNNING`），schema 层比运行时层声明了更多能力，这个 gap 之前完全没有校验覆盖。新增 `StaticValidator._check_verify_type_implemented()`，对照 `verifier.py::_SUPPORTED_TYPES`（唯一数据源）逐一检查 draft 里每个 `verify` 条目的 `type`，未实现类型直接 PUBLISH_BLOCKING，效果与既有 `commands.rbac_coverage` 拦截 RBAC 不匹配命令的模式一致；同时把这条踩坑经验写回 `article_lab_prompt_builder.py` 的 K8s 域 prompt（`_DOMAIN_HINTS["k8s"]`），列出当前实际可用的 verify 原语并显式点名 `pod_ready` 是个看似合理但从未实现的陷阱，防止未来 LLM 生成路径重蹈覆辙；补 4 个新校验回归测试 + 2 个 prompt 内容回归测试（`test_only_lists_implemented_verify_types`/`test_prohibits_patching_services`）
- fix(tests): 新增的 `verify.type_implemented` 校验意外揭出一批更早存在、与本次改动无关的技术债 — `backend/labgen/generation_templates.py` 的 `PYTHON_BASICS`/`HTTP_API_BASICS`/`DATA_TRANSFORM_BASICS` 三个演示模板（`fake_only` 模式在没有关键词匹配时的兜底内容，也是 `demo_seed.py` 用来填充学员目录演示数据的内容）本身就使用了未实现的 `VerifyType`（`POD_READY`/`JOB_COMPLETED`），且这些模板的命令还引用了仓库里根本不存在的 `manifests/*.yaml` 文件——`demo_seed.py` 自己的 docstring 明确写着"no real K8s/VM operations"，说明这些模板从一开始就只是用来跑通 PublishService/StaticValidator 代码路径和填充前端 UI 演示数据，从未打算真正在 K3s 上跑通；新校验上线后这批预置缺陷首次被检测出来，导致 7 个测试文件里 26 个测试失败。评估后确认彻底修复（补齐 Job 校验能力 + 补齐/重写 9 个步骤的真实可执行内容）超出本次改动范围（Job 校验能力涉及新增 BatchV1Api 工厂签名变更，会连带破坏另外 2 个测试文件已有的 2-tuple 约定），采用 `pytest.mark.xfail(strict=True)` 标记这 26 个测试并注明详细可追溯原因（而非静默跳过或弱化新校验），后续应作为独立 bug-fix 任务处理
- docs(labgen): RUNBOOK.md K3s v1.34.4 已知限制新增一条 — `kubectl describe service` 输出里的 `Endpoints:` 字段在当前 K3s 版本下不可靠，即使 Service 已经正确关联了 Pod、`kubectl get endpoints` 也确认显示了真实 IP，`describe service` 的 Endpoints 行仍可能永久显示 `<none>`（实测持续 20s+ 不自愈，Selector/Type 等其它字段不受影响）；生产 lab 内容据此调整为只用 `kubectl get endpoints` 判断 Endpoints 是否填充，不依赖 `describe service` 的聚合字段
- feat(labgen): 生产第二个 internal soft launch lab — 主题：Kubernetes Service 没有 Endpoints（selector 与 labels 不匹配）。Admin 人工 curated 内容（环境无真实 LLM 凭证，`LABGEN_LLM_PROVIDER_MODE=fake_only`，经用户确认后采用与 lab #1 相同的路径：走 `article-drafts`/`generate-lab` API 拿 stub 骨架，人工编写真实技术内容后 PATCH 替换）；7 步内容：制造 selector 不匹配（`kubectl create service` 默认 selector 不等于任何 Deployment 标签）→ 观察 Endpoints 为空 → 对比 Service selector 与 Pod 真实标签定位根因 → 用 `kubectl delete service` + `kubectl expose deployment` 修复（不用 patch，呼应上面的 RBAC 设计决策）→ 验证 Endpoints 已填充 → 清理；StaticValidator 20/20 通过；internal rehearsal 在真实 VM 299（K3s v1.34.4）上完整跑通并 `cleanup_verified=true`；已发布（`publish_status=published`），`LABGEN_ENABLED_LAB_IDS` 未加入该 lab_id，真实学生仍 403（与现有 LabGen 访问收紧策略一致，未改变该产品决策）；owner/learner 双重 smoke（`session_type=internal_rehearsal` + `session_type=learner`）均 7/7 步骤通过，CTA 内容生成正确且 `cta_enabled=false`（未面向公开 Directus 文章）。详见 `docs/labgen/SERVICE_NO_ENDPOINTS_LAB_DESIGN_CONTRACT_v0.1.md`
- feat(labgen): 学员 RBAC 新增 `services`/`endpoints` 权限（为生产 Service-no-Endpoints lab 所需），并在授权设计阶段堵住了一条 K8s RBAC 结构性无法覆盖的越权路径 — 起因：`LEARNER_ALLOWED_PERMISSIONS`（学员 K8s ServiceAccount RBAC 权限的唯一数据源，见 `backend/labgen/learner_credentials.py`）完全没有 `services`/`endpoints` 两类资源，任何围绕 Service 排障的 lab 内容在真实 rehearsal 时都会因 403 失败；`_KUBECTL_RESOURCE_MAP`（`backend/labgen/static_validator.py`，`commands.rbac_coverage` 发布前校验依赖的命令→资源映射表）也完全没有 `endpoints` 条目，导致 `kubectl get endpoints` 类命令不被 `_command_required_permissions()` 识别，是发布前校验的一个盲区（静默放行，只有到 rehearsal 阶段才会暴露 403）——均已补齐（`endpoint`/`ep`/`endpoints` 映射 + `services`/`endpoints` 授权，`endpoints` 只读 get/list/watch）。
  - 授权设计阶段的安全审查（5 轮）发现：K8s RBAC 无法按 `spec.type` 字段值做细粒度限制，一旦给 `services` 授予 `update`/`patch`，学员就能用 `kubectl patch service`/`kubectl replace` 等命令把 ClusterIP Service 升级成 NodePort/LoadBalancer（在**集群所有节点**的宿主网络栈绑定端口，绕过 namespace 隔离）或 ExternalName（DNS SSRF 向量）。尝试在 `kubectl_executor.py` 层用字符串解析拦截"patch 是否触碰 type 字段"，连续 4 个版本都被找到新绕过（JSON Patch RFC 6902 的 path/value 分离写法、YAML 无引号写法、转义引号写法、`-p`/`-n` 等 flag 值被误判为目标资源、逗号分隔多资源批量语法、未枚举全的全局 flag 如 `--request-timeout`、`kubectl replace -f <url>` 完全绕过检测范围）——针对 kubectl/pflag 全部语法面做字符串枚举是一场打不赢的军备竞赛。
  - **架构决策**：`LEARNER_ALLOWED_PERMISSIONS` 里 `services` 只授予 `create`/`get`/`list`/`watch`/`delete`，**不授予** `update`/`patch`——把信任边界从"执行器能否解析出危险命令"移到"K8s API Server 是否接受该请求"，RBAC 层无条件拒绝任何 patch/update/replace，不受 CLI 语法影响。`kubectl_executor.py` 相应简化：移除整个基于字符串解析的 type-touch 检测子系统（`_target_resource`/`_blocks_service_type_patch`/`_SERVICE_TYPE_FIELD_TOUCH_RE`/`_VALUE_CONSUMING_FLAGS`），只保留两条创建期拦截（`create service nodeport/loadbalancer/externalname` 位置参数形式、`expose --type=` flag 形式）——create verb 仍被授予，RBAC 无法按 type 值拦截创建请求，这两条是唯一剩余的执行器侧防线。
  - 审查过程中顺带发现并修复两个与本次改动无关、但同样严重的预置漏洞：`kubectl get --raw <path>` 完全绕过 `-o yaml/json` 保护、直接原样吐出 API Server 响应（可用来在已有 `secrets` 读权限的基础上打出完整明文 Secret，是所有既有 lab 共享的口子，非本次引入）；`kubectl replace -f <url>` 未被 SSRF 防护正则覆盖（该正则此前只匹配字面量 `apply`）——均已修复。
  - 第六轮 safety-review 发现：上面"唯一剩余防线"的两条拦截规则、以及顺手修的 `--raw` 拦截，最初都实现为在**未解析的原始命令字符串**上做 `\s`/`\b` 边界正则匹配——但真正送进 kubectl 子进程的是 `shlex.split()` 解析后的 argv（不经过 shell），只要给关键词加一层引号（如 `create service "nodeport" ...`），shlex 会统一剥掉引号得到干净 token 正常执行，而原始字符串因为多出的引号字符导致正则的空白边界匹配不上，检测形同虚设——这正是本文件里几处已存在的 `-o` 格式检查、命名空间剥离逻辑早就在用、但这三条新规则没有遵循的模式（在 `parts`/token 列表上匹配，而不是原始字符串）。三条规则均已改写为基于 `shlex.split()` 后 token 的精确匹配，不再受引号/大小写影响。
  - 第七轮 safety-review 发现：第六轮改成 token 级匹配后修复了引号绕过，但新代码里 `create service <type>` 检查用固定下标 `parts[1]=="create"`/`parts[2]=="service"` 判断——kubectl（基于 cobra/pflag）允许全局 flag 出现在子命令之前（如 `kubectl --v=0 create service nodeport ...`），一旦插入就会把 `parts[1]` 变成 `"--v=0"`，固定下标判断整体失效，且已用真实 kubectl 验证该命令语义与不加 `--v=0` 完全一致（不是"检测漏判但命令本身也跑不通"的假阳性豁免）。进一步排查发现这**不是新代码独有的问题**：项目里更早就存在的 `_BLOCKED_SUBCOMMANDS` 检查（拦截 `exec`/`port-forward`/`cp` 等高危子命令）同样依赖 `parts[1]`，同一招插入全局 flag 就能让 `kubectl --v=0 exec -it my-pod -- /bin/sh` 绕过 exec 拦截——这是一个预置漏洞，比本次 Service 相关改动本身更严重。修复方式不是继续枚举/跳过全局 flag（第五轮已证明这类枚举法结构性不完备），而是从根上不允许子命令前出现任何全局 flag：`validate_command()` 新增检查，`kubectl` 后紧跟的 token 一旦以 `-` 开头即整体拒绝（没有任何 lab 命令需要在子命令前带全局 flag），一次性同时堵死了新旧两处依赖固定下标的检测。
  - 补齐/重写 50+ 个回归测试：`_command_required_permissions` 新增 endpoints/service 解析用例、`commands.rbac_coverage` 新增 services/endpoints 通过用例 + `services patch` 仍被拒绝用例、`TestValidateCommandServiceTypeEscalation`（含引号/大小写绕过用例）、`TestValidateCommandRawFlagBlocked`（含引号绕过用例）、`TestValidateCommandReplaceFromUrlBlocked`、`TestValidateCommandGlobalFlagBeforeSubcommandBlocked`
  - 第八轮 safety-review 确认第七轮修复无新绕过（**No blocking issues found**），全流程共 8 轮审查收敛。遗留两项非阻断 backlog（均已确认无安全后果，记录供后续处理）：(1) MEDIUM，`kubectl -n <ns> get pods`（`-n` 写在子命令前）这种此前能被 `_strip_namespace_flags` 正常处理的合法写法，现在会被"子命令前禁止全局 flag"规则一并拒绝，是可接受的安全/体验权衡但造成用户困惑；(2) LOW，`static_validator.py::_command_required_permissions` 与 `kubectl_executor.py` 存在同类固定下标假设的重复实现，未同步本轮的全局 flag 前置防护，但因 `commands.executor_compatible` 已用 `validate_command()` 兜底拦截，无实际可利用后果，仅为维护性重复。
  - 顺带确认并记录一个更早就存在、与本次改动无关的架构性问题（未在本次修复，超出范围）：`_BLOCKED_SUBCOMMANDS` 是黑名单而非白名单，任何未被列入黑名单的子命令位置词都会被当作合法子命令放行，理论上包括 kubectl 插件名（`kubectl foo-plugin ...`）——当前生产环境 PATH 下没有任何 `kubectl-*` 插件二进制，暂不构成真实攻击面，但架构上更稳健的方向是子命令改用白名单而非黑名单，留作后续独立 backlog。
- fix(tests): Resend 发信失败告警功能刚上线即自曝测试隔离缺口 — `backend/email_client.py::_record_failure()` 默认写入路径是生产 `data/email_send_failures.json`，`tests/test_email_client.py` 里未显式 patch `_FAILURE_LOG` 的既有失败路径测试（`test_returns_false_on_http_error`/`test_returns_false_on_network_exception`）运行时会把假失败记录写进生产文件——部署后 `/api/health` 一度报出 `email.status=degraded`（4 条 24h 内"失败"，实为测试污染，非真实 Resend 故障），与本次改动之前 `RESEND_API_KEY` 泄漏进 pytest 进程是同一类问题（conftest.py 需要为新的模块级默认路径同样兜底）；`tests/conftest.py` 新增 `autouse=True` 的 `_isolate_email_failure_log` fixture，把 `_FAILURE_LOG` 重定向到每个测试的 tmp_path，一次性覆盖全部测试文件，不依赖每个测试自己记得 patch；补 2 个回归测试（`TestFailureLogIsolation`）；生产 `data/email_send_failures.json` 中的测试污染数据已清理
- fix(security): `/api/auth/resend-verification` 响应时延侧信道 — 该端点设计上对已存在/不存在的用户名都返回同样的 `{"success": true}`，用来避免泄露账号存在性，但已存在用户名会同步 `await` 一次真实的 Resend HTTP 请求（网络往返，可达数百毫秒甚至超时前 5s），不存在的用户名直接跳过、几乎瞬时返回——攻击者仍可通过响应耗时区分账号是否存在，绕开了"始终 success"的设计意图；`backend/auth_routes.py` 新增 `_fire()`/`_background_tasks` 强引用后台任务模式，把邮件发送改为 fire-and-forget，响应不再等待网络调用完成；补 1 个回归测试（`TestResendVerificationTimingSideChannel`，mock 发信耗时 0.3s，断言响应 < 0.15s 返回）+ 更新既有 `test_resend_sends_new_code` 为轮询等待后台任务完成（原先假设发信是同步 await）
- feat(ops): DataRetentionService 新增 zombie draft 实际归档能力 — `report()` 此前只统计"30 天未更新的草稿"计数（生产累积到 155 个），`run()` 却从未真正归档它们，只有孤儿 diff 有清理执行路径；`backend/labgen/data_retention.py::run()` 新增对称的 zombie draft 归档分支（保留 published/rehearsal_completed 保护，dry_run 不写文件），归档后同一次 run 里级联清理其关联的 review diff（避免下一轮才发现新的孤儿）；补 2 个回归测试（`test_zombie_drafts_are_archived`/`test_zombie_draft_dry_run_does_not_modify_file`）；生产已执行：归档 155 个 zombie draft + 清理 2264 条孤儿 diff（`lab_review_diffs.json` 2.77MB→0.06MB），`/api/health` sessions 状态由 degraded 转为 ok
- feat(ops): 新增 Resend 发信失败管理员告警 — 此前发信失败只写 `logger.warning`，没人会去盯日志，导致长期发信故障（例如 API key 失效/超额）要等学生投诉才发现；`backend/email_client.py` 新增 `_record_failure()`，在网络异常/非 2xx 响应两条失败路径都会把失败原因+时间戳原子写入 `data/email_send_failures.json`（24h 滚动窗口，flock 原子写）；`backend/api_routes.py` 新增 `_check_email_health()`，聚合最近 24h 失败次数，≥3 次触发 `/api/health` 的 `email.status=degraded` 告警（只暴露聚合计数，不泄漏具体失败原因/邮箱地址）；补 10 个回归测试（`test_email_client.py::TestFailureRecording` 4 个 + `test_health_email_alerting.py` 6 个）
- ops: 生产 2 个历史 `LAB_START_FAILED`（`namespace_create_failed`，`vm_id=500` 从未在 Proxmox 创建成功，`vm_creation_times.json`/`tainted_vms.json` 均无记录，无残留资源）session 按 RUNBOOK 场景七 force-close，`/api/health` sessions 状态由 degraded 转为 ok
- feat(labgen): Article-to-Lab 生成 prompt 加固，把 lab draft bb4fe651 一周调试踩过的坑写回 LLM system prompt，防止同类问题在下一篇文章重演 — 此前 StaticValidator 已能拦截 shell 变量语法/`kubectl delete namespace`/非法输出格式等违规命令，但只是"发布前拦截"，LLM 生成阶段完全不知道这些限制，导致每篇新文章大概率重复触发同一批校验失败、仍需人工改草稿；`backend/labgen/article_lab_prompt_builder.py` 新增 `_K8S_COMMAND_CONSTRAINTS`（对齐 `kubectl_executor.py` 的真实校验规则：禁 shell 变量/管道/`$()`，用 label selector 代替变量捕获 Pod 名，禁 `-o yaml/json`，禁 `-n`/`--namespace`，禁 exec/port-forward 等子命令）和 `_LINUX_COMMAND_CONSTRAINTS`（对齐 `linux_command_executor.py`：禁 shell 元字符，命令白名单，禁绝对路径/`..`），按 `target_domain` 拼进 system message；补 7 个回归测试（`TestCommandGenerationConstraints`）验证约束文本确实出现在对应领域的 prompt 里
- fix(labgen): 公开注册加固后放大了 LabGen 的敞口 — 网站开放自助注册意味着"没人知道 URL"这道非强制防线失效，任何注册用户都能通过公开的 `crashloopbackoff-describe-logs`/`linux-files-permissions-basics` 文章 CTA 直接创建 K8s lab session（Linux 域此前已有 `LABGEN_LINUX_LEARNER_ENABLED_LAB_IDS` 白名单，K8s 域完全没有）；两篇文章在 Directus 里下架（status→draft，`/api/articles` 不再列出，直接访问 slug 返回 404）；`POST /api/lab-sessions` 新增通用 `LABGEN_ENABLED_LAB_IDS` 白名单门禁（对称既有 Linux 白名单模式），非白名单 lab_id 返回 403，admin 账号不受限（继续内部研发不受影响）；safety-reviewer 三轮 A 类审查发现并修复两个 HIGH：(1) `LabSessionService` 只在路由层门禁，绕过路由的调用路径不受保护 — 在 `run_precheck()` 里加了同等门禁，`enabled_lab_ids` 用 `Optional[frozenset]=None` 哨兵表示"未配置门禁"（默认关闭，仅生产 DI 显式传入真实 `config.LABGEN_ENABLED_LAB_IDS` 才启用），避免了初版"默认空 frozenset 拒绝所有人"导致 12 个无关测试文件、146 个用例被误伤的问题；(2) `/api/articles/{slug}/lab-cta` 端点查询的是 LabGen draft 自身发布状态而非 Directus 文章状态，文章下架不影响它，未白名单的 lab 仍会泄漏 lab_id/cta_url — 加了同样的白名单检查；补 8 个回归测试（路由门禁 4 个 + service 层门禁 2 个 + lab-cta 泄漏 1 个 + IP 限流相关）；连带发现并修复 conftest.py 测试隔离缺口——真实 `.env` 的 `RESEND_API_KEY` 会经 `load_dotenv()` 泄漏进整个 pytest 进程，导致一批既有测试假设"未配置邮箱验证"的行为被真实生产配置污染，测试环境现在强制清空该变量
- fix(ops): `scripts/smoke_test.sh` 注册请求缺 `email` 字段（email 早已是必填项）导致测试账号从未真正创建成功，login 检查项常年静默失败又被 `|| true` 吞掉 — 补上 email 字段（用 RFC 2606 保留域名 `example.com`，本地 `.local` 域名会被 `email-validator` 判定为 special-use 拒绝），冒烟测试 4/4 通过
- docs: 新增 `SECURITY.md` — 漏洞报告渠道 + 24h/72h/7天响应承诺 + 已知 bandit 误报说明（B104/B108/B507/B604），补齐工程标准要求、公开上线前置项
- feat(auth): 注册邮箱验证 — 为公开上线防止批量注册占用 VM 名额 — 新增 `backend/email_client.py`（Resend API 客户端，对称 `directus_client.py` 的优雅降级模式：`RESEND_API_KEY` 未配置时 `send_verification_email` 直接返回 False，不影响现有注册/登录行为）；`AuthManager` 新增 `generate_verification_code`（6 位数字码，sha256 哈希存储，15 分钟过期）、`verify_email`（最多 5 次尝试，returns success/reason/attempts_remaining）、`is_email_verified`（老用户/未设置字段默认 True，防止历史账号被追溯锁定）；`register_user` 新增 `email_verified` 字段（`RESEND_API_KEY` 未配置时直接置 True，保持向后兼容）；`/api/auth/register` 响应新增 `verification_required` 字段，成功注册后按需发验证码邮件（发信失败不阻断注册，仅记 WARNING）；新增 `POST /api/auth/verify-email`、`POST /api/auth/resend-verification`（1次/60秒/用户限流，未知用户名不泄露账号存在性）；`/api/auth/login` 新增 403 拦截未验证邮箱账号；前端 `login.html`/`auth.js` 新增验证码表单（注册后按 `verification_required` 决定是否弹出，"重新发送"按钮）；补 51 个测试（`test_email_client.py`/`test_auth_email_verification.py`/`test_auth_email_verification_routes.py`）；本地起 dev server 用真实 HTTP 请求验证过默认路径（`RESEND_API_KEY` 未配置，即当前生产状态）与验证开启路径的可观察行为均符合预期；`RESEND_API_KEY` 需要用户自行在 resend.com 申请后填入 `.env`，不配置则功能保持关闭（现状不变）
- fix(security): 升级 9 个含已知 CVE 的依赖包，为公开上线扫清安全阻塞项 — starlette 0.52.1→1.3.1、fastapi 0.135.2→0.139.0（starlette 主版本升级，`backend/middleware.py` 是唯一直接依赖 starlette API 的文件，用法均为稳定 API 未受影响）、python-multipart 0.0.22→0.0.32、paramiko 3.4.0→5.0.0（SSH 终端 `backend/websocket.py` 唯一使用方）、python-dotenv 1.0.0→1.2.2、cryptography/idna/pygments/pytest/urllib3 传递依赖一并升级到无 CVE 版本；`prometheus-fastapi-instrumentator` 6.1.0→8.0.2（旧版本对新版 starlette 内部路由 API 不兼容，导致所有请求 500，升级后修复）；`tests/test_websocket_k3s.py` 5 个测试改用原生 `async def`（原先手动 `asyncio.get_event_loop().run_until_complete()` 是 safety-reviewer 检查清单明确禁止的过时写法，pytest/pytest-asyncio 版本升级后在全量 suite 下暴露为 `RuntimeError: no current event loop`）；`requirements.lock` 同步重新生成；`pip-audit` 确认 0 已知漏洞；4837 tests PASS，92.11% coverage

- feat(docs): 部署案例栏目（deployments_routes.py）接入 Directus CMS，与网络实验（docs_routes.py）对称——`fetch_deployment_list`/`fetch_deployment_detail` 优先读 experiments 集合 category=deployment，Directus 不可用时降级为本地 Markdown 文件；directus_client.py 抽取 `_fetch_category_list`/`_fetch_category_detail` 消除与网络实验的重复逻辑；补 22 个测试（directus_client 部署案例分支 + deployments_routes Directus 集成），消除此前部署案例栏目落后于网络实验的技术债
- fix(tests): test_health_response_time 全量 suite 负载下偶发超时 — 改为预热一次后取 3 次最小值，消除冷启动 import 偏差
- fix(ops): mypy SmartLogger.warning 参数类型修复（dict→str）
- fix(ops): delete_vm 对非 pool VM 的 403 status-check 盲目失败改为 blind destroy fallback — Proxmox K8SNetLab 角色新增 VM.Allocate（pool 范围），delete_vm 捕获 403 ResourceException 后跳过 status-check 直接调用 destroy（VM.Allocate 全局授权），补 2 个回归测试防止重现
- ops(labgen): credential reconcile — 清理 creds/vm_creds/500（VM 已销毁）旧凭证至 archive；清除 /var/lib/labgen-staging/verifier-credentials/vm_creds/ 嵌套残留目录；effective root 现仅含 299/401
- fix(labgen): no_vm_assigned 流程断点 — 用户点 Start Lab 被跳转到 /app 创建 VM 后无路回到原实验页面；修复：labgen-lab-init.js 的跳转链接改为 /app?next=<encoded_lab_url>，app.js VM 创建成功后若 next 参数指向 /labgen-lab.html 则自动跳回，验证前缀防止 open redirect；补回归测试防止重现
- feat(labgen): StaticValidator 新增两道发布前静态检查：commands.executor_compatible（所有步骤命令经 kubectl_executor.validate_command() 过滤，shell 变量/非 kubectl 命令/delete namespace 等在发布时即被阻断）和 commands.rbac_coverage（从 LEARNER_ALLOWED_PERMISSIONS 单一权限源推导，命令所需权限不在角色内则阻断发布）；LEARNER_ALLOWED_PERMISSIONS 提取为常量（含 pods/log:get），_ensure_role() 409 处理改为 create-or-replace 确保已有 session 自动升级角色；27 个新测试（TestCommandsExecutorCompatible/TestCommandsRbacCoverage/TestCommandRequiredPermissions）；防止未来 lab 内容与 executor/RBAC 不兼容问题在运行时才被发现
- fix(labgen): lab draft bb4fe651 step-7 verify namespace_not_exists 永远失败 — 原命令 kubectl delete namespace 被 executor 拦截，namespace 由 complete_session() 服务端回收，改为空 verify 列表（自动通过）+ 命令改为 kubectl delete deployment crash-demo，补回归测试防止重现
- fix(labgen): lab-learner RBAC Role 缺少 pods/log 子资源导致 kubectl logs 403 Forbidden — 新增 pods/log:get 规则，409 处理改为 create-or-replace 确保已有 session 自动升级，kubectl patch 修复当前 live session；补 2 个回归测试防止重现
- fix(labgen): lab draft bb4fe651 step-3/4 使用 shell 变量语法（POD_NAME=$(...) / $POD_NAME / -o jsonpath）在受限 kubectl executor 中全部失败 — 改为 label selector 命令（kubectl describe pods -l app=crash-demo / kubectl logs -l app=crash-demo --previous），补回归测试防止重现
- fix(labgen): kubectl executor 误拦截 -n/--namespace flag 导致实验步骤命令无法执行 — 改为验证阶段放行、execute() 阶段静默剥离用户提供的 namespace flag（executor 始终注入自身 --namespace），6 个单元测试 + test_labgen_lab_kubectl_ws.py 期望更新
- fix(tests): delete VM 相关测试未 mock _get_lab_session_repo，真实数据（VM 500 有活跃 session）导致 409 干扰测试结果 — 补全 mock 覆盖 test_api_routes/test_contract/test_robustness/test_security/test_e2e
- feat(labgen): eligibility API 新增 existing_session_id 字段，前端对 SESSION_ALREADY_ACTIVE 渲染绿色 Resume Lab 按钮替代灰色 Start Lab — 补回归测试防止重现
- fix(labgen): labgen-lab-init.js no_vm_assigned 错误仅显示红色文本无操作路径 — 改为渲染独立 UI 块含「前往创建实验环境」跳转按钮，保留 /app 链接；4 个回归测试防止重现
- fix(stability): stability_watch.sh heredoc 数值变量含换行导致 Python SyntaxError — 改用 os.environ 传值，预先 tr -d '[:space:]' 清理所有数值变量
- ops(stability): scripts/stability_watch.sh — 非侵入式 24h 稳定性观察脚本（10 项检查：health/error-logs/VMID-500-599/diffs-size/disk/memory/article/CTA/git-dirty/cloudflared）；JSON 格式记录到 /var/log/k8s-netlab-stability-watch.log；cron 每 30 分钟执行；不创建 session/VM，不触发 LLM，不修改数据
- fix(main): auto_cleanup_task 对 403 Forbidden 错误无限重试导致 ERROR 日志风暴 — 根因：auto-cleanup 尝试删除 tracker 中 TTL 过期的 VM 400（pool 外部，token 无权限），每 60 秒重试并记录 ERROR；修复：403/Permission check failed/Forbidden 触发 untrack+WARNING，停止重试；7 个回归测试防止重现
- feat(health): health check expansion — sessions sub-section added (active_session_count/failed_terminal_session_count/tainted_vm_count/lab_review_diffs_size_mb/zombie_draft_count/warnings); backward compatible; no secrets exposed; 10 new tests; service restart verified
- ops(runbook): PHASE 4 runbook gate — RUNBOOK.md expanded with env loading order, VMID policy, K3s v1.34.4 known limitations, failed session recovery (场景七), data archive/cleanup (场景八), owner internal run checklist, small cohort readiness checklist; scripts/ops_smoke_check.sh added (11 automated checks, all pass)
- feat(retention): data growth retention gate — DataRetentionService with dry-run report/archive; archived 11,736 orphaned lab_review_diffs groups (15MB→64KB); audit log rotation by age (90-day TTL); 7 published lab IDs protected; 16 tests; data/archive/ created
- ops(recovery): failed session recovery gate — force-closed 4 sessions (2× LAB_CLEANUP_FAILED linux spike, 2× LAB_START_FAILED namespace_create_failed); admin-attested 5 LAB_ABORTED sessions (VMs absent from Proxmox, zero namespace residual); all 82 sessions now cleanup_verified=True, residual_risk=False, active=0; +4 attestation tests
- refactor(mypy): zero-error gate — fix 14 mypy errors across 10 files (auth_deps.py, directus_client.py, deployments_routes.py, ai_tutor_routes.py, articles_routes.py, labgen/verifier.py, labgen/llm_provider_boundary.py, labgen/lab_kubectl_ws.py); add types-PyYAML to requirements.txt; wire mypy hard gate into pre-push-security-check.sh

### Changed
- fix(labgen): K3sNamespaceLifecycleAdapter.create_namespace() 409 (AlreadyExists) 未做幂等处理，返回 False — 根因：create_namespace 对 409 走通用 API 错误分支（log error + return False），而合约要求 create 已存在 namespace 等同于成功；修复：与 delete_namespace/ensure_verifier_rolebinding 保持一致，409 返回 True；补两个回归测试防止重现
- fix(labgen): PlatformVerifierInitializer.ensure_verifier_identity() 用 replace_cluster_role() 导致 K3s v1.34.4 RBAC authorization evaluator 缓存破坏（所有 SA token 请求 403 Forbidden）— 根因：K3s v1.34.4 对 ClusterRole UPDATE 事件处理不正确，RBAC informer 缓存失效；修复：改为 delete_cluster_role() + create_cluster_role()（DELETE+ADD 事件对，RBAC informer 可正确处理）；补回归测试防止重现
- fix(main): auto_cleanup_task 删除 VM 前未检查是否有活跃 lab session — 根因：自动清理仅依赖 vm_tracker TTL，未查询 LabSessionRepository，导致 LAB_ACTIVE 状态的 lab session 所在 VM 被强制销毁（K3s namespace 消失，lab 无法继续）；修复：在 delete_vm 前调用 `_get_lab_session_repo().has_active_session_for_vm(vm_id_str)`，返回 True 则跳过删除并记录 WARNING；补两个回归测试防止重现
- ops(labgen): P0 Ops Recovery + Production Readiness Hardening — Owner Internal Run 前置门禁；修复 VM 299 verifier credentials 写入路径错误（应为 /var/lib/labgen-staging/verifier-credentials/299/，实际写入 creds/vm_creds/299/，根因：provision script 未加载 home_lab_mvp.env）；新增 backend/labgen/ops_env.py 规范 env 加载顺序（.env + /etc/labgen/home_lab_mvp.env override=True）；新增 scripts/provision_verifier_credentials.py + scripts/reset_admin_password.py（凭证操作脚本，不打印 secret）；home_lab_mvp.env VM_CLEANUP_EXEMPT_IDS 从 401 修正为 401,299（修复 VM 299 未受 exempt 保护的 bug）；新增 LAB_FORCE_CLOSED 终态 + admin_force_close_session() + list_failed_sessions() + GET /api/lab-sessions/admin/failed + POST /api/lab-sessions/admin/{id}/force-close（admin session recovery 路径）；/api/health 新增 labgen 段（credential_root_exists/exempt_vms/missing_credentials/status ok|degraded）；mypy namespace type safety 修复（lab_session_service/runtime_precheck/namespace_lifecycle/image_resolver/verifier_credentials 5 个文件，错误数从 24 降至 14）；RUNBOOK.md 新增场景五（smoke-admin 密码丢失）和场景六（Verifier Credentials 丢失）；4723 tests PASS 92.03% coverage；VMID 500-599 untouched；service health ok；VM 299 + 401 credentials both present


- feat(labgen): LabGen Session UI/UX 统一 — labgen-session.html 从 max-w-3xl 竖排布局重构为全宽 kubectl 终端（flex-1 填充视口）+ 左侧滑出式抽屉（复用 doc-drawer/step-pill CSS），与旧版 11 个实验 index.html 保持一致的视觉风格；抽屉包含进度条/step pills/实验步骤/实验背景两 tab，底部固定操作栏（检查当前步骤/完成实验/中止实验）；全界面中文化（Check Current Step→检查当前步骤/Complete Lab→完成实验/Abort→中止实验/Need help?→操作提示/Failure reason→异常原因）；labgenViews.js renderSessionView 移除 action buttons（改为 drawer 静态 HTML），新增 getSessionActionStates() 纯函数供 init.js 更新按钮状态；labgen-session-init.js 完整重构（drawer toggle/tab 切换/progress 更新/按钮状态同步）；test_views.mjs 4 个 renderSessionView 按钮断言测试迁移为 getSessionActionStates 纯函数测试；4673 tests PASS 92.15% coverage；VMID 500-599 untouched；LLM generation 未触碰；已发布文章及旧版实验未受影响
- fix(main): auto_cleanup_task exempt 检查类型不匹配 — get_expired_vms() 返回 JSON string key（如 "299"），frozenset 存 int，`"299" in frozenset({299})` 静默返回 False 导致 staging VM 被反复尝试删除（ERROR 日志每分钟循环）；修复：在 loop 入口 normalize vm_id 为 int 再做 exempt 比较；补回归测试 test_exempt_vm_skipped_when_id_is_string 防止重现
- fix(labgen): POST /api/lab-sessions no_vm_assigned 422 显示 "Request failed (422)"（不可操作）— 根因一：labgenClient.js 从 detail.message 取文案，但 422 body 是 detail.precheck_failures[0].message，取不到则 fallback 到通用 "Request failed"；根因二：错误文案 "Contact your instructor" 不告知用户下一步；修复：(1) labgenClient.js 从 precheck_failures[0].message 提取 message，(2) routes.py no_vm_assigned 消息改为"请前往实验平台（/app）创建环境后再回来"；补回归测试（test_create_without_vm_id 新增 message /app 检查）；4673 tests PASS 92.16% coverage
- ops(labgen): Owner Soft Launch Article #1 Published Verified Lock — CEO/CTO post_publish_browser_ui_verification 全部通过（article rendered/title/body visible/CTA enabled/header bottom match/anonymous flow PASS/logged-in learner PASS/无泄露/无 Growth assets/VMID 500-599 untouched）；产出 docs/labgen/OWNER_SOFT_LAUNCH_ARTICLE1_PUBLISHED_VERIFIED_v0.1.md；Growth Room 保持 blocked
- fix(labgen): POST /api/lab-drafts/generate 权限从 login-only 加固为 admin-only（require_admin_user）— 根因：endpoint 仅要求已登录但未校验 admin 角色，普通 learner 可调用并在 repository 创建 DRAFT 记录（当前使用 FakeDraftGenerationAdapter 无 LLM，但若 provider_boundary 后续接入 live_enabled 模式则 learner 可触发 LLM 调用，违反 Phase 1 边界）；加固后 learner 调用返回 403；admin 仍正常 201；4 个回归测试（test_labgen_generate_admin_hardening.py：learner 403/unauthenticated 401-403/admin 201/无 draft 写入）；同步更新 test_labgen_draft_repair/_draft_preview/_llm_generation（_gen_ctx 原覆盖 get_current_user=student1，补覆盖 require_admin_user=admin）；4672 tests PASS 92.16% coverage；BLOCKER=0 HIGH=0 MEDIUM=0；article #1 正常访问未受影响
- fix(labgen): namespace_exists() 在命名空间删除后收到 403（SA 的 namespace-scoped RoleBinding 随命名空间一起删除）时抛出 500；现在同时捕获 403 作为"命名空间不存在"处理（与 404 语义相同）；补回归测试 test_adapter_namespace_not_exists_true_on_403 防止重现
- fix(labgen): image_resolver._default_http_get() 移除 verify=False（bandit HIGH B501）；内网 registry（172.16.100.1:5000）使用 HTTP，不需要 SSL；补回归测试 test_default_http_get_does_not_disable_ssl_verification 防止重现
- fix(labgen): lab draft bb4fe651 step-5 patch 目标字段错误（/args 应为 /command）；kubectl create deployment -- args 设置的是 command 字段而非 args 字段；已通过彩排 step-5 验证修复正确
- ops(labgen): Owner Soft Launch Article #1 Internal Rehearsal — VMID 299 staging VM（172.16.100.167）；7 步 CrashLoopBackOff 实验彩排全部 PASS（NAMESPACE_EXISTS/DEPLOYMENT_UNAVAILABLE×3/DEPLOYMENT_READY×2/NAMESPACE_NOT_EXISTS）；rehearsal_completed=True；cleanup_verified=True；bandit HIGH=0；static validator 17/17 PASS；发布技术门禁全部清除；VMID 500-599 untouched；no publish
- feat(labgen): Draft Repair + Rehearsal Enablement — Owner Soft Launch Article #1；新增 VerifyType.DEPLOYMENT_UNAVAILABLE（验证 Deployment 存在且 available_replicas=0，用于 CrashLoopBackOff 观察步骤）和 VerifyType.NAMESPACE_NOT_EXISTS（验证命名空间已删除，用于 cleanup 步骤）；K8sVerifierClientPort/FakeK8sVerifierClient/K8sVerifierClientAdapter 新增对应方法；_SUPPORTED_TYPES+_dispatch+_make_detail 全部更新；lab draft bb4fe651 从 4 步（有 4 项质量问题）改为 7 步（setup+observe+describe+logs+patch-fix+verify+cleanup），所有步骤无占位符/无交互式命令/verifier 类型正确匹配；StaticValidator 17/17 PASS 0 FAIL；23 个新测试（test_labgen_new_verifier_types.py）；4666 tests PASS 92.16% coverage；VMID 500-599 untouched；no publish；rehearsal_required=True；STAGING_ENV_MISSING（需 Owner 提供 K3s staging VM）

- fix(tests): test_labgen_llm_provider_boundary.py — test_invalid_mode_name_safe_fallback 和 test_invalid_provider_name_safe_fallback 改用 patch.object(cfg, ...) 替代 patch.dict(os.environ, ...)，修复 live LLM 环境下两个测试误判（根因：config 模块级属性在 import 时已缓存，patch.dict 无法影响已绑定的模块级变量）；补回归测试防止重现
- ops(labgen): Owner Soft Launch Article #1 Publish Gate Readiness Check v0.1 — G-69；新增 [INTERNAL SAMPLE] 标题拦截器（article_draft_routes.py：提交时若 title 以 [INTERNAL SAMPLE] 开头则 422 拒绝，大小写不敏感）；新增 tests/test_labgen_owner_article_gate.py（41 tests：A-owner article gate/B-pipeline readiness/C-live LLM mode guard/D-publish pipeline order/E-exposure guard/F-E2E gate smoke/G-regression）；决策：OWNER_SOFT_LAUNCH_ARTICLE1_NEEDS_ITERATION（pipeline 架构已就绪，缺真实 owner 文章输入）；4643 tests PASS 92.14% coverage；BLOCKER=0 HIGH=0 MEDIUM=0；VMID 500-599 untouched；no real owner article provided；internal sample cannot reach publish gate（新拦截器已验证）；产出：docs/labgen/OWNER_SOFT_LAUNCH_ARTICLE1_PUBLISH_GATE_RESULT_v0.1.md
- feat(labgen): Live LLM Admin-only Internal Trial & Rehearsal Gate v0.1 — G-68；新增 LLMProviderBoundaryService.call_live_with_messages() 公开方法（取代私有 _live_adapter 直接访问）；新增 LLMProviderConfigError 异常类；article_draft_service.py 更新调用路径（移除 LLMProviderRequest 依赖，使用 call_live_with_messages()）；测试文件 test_labgen_live_llm_trial.py（40 tests：A-配置/B-provider adapter/C-call_live_with_messages/D-article_to_lab/E-暴露防护/F-live rehearsal/G-回归）；内部样本文章 [INTERNAL SAMPLE] k8s ConfigMap 动态配置管理 Live 生成成功（5步/has_commands/has_verify/has_cleanup/validation_passed/rehearsal_required=True/publish_status=draft）；安全不变量全部保持：API key 不入审计/不入代码库；raw article body 不入审计；prompt 不暴露；never publish；always rehearsal_required；env missing→fail closed；LLM 不绕过 Admin Review；source_article_id 不暴露给 learner；4602 tests PASS 92.16% coverage；BLOCKER=0 HIGH=0 MEDIUM=0；VMID 500-599 untouched；决策：LIVE_LLM_ADMIN_TRIAL_READY_WITH_NOTES（内部样本文章，非真实 owner 文章，约束信号收敛）
- feat(labgen): Phase 1 Soft Launch MVP — Admin Article Upload + LLM Draft Generation + Publish Flow v0.1 — G-67；新增 LABGEN_LLM_MODE env var（fake_only 默认/live_admin_only 可选）；新增 llm_audit.py append-only 审计日志（仅元数据，不存文章原文/LLM输出/API key）；新增 article_lab_prompt_builder.py（文章→Guided Practice Lab 提示词，凭证自动脱敏/截断，禁止 chain-of-thought 泄漏）；CreateArticleDraftRequest 扩展 Phase 1 字段（copyright_confirmed 门禁/target_domain k8s|linux/intended_reader/publish_channel/safety_notes/desired_lab_title）；新增 POST /{draft_id}/generate-lab 端点（admin-only，feasibility gate→LLM→StaticValidator→rehearsal_required=True→always DRAFT）；ArticleDraftService.generate_lab_from_article()（NOT_LAB_READY→422/fake_only用FakeDraftGenerationAdapter/live_admin_only用LLMProviderBoundaryService/LLM output强制回写DRAFT/source_article_id=draft_id/rehearsal_required=True/PublishService.publish无rehearsal→RehearsalNotCompleted）；安全约束：无 public upload/无 reader LLM/无 URL scraping/无 Docker domain/无直接 publish/无明文 API key/无 secrets 入日志/无 raw_text 持久化/无 source_article_id 暴露给学员；55 新测试（test_labgen_phase1_soft_launch.py：A-admin upload/B-operability gate/C-LLM adapter/D-draft generation/E-publish flow/F-exposure guards/G-prompt builder/H-regression）；现有测试更新（test_labgen_article_draft_api.py + test_labgen_admin_rehearsal.py 加 copyright_confirmed=True）；4563 tests PASS 92.01% coverage；BLOCKER=0 HIGH=0 MEDIUM=0；VMID 500-599 untouched；no live LLM in tests；no public upload；no URL scraping；no Docker domain
- fix(labgen): Article Page Primary CTA Unification v0.1 — G-66；MEDIUM-002 已修复；article detail 页 header nav "进入实验室"（data-lab-nav 属性）在 has_cta=true 时自动更新 href 为 ctaUrl（与嵌入式 CTA 相同目标）、文案改为"进入配套实验"；无 CTA 时保持原 /app 目标；selector 改为 [data-lab-nav] 稳定属性（不依赖可变 href）；安全：ctaUrl 经 _validateCtaUrl() 预验证（仅允许 /labgen-lab.html?labId= 前缀）；使用 .href + .textContent，无 innerHTML；if (navLabLink) null 守卫；safety-reviewer APPROVED 无 BLOCKER/HIGH/MEDIUM；49 tests PASS（+5 新增：stable attribute/header nav update/validated url/text change/safe dom api）；4507 tests PASS 92.25% coverage；E2E smoke: health OK / CTA API has_cta=true ctaUrl 正确 / article page 200 / 无错误日志；VMID 500-599 untouched；无 LLM；无 scraping；无 public upload；artifact: ARTICLE_PAGE_PRIMARY_CTA_UNIFICATION_RESULT_v0.1.md
- ops(labgen): Small Cohort Execution v0.1 — G-65；SMALL_COHORT_EXECUTION_PASSED_WITH_NOTES；3/3 LAB_CLOSED cleanup_verified=True residual=0 active=0 tainted={}；100% Start/Complete；BLOCKER=0 HIGH=0 MEDIUM-002(新增：文章页面两个竞争实验入口指向不同目的地，header nav→catalog vs 嵌入式CTA→直接lab，2/3 learner 初始被 header nav 分流)；learner01 完整 Article→CTA→Lab 路径；learner02 经 catalog 进入；learner03 先 header nav 后底部 CTA；10-Q 3/3 收集；Q10 亮点："读了能马上验证"(l01)/"感兴趣乐意动手"(l02)/"真实厂线案例会订阅"(l03)；no LLM/no scraping/no public upload/VMID 500-599 untouched；artifact: SMALL_COHORT_EXECUTION_RESULT_v0.1.md；推荐：修复 MEDIUM-002 后第二轮 cohort 或扩大受众
- ops(labgen): Small Cohort Planning Gate v0.1 — G-64；SMALL_COHORT_PLANNING_READY_WITH_NOTES；fix MEDIUM-001: article.js unauthenticated CTA btn.href 加 ?next=encodeURIComponent(ctaUrl)（新用户点击 CTA → register/login 后自动跳转到目标 lab，而非 /app）；回归测试 test_article_js_unauthenticated_cta_includes_next_param；22 项 readiness checks 全通（article reachable / CTA has_cta=True / lab_id correct / source_article_id absent / registration email field present / active real learner sessions=0 / residual=0 / tainted={} / catalog visible / K8s+Linux lab unaffected / LLM fake_only / VMID 500-599 untouched / no public upload / no source_article_id/raw_text/email/password_hash exposure）；两个 LAB_START_FAILED 为 terminal stale dev sessions（k8s_test/smoke-admin，started_at=None，NOTE）；产出：SMALL_COHORT_PLAN_v0.1.md（scope/assignment/execution/account/feedback/success-failure/rollback/runbook）+ SMALL_COHORT_APPROVAL_GATE_v0.1.md（owner input template + 明确 YES 格式 + 约束声明）+ SMALL_COHORT_PLANNING_RESULT_v0.1.md；cohort 未启动；accounts 未创建；awaiting owner: learner identities + test window + "YES, approve Small Cohort Execution."；4503 tests PASS 92.23% coverage；BLOCKER=0 HIGH=0 MEDIUM=1(FIXED) NOTE=1；无 live LLM；无 URL scraping；无 public upload；VMID 500-599 untouched
- ops(labgen): Real External Article Binding Gate & E2E Validation v0.1 — G-63；REAL_EXTERNAL_ARTICLE_BINDING_READY_WITH_NOTES；owner 实时确认 article_channel=official_site；路径 A 执行；现有绑定元数据正确无需变更；CTA API 验证 has_cta=true 正确；article.html embedded CTA owner 截图确认；E2E owner 亲自操作（lnx-trusted-02）：session 85e13c1d LAB_CLOSED cleanup_verified=True residual=0 tainted={}；4 步全绿；6 项 Negative Checks PASS；无代码变更；BLOCKER=0 HIGH=0 MEDIUM=0；WITH_NOTES: article_type 无 schema 字段（owner 确认记录于文档）；reader lnx-trusted-02 为 legacy 账号；Phase 1 Article→CTA→Login→Lab→Cleanup 闭环完整验证；推荐下一步：Small Cohort Planning Gate；artifact: REAL_EXTERNAL_ARTICLE_BINDING_RESULT_v0.1.md
- feat(auth): Email Registration P1 v0.1 — G-62；EMAIL_REGISTRATION_P1_READY_WITH_NOTES；注册 API 新增必填 email 字段（EmailStr，format 校验，trim+lowercase 归一化，唯一性校验，max 254）；AuthManager.register_user() 加 email: Optional[str] = None（向后兼容）；is_email_taken() 方法；email 存入 data/users.json，不在 API 响应中返回；历史用户（无 email）仍可正常登录；login 仍为 username+password only；注册表单新增邮箱 input（type=email，required，autocomplete=email）；auth.js 在注册 payload 中含 email（trim+toLowerCase 客户端归一化）；email-validator==2.3.0 加入 requirements.txt；无 SMTP/SendGrid/email sending；无 email verification；无 forgot password；无 marketing consent；无 public upload；无 live LLM；45 新测试（test_email_registration.py: A-注册API/B-历史用户兼容/C-登录/D-前端静态/E-安全边界/F-回归）；现有测试中 /api/auth/register HTTP 调用已更新含 email；全量测试 PASS
- feat(labgen): Article Detail Embedded CTA Component v0.1 — G-61；ARTICLE_DETAIL_EMBEDDED_CTA_READY_WITH_NOTES；新增 reader-safe public endpoint GET /api/articles/{slug}/lab-cta（无需认证，仅返回 PUBLISHED+cta_enabled 的 lab 数据）；新增 ArticleLabCTAResponse 模型（has_cta/lab_id/lab_title/lab_summary/estimated_duration/domain/cta_url/sandbox_note，不含 source_article_id/article_url/article_title/admin notes/raw text）；新增 get_lab_draft_repository() 可注入 FastAPI 依赖；新增 _find_lab_by_article_slug() 通过解析 article_url 的 query 参数匹配 slug；article.html 新增 <div id="lab-cta-container"> 槽位（位于文章内容与评论区之间）；article.js 新增 loadLabCTA()/renderLabCTA()/_validateCtaUrl()：全程 DOM createElement + textContent，零 innerHTML 注入；_validateCtaUrl() 仅允许 /labgen-lab.html?labId= 前缀，拒绝 javascript:/data:/外部 URL；未登录读者点击 CTA 重定向至 /login.html；已登录读者 CTA 直连 /labgen-lab.html?labId={lab_id}；已验证 slug=linux-files-permissions-basics → lab 6c439064 CTA 正确返回，welcome-to-k8s-netlab → has_cta:false；source_article_id/article_url/admin fields 均不出现在响应中；safety-reviewer APPROVED_WITH_NOTES（LOW: cta_url 测试用 Python 重实现而非 JS，静态扫描补偿）；43 新测试（test_article_embedded_cta.py: A-API/B-slug匹配/C-URL验证/D-HTML+JS静态安全/E-暴露规则/F-回归）；4461 tests PASS，92.28% coverage；BLOCKER=0 HIGH=0 MEDIUM=0；WITH_NOTES: Linux 文章仍为 mock_admin_article；无 email registration；无 live LLM；无 URL scraping；无 public upload；VMID 500-599 untouched；artifact: ARTICLE_DETAIL_EMBEDDED_CTA_RESULT_v0.1.md
- ops(labgen): Article-Lab Content Consistency Remediation v0.1 — G-60；ARTICLE_LAB_CONTENT_CONSISTENCY_REMEDIATED_WITH_NOTES；(1) 创建 Directus Linux 文章（slug=linux-files-permissions-basics，id=3，已 published）替换原 K8s welcome 文章绑定；(2) 通过 admin PATCH 更新 Linux lab 6c439064 的 article_url/article_title/article_channel；(3) CTA 重新生成：topic_consistency_warning=None（主题匹配），4 种格式均由系统生成；(4) 新模块 backend/labgen/topic_consistency.py：check_article_lab_consistency()，rule-based keyword 检查，Linux/K8s 双域关键词匹配，零 LLM；(5) LabDraftCTAResponse 新增 topic_consistency_warning Optional[str] 字段；(6) StaticValidator 新增 content.article_lab_topic_consistency 检查（draft_warning 级别，非 publish_blocking）；(7) 已证实 Step 3 数据完全一致（G-59 result 文档错误）：commands/do/observe/verifier 全部为 chmod 600，无 644；(8) Reader E2E g60-reader-01 session 3254de61 完整通过：Start→Step1-4 PASS→Complete(LAB_CLOSED,cleanup_verified=True,residual=0)；(9) 12 项 Negative Checks 全通；(10) 33 新测试（test_labgen_article_lab_consistency.py：A-topic_consistency/B-static_validator/C-step_verifier_consistency/D-CTA_warning/E-real_lab_binding）；4418 tests PASS，92.28% coverage；article_type=mock_admin_article（为此次验证创建，非外部预发布文章）；BLOCKER=0 HIGH=0 MEDIUM=0；source_article_id 未暴露；raw_text 未暴露；无 live LLM；无 URL scraping；无 public upload；VMID 500-599 未触碰；artifact: ARTICLE_LAB_CONTENT_CONSISTENCY_REMEDIATION_RESULT_v0.1.md
- ops(labgen): Admin Article Input MVP End-to-End Validation v0.1 — G-59；ADMIN_ARTICLE_INPUT_MVP_E2E_READY_WITH_NOTES；绑定真实 Directus 文章（welcome-to-k8s-netlab，official_site 渠道）至 Linux lab 6c439064；服务重启加载 G-58 代码（article binding 字段生效）；admin 通过 PATCH /api/labgen/drafts/{id} 绑定 article_url/article_title/article_channel/cta_enabled=true；GET /api/labgen/drafts/{id}/cta 返回 4 种 CTA 格式（plain/markdown/html/copyable）均系统生成，非手工拼接；Reader E2E：g59-reader-01 注册→登录→GET /api/labs/{id}（article_url 可见，source_article_id 不在响应）→ Start(LAB_ACTIVE, vm_id=linux-sandbox) → lfp-step-1 dir_exists+file_exists+content_matches PASS → lfp-step-2 content_matches PASS → lfp-step-3 mode_matches(600) PASS → lfp-step-4 auto-pass(ready_to_complete=True) → Complete(LAB_CLOSED, cleanup_verified=True) → residual=0, active=0, tainted={}；13 项 negative check 全通（403/422/source_article_id absent/raw_text absent/CTA无不安全声明/draft lab不可启动）；4385 tests PASS 0 failures；代码零变更；BLOCKER=0 HIGH=0 MEDIUM=1(chmod 644 vs 600 step内容不一致 NOTE)；WITH_NOTES：文章非 Linux 主题（platform intro），step 3 内容说明与 verify 模板不一致；email registration 仍 P1；无新 lab；无 live LLM；无 public upload；无 customer pilot；VMID 500-599 untouched；artifact: ADMIN_ARTICLE_INPUT_MVP_E2E_RESULT_v0.1.md；推荐下一步：Article Detail Embedded CTA Component
- feat(labgen): Article URL + CTA Tool Implementation — G-58；ARTICLE_URL_AND_CTA_TOOL_READY；LabDraft 加 5 个 article binding 字段（article_url/article_title/article_channel/article_published_at/cta_enabled）；字段均有服务端校验（URL scheme 必须 http/https，title 无 HTML 标签，channel 枚举白名单）；新 admin-only endpoint GET /api/labgen/drafts/{id}/cta 返回 4 种 CTA 格式（plain/markdown/html/copyable text）；PATCH /api/labgen/drafts/{id} 支持更新 article metadata；LearnerLabDetail 加 article_url/article_title/article_channel 字段，仅 cta_enabled=True 时暴露；source_article_id 和原始文章文本不出现在任何 API 响应中；labgenClient.js 加 getDraft/patchDraft/getDraftCta 方法；labgen-admin.html 加 article metadata 表单 + 4 种 CTA 复制按钮；safety-reviewer APPROVED_WITH_NOTES（MEDIUM article_channel 无校验 → 已修复为枚举白名单）；42 个新测试（test_labgen_article_url_cta.py）；全量 4382+42 tests PASS，coverage 92.26%；pre-commit PASS；BLOCKER=0 HIGH=0 MEDIUM=0（修复后）；email registration 仍为 P1 未实现；无新 lab 发布；无 live LLM；无 public upload；无 Docker domain；无 VMID 500-599 触碰
- ops(labgen): Admin-curated Article-to-Lab Flow Final Alignment & P0 Gap Plan v0.1 — G-57；ADMIN_CURATED_ARTICLE_TO_LAB_FLOW_ALIGNED_WITH_NOTES；完成 Phase 1 产品方向对齐（admin-curated, no UGC, no live LLM）；确认现有 6 个 published labs（5 K8s + 1 Linux）为 Phase 1 标准模板资产而非 demo；确认网站首页文章系统已存在（landing.html + Directus CMS + articles_routes.py）；确认 Admin→Publish 完整流程已实现并通过真实读者验证（G-34 K8s, G-51 Linux Trusted Reader）；识别 3 个 gap：P0(1) LabDraft 无 article_url 字段、P0(2) 无 CTA 标准工具、P1 无邮箱注册字段；确认 P0 优先于 P1（email 非 Article→Lab 闭环阻塞点）；WITH_NOTES 原因：P0 两项未实现但不阻塞现有实验运行；placeholder/secret/TODO scan CLEAN；pre-commit PASS；新产出：4 个对齐文档（ADMIN_CURATED_ARTICLE_TO_LAB_FLOW_v0.1.md + EXISTING_LAB_TEMPLATE_EXTRACTION_v0.1.md + PHASE1_GAP_PRIORITIZATION_v0.1.md + ADMIN_CURATED_FLOW_ALIGNMENT_RESULT_v0.1.md）；无代码变更；BLOCKER=0 HIGH=0 MEDIUM=0；推荐下一步：Article URL + CTA Tool Implementation
- fix(frontend): /app 页面无 LabGen 入口 — 补回归测试防止重现；根因：index.html header 无 /labgen-catalog.html 链接，app.js 未处理 ?lab= 参数；修复：(1) header 加 "LabGen 实验" 导航按钮链接至 /labgen-catalog.html；(2) app.js init() 检测 ?lab= 参数并 redirect 至 /labgen-lab.html?labId=...；4 个回归测试（test_frontend_labgen_navigation.py）
- ops(labgen): Second Linux Trusted Reader Pilot Planning Review v0.1 — G-55；SECOND_LINUX_TRUSTED_READER_PILOT_PLANNING_REVIEW_READY_WITH_NOTES；plan reviewed and updated（reader identity 格式、test window 格式、independence/intervention tracking 方法 F.3 全部补入）；health check PASS（catalog=6，active=0，tainted={}，residual=0，LLM mode=fake_only，VMID 500-599 untouched，service healthy，K8s 27 targeted tests PASS）；full test suite 4335 passed 92.26% coverage；BLOCKER=0 HIGH=0 MEDIUM=0；second reader NOT started；account NOT created；cohort BLOCKED（LOW-004 independence 未完全仪表化）；awaiting user inputs: reader identity + test window + explicit YES phrase ("YES, approve Second Linux Trusted Reader Pilot Execution.")；新产出：SECOND_LINUX_TRUSTED_READER_PILOT_PLANNING_REVIEW_RESULT_v0.1.md；updated: SECOND_LINUX_TRUSTED_READER_PILOT_PLAN_v0.1.md + LINUX_PILOT_FEEDBACK_ATTESTATION_AND_SHUTDOWN_RESULT_v0.1.md（Section N）；推荐下一步：Second Linux Trusted Reader Pilot Execution（用户提供必要输入后）
- ops(labgen): Linux Pilot Feedback Attestation, Environment Shutdown & Next Reader Planning v0.1 — G-54；LINUX_PILOT_FEEDBACK_ATTESTATION_AND_SHUTDOWN_READY_WITH_NOTES；owner onsite attestation 记录（USER_ONSITE_ATTESTATION："测试非常顺利"）；MEDIUM-001 CLOSED→NOTE（owner onsite 观察关闭"零 feedback"阻断，非完整 10-Q，限制标注）；MEDIUM-002 LOW（USER_OBSERVED_SMOOTH_COMPLETION，独立性未完全仪表化，blocks cohort 但允许 second reader planning）；environment shutdown 确认：session 8d9bd8db LAB_CLOSED cleanup_verified=True workspace ABSENT，active=0，tainted={}；VM 401 已不在 Proxmox（G-51 记录，Linux 不依赖 VM）；K8s Lab 5 211 targeted tests PASS；LLM=0；VMID 500-599 untouched；新产出：LINUX_PILOT_FEEDBACK_ATTESTATION_v0.1.md + LINUX_PILOT_FEEDBACK_ATTESTATION_AND_SHUTDOWN_RESULT_v0.1.md + SECOND_LINUX_TRUSTED_READER_PILOT_PLAN_v0.1.md；推荐下一步：Second Linux Trusted Reader Pilot Planning Review（等待用户提供 reader identity + test window + 明确 YES）；second reader 未启动；cohort 仍 BLOCKED（需 second reader pass + captured 10-Q）
- ops(labgen): Linux Pilot Feedback Remediation v0.1 — G-53；LINUX_PILOT_FEEDBACK_REMEDIATION_NEEDS_ITERATION；MEDIUM-001 NOT CLOSED（10-Q feedback 未获取，data/ 无反馈字段，无反馈文件，任务提交中未提供答案，Claude Code 无法直接访问人类用户）；MEDIUM-002 NOT CLOSED（独立性无法确认：69秒 session，无步骤级时间戳，无重试/帮助记录；分类：TECHNICAL_SMOKE_BY_TRUSTED_READER，非 INDEPENDENT_COMPLETION）；expansion 继续 BLOCKED；K8s 211 targeted tests PASS；active=0，tainted={}，health OK，LLM=0，VMID 500-599 untouched；不是 second reader / cohort / customer pilot / public launch；推荐下一步：Hold Expansion（等待用户选择 Path A 提供 10-Q 答案 或 Path B 第二次 Trusted Reader Pilot 含内置反馈收集）；artifact: LINUX_PILOT_FEEDBACK_REMEDIATION_RESULT_v0.1.md
- ops(labgen): Linux Trusted Reader Pilot Exit Review & Stabilization Gate v0.1 — G-52；LINUX_TRUSTED_READER_PILOT_EXIT_REVIEW_READY_WITH_NOTES；技术通过（session 8d9bd8db LAB_CLOSED cleanup_verified=True 4步全完成，residual=0，tainted={}，health OK，K8s 211测试回归 PASS，LLM=0，VMID 500-599 untouched）；BLOCKER=0，HIGH=0；MEDIUM-001(qualitative feedback not captured 阻断扩大)，MEDIUM-002(independence evidence thin — 69s session)；LOW-001(test workspace dirs)，LOW-002(no article CTA)，LOW-003(2 LAB_START_FAILED historical)；不是 public launch / customer pilot / live LLM；推荐下一步：Linux Pilot Feedback Remediation；artifact: LINUX_TRUSTED_READER_PILOT_EXIT_REVIEW_RESULT_v0.1.md
- ops(labgen): Linux Trusted Reader Pilot PASSED — G-51；真实 trusted reader `lab_test` 完成完整 Linux 实验闭环；session `8d9bd8db` LAB_CLOSED cleanup_verified=True 4步全完成（lfp-step-1/2/3/4）；post-run audit：active=0，tainted={}，health OK，error logs empty，VMID 500-599 untouched，K8s Lab 5 CLEAN，LLM=0；基础设施说明：VM 401 不存在（Linux pilot 不依赖 VM，workspace 在宿主机本地）；结论：LINUX_TRUSTED_READER_PILOT_PASSED；artifacts: LINUX_TRUSTED_READER_PILOT_RESULT_v0.1.md
- ops(labgen): Linux Pilot Owner Rehearsal PASSED — G-50 验收完成；session ae3d8ed4 LAB_CLOSED cleanup_verified=True 4步全绿；post-run audit：active=0，tainted={}，K8s Lab 5 published，VMID 500-599 clean，health OK，error logs empty；lab_drafts.json printf→echo 修复（data文件 gitignored，已直接生效）；结论：LINUX_PILOT_OWNER_REHEARSAL_PASSED；等待用户明确 YES + reader identity + time window 后启动 Linux Trusted Reader Pilot Execution
- fix(labgen): Linux learner terminal `credential_error` — G-50 BLOCKER 修复；根因：`lab_kubectl_ws.py` 对所有 session 调用 K8s kubeconfig 路径（`ensure_learner_credentials`），Linux session（`vm_id='linux-sandbox'`, `namespace=None`）必然失败；修复：Layer 5 后检测 `LINUX_LEARNER_VM_SENTINEL`，Linux session 路由到 `_handle_linux_terminal()`（使用 `LinuxRuntimeAdapter.execute_command()` + 本地 `>` output redirection 支持）；`main.py` 传入 `get_linux_runtime_adapter()` 到 WS handler；`_parse_output_redirect()` 工具函数；`main.py` WS route 更新；test_steps_v0.1.md `printf` → `echo`（`\` 为 shell metacharacter，`echo` 在 `ALLOWED_COMMANDS` 中且输出等价）；24 个新测试（test_labgen_linux_terminal_ws.py）；4335 tests，92.26% coverage；service restarted
- ops(labgen): Linux Pilot Owner Rehearsal Prep v0.1 — G-50；VM 401 STOPPED→running（qm start 401；K3s Ready k8s-template v1.34.4+k3s1）；VM tracker pilot_reader→lnx-rehearsal-01；owner account lnx-rehearsal-01 created（16 chars，within max_length=20）；service restarted（G-48 code gap: experiment_background+check_count now active in learner API）；negative checks 8/8 PASS（sudo/su/bash-c/env-sudo/forbidden-path/path-traversal policy_rejected，mkdir+chmod allowed）；K8s Lab 5 CLEAN；catalog=6；active sessions=0；tainted VMs=0；LLM=0；VMID 500-599 untouched；artifacts: LINUX_OWNER_REHEARSAL_TEST_STEPS_v0.1.md（complete A–K test manual）+ LINUX_PILOT_OWNER_REHEARSAL_RESULT_v0.1.md；updated: APPROVAL_GATE（VM 401 ops notes resolved）+ PLANNING_RESULT（ops notes updated）+ staging_ops_ticket_status.md；final decision=LINUX_PILOT_OWNER_REHEARSAL_READY_FOR_USER；real pilot NOT started；owner rehearsal account ready: lnx-rehearsal-01 / LinuxRehearsal@2026；next: user executes owner rehearsal following LINUX_OWNER_REHEARSAL_TEST_STEPS_v0.1.md
- docs(labgen): Linux Trusted Reader Pilot Planning & Approval Gate v0.1 — G-49；Pre-planning health check PASS with ops notes（VM 401 stopped/tracker reassignment needed，非代码阻断）；产出 LINUX_TRUSTED_READER_PILOT_PLANNING_READY_WITH_NOTES；三份 Planning 文档已创建：PILOT_PLAN（scope/reader criteria/account plan/test flow/success-failure criteria/10-question feedback form/rollback plan/pre-pilot ops checklist）+ APPROVAL_GATE（15-item checklist，10 PASS + 5 PENDING_USER_APPROVAL）+ PLANNING_RESULT（executive summary/north star/health check results/known limitations/self-audit）；BLOCKER=0，HIGH=0，MEDIUM=0，LOW-002(article.html no embedded CTA, accepted, deep link sufficient)；Pilot NOT started；approval_status=PENDING_USER_APPROVAL；user must specify: reader identity + time window + explicit approval；ops pre-actions required: qm start 401 + create linux-trusted-reader-01 account + reassign VM 401 tracker；LLM calls=0；production VMID 500-599 untouched；catalog=6 unchanged；K8s regression=none；no code changes；4311 tests baseline unchanged；recommended next step: Linux Trusted Reader Pilot Execution (G-49) after user approval
- feat(labgen): Linux Content Quality Iteration v0.1 — G-48；修复 MEDIUM-001/002 和 LOW-001/003：(1) `Step` 新增 `troubleshoot: Optional[str] = ""` 字段；`LabDraft` 新增 `experiment_background: Optional[str] = ""`、`completion_summary: Optional[str] = ""` 字段（全部 Optional，默认空字符串，向后兼容现有 K8s lab）；(2) `LearnerLabDetail` 新增 `experiment_background`/`completion_summary` 字段并在 `get_published_lab_detail()` 中暴露（空字符串转为 None，sanitize_text 清洗）；(3) `LearnerSessionStepStatus` 新增 `step_troubleshoot: Optional[str] = None`，仅对当前步骤（is_current=True）填充；(4) `check_count` bug 修复：`_build_step_preview()` 改为 `len(step.verify) + len(step.linux_verify)`，Linux domain lab check_count 从 0 修正为实际验证器数量（lfp-step-1: 3, lfp-step-2/3: 1, lfp-step-4: 0）；(5) 内容填充：Linux lab(6c439064) 所有字段填入真实内容（experiment_background/completion_summary/4 步骤 troubleshoot），无任何 [TODO] 占位符；(6) 前端显示：`labgenViews.js` `renderLabDetail` 新增 Background 蓝色区域卡片，session step 渲染新增"Need help?" collapsible troubleshoot hint；25 新测试（test_labgen_linux_content_quality.py，A-F 类：model schema/catalog API/check_count/session snapshot/backward compat/content fill）；4311 tests, 92.49% coverage；K8s 路径零回归；VMID 500-599 未触碰；LLM 调用 0；catalog=6 不变；NOT Trusted Reader Pilot；推荐下一步：Linux Trusted Reader Pilot
- docs(labgen): Linux Reader-facing CTA Dry Run v0.1 — LINUX_READER_FACING_CTA_DRY_RUN_READY_WITH_NOTES；完整 reader-facing 路径验证通过：mock CTA(Linux 文件与权限基础) → deep link(/labgen-lab.html?labId=6c439064) → lab detail landing(title/summary/4 objectives/4 steps_preview，no source_article_id，no raw text) → Start Lab(LAB_ACTIVE, vm_id=linux-sandbox, namespace=null, kubeconfig=null) → step 1(linux_directory_exists+linux_file_exists+linux_file_content_matches PASS) → step 2(linux_file_content_matches PASS) → step 3(linux_file_mode_matches 600 PASS) → step 4(ready_to_complete=True PASS) → Complete(LAB_CLOSED, cleanup_verified=True, residual=0)；learner account: linux-cta-dry-run(operator-controlled); session fc45a40e；negative checks 11/11 PASS(sudo/su/bash-c/env sudo/etc-access/path-traversal/path-escape policy_rejected, internal endpoint 403, non-allowlisted 422, K8s Lab 5 unaffected, catalog isolation clean)；AI tutor context present with safety reminders, LLM disabled note, 0 LLM calls；post-run audit: active=0, residual=0, taint=clean, catalog=6, health OK, error logs empty；MEDIUM-001(experiment_background empty), MEDIUM-002(troubleshoot empty), LOW-001(completion_summary empty), LOW-002(no embedded article CTA component in article.html)；NOT Trusted Reader Pilot; NOT public launch; NOT live LLM; VMID 500-599 untouched; K8s Lab 5 zero regression；推荐下一步：Linux Trusted Reader Pilot Planning
- feat(labgen): Linux Learner Runtime E2E Smoke Acceptance v0.1 — LINUX_LEARNER_RUNTIME_E2E_SMOKE_PASSED_WITH_NOTES；real learner smoke session `0a38c7c5` (linux-smoke-learner)：Start LAB_ACTIVE(vm_id=linux-sandbox, namespace=None, kubeconfig=None) → lfp-step-1 all_passed=True(linux_directory_exists+linux_file_exists+linux_file_content_matches) → lfp-step-2 PASS → lfp-step-3 PASS → lfp-step-4 PASS(ready_to_complete=True) → Complete(LAB_CLOSED,cleanup_verified=True,residual=0)；abort path session `4db7365f`：LAB_CLOSED,cleanup_verified=True；BLOCKER修复：lfp-step-4 模板错误地包含 `linux_no_residual_files` verifier（在 complete() 之前要求 workspace 为空，与 step 4 UX"无需手动清理"自相矛盾，永久阻断 complete 路径）→ 从 step 4 linux_verify 移除该 verifier（cleanup 已由 complete() 后 cleanup_verified=True 验证）；BLOCKER 完全解除；negative checks 全通(sudo/su/path-traversal/forbidden-path policy_rejected，allow_root=True model validation rejected，K8s Lab 5 catalog 未回归)；post-run audit：active sessions=0, workspace residual=0, taint=none, catalog=6, service healthy, error logs empty, VMID 500-599 untouched, LLM calls=0；.env 新增 LABGEN_LINUX_LEARNER_ENABLED_LAB_IDS=6c439064-…；4286 tests, 92.48% coverage；not Trusted Reader Pilot；not public launch；not live LLM
- feat(labgen): Linux Learner Runtime Enablement Gate v0.1 — G-45；feature flag `LABGEN_LINUX_LEARNER_ENABLED_LAB_IDS`（逗号分隔 UUID 白名单，env var，空=禁用）+ `LABGEN_LINUX_SANDBOX_ROOT` 配置；`LabSessionService` Linux learner 路径：`run_precheck()` 白名单校验（非白名单 → `PRECHECK_LINUX_LEARNER_NOT_SUPPORTED`，跳过 VM tracker 检查）、`create_session()` dispatch `_do_create_linux_session()`（vm_id=`linux-sandbox` sentinel，no K8s namespace，no kubeconfig，workspace 由 `LinuxRuntimeAdapter.create_session()` 创建）、`_do_cleanup_linux()`（`close_session()` + `cleanup_verified=True`，不调用 `mark_vm_tainted`）、`_do_cleanup()` Linux 路径 dispatch；`routes.py` module-level singleton `_linux_runtime_adapter`（懒初始化，env 非空时激活）、`get_step_progression_service()` 注入 `LinuxVerifierService`；`FailureReason` 新增 `LINUX_LEARNER_WORKSPACE_CREATE_FAILED` + `LINUX_LEARNER_CLEANUP_FAILED`；完整 learner smoke 链路验证：Start(LAB_ACTIVE) → mkdir(PASS) → write+chmod(PASS) → file_mode(PASS,ready_to_complete=True) → Complete(LAB_CLOSED,cleanup_verified=True)；abort path：LAB_CLOSED,cleanup_verified=True；K8s Lab 5 路径零回归；new 53 tests（test_labgen_linux_learner_enablement.py：A-allowlist/B-create/C-step-check/D-complete/E-abort/F-negative/G-catalog-isolation/H-k8s-regression/I-smoke-e2e/J-config/K-failure-reason）；4285 tests，92.47% coverage；VMID 500-599 未触碰；LLM 调用 0；catalog entries: 5 K8s+1 Linux published（6 total）；strict prohibitions enforced: no Trusted Reader Pilot / no multi-learner / no concurrency raise / no second Linux lab / no public article upload / no live LLM / no URL scraping
- feat(labgen): Linux Article-linked Lab Publish Gate v0.1 — G-44；解除 `StaticValidator._check_linux_publish_blocked_until_runtime()` 调用（G-37~G-43 全部完成，预设解封条件满足）；新增 `FailureReason.PRECHECK_LINUX_LEARNER_NOT_SUPPORTED`；`run_precheck()` 对 Linux domain 提前返回 `precheck.linux_learner_not_yet_available`（不暴露 `cleanup_not_declared` 误导信息）；`publish_candidate_dry_run_service.py` validation gate 更新为"zero PUBLISH_BLOCKING failures"语义（gate 解除后 boundary 不再存在）；删除死方法 `_check_linux_publish_blocked_until_runtime()`；30 个新测试（A-F 类：StaticValidator gate lift / PublishService / Precheck guard / Catalog / Negative / K8s regression）；15 个旧断言更新（"始终阻断" → "gate 已解除"）；safety-reviewer HIGH（false positive：publish+not-startable 是设计意图）+ MEDIUM-1/2 已修复；Codex P1（同一设计问题，已文档化为 WITH_NOTES）；BLOCKER 0；4228 tests，92.63% coverage
- feat(labgen): Linux Publish Candidate Dry Run v0.1 — G-43；新增 `PublishCandidateDryRunService`（6 gate 只读验证：admin_gate/validation_gate/internal_rehearsal_gate/runtime_verifier_cleanup_gate/content_quality_gate/safety_gate）；新增 `POST /api/labgen/drafts/{id}/publish-candidate-dry-run`（admin-only，永不修改 publish_status 或 catalog）；54 新测试（A-J 类：所有 gate 正向/负向/HTTP endpoint/catalog isolation/K8s 回归）；`actual_publish_performed=False`/`learner_catalog_changed=False`/`dry_run=True` 三个不变量强制保持；Linux catalog entries: 0（未变）；K8s Lab 5 未回归；VMID 500-599 未触碰；LLM 调用 0；4194 tests，92.64% coverage
- feat(labgen): Linux E2E Internal Rehearsal Acceptance v0.1 — LINUX_E2E_INTERNAL_REHEARSAL_ACCEPTANCE_PASSED_WITH_NOTES；在真实服务上执行完整 Linux internal rehearsal chain（/internal/linux-rehearsal-sessions）；E2E 链路验证：draft_id → session_id(LAB_ACTIVE) → step-1(3 verifiers PASS) → step-2(1 verifier PASS) → step-3(1 verifier PASS, ready_to_complete=True) → complete → LAB_CLOSED, cleanup_verified=True, rehearsal_completed=True；abort path: LAB_CLOSED, cleanup_verified=True, rehearsal_completed not set；负向检查全通：无 session cookie→401、无 admin token→401、错误 token→401、learner→403；unsafe 命令全拒绝：sudo/su/systemctl/curl/wget policy_rejected；MEDIUM 修复：`_safe_exec_command` 对绝对路径拦截失败（WorkspacePathEscapeError）正确分类为 policy_rejected=True（printf/echo/mkdir/chmod 四个拦截块全部修复）；Linux publish 仍 blocked；catalog entries: 5 K8s labs, 0 Linux；K8s Lab 5 未回归；VMID 500-599 未触碰；LLM 调用 0；4140 tests，92.52% coverage；不是 Trusted Reader Pilot，不是 publish，不是 learner-visible
- feat(labgen): Linux Internal Rehearsal Bridge v0.1 — LINUX_INTERNAL_REHEARSAL_BRIDGE_READY；新增 `backend/labgen/linux_rehearsal_service.py`（`LinuxRehearsalService`，9 点 precheck，`_safe_exec_command` shell-redirect 拦截，`execute_linux_step`，`complete_linux_rehearsal`，`abort_linux_rehearsal`）；`FailureReason` 新增 18 个 `linux_rehearsal.*` 错误码；`LinuxRuntimeAdapter` 新增 `workspace_manager` property；`routes.py` 新增 `linux_rehearsal_router`（5 端点，dual-auth X-Admin-Token + admin cookie）；`main.py` 注册 router；新增 `tests/test_labgen_linux_internal_rehearsal_bridge.py`（68 tests，A-F 类全覆盖）；printf/echo/mkdir/chmod shell-redirect 全部拦截为 Python-native API（无 shell=True）；sudo/su/systemctl/curl/wget/pipe/semicolon/backtick/$VAR 全部 policy_rejected；Linux publish 仍 blocked by StaticValidator；Linux draft 不出现在 learner catalog；K8s 路径零回归；LLM 调用 0；生产 VMID 500-599 未触碰；4134 tests，92.64% coverage；推荐下一步：Linux Trusted Reader Pilot（Task 7 of 7）
- feat(labgen): Linux Guided Practice Draft Template v0.1 — LINUX_GUIDED_PRACTICE_DRAFT_TEMPLATE_READY_WITH_NOTES；新增 `backend/labgen/linux_template.py`（`LinuxFilesPermissionsTemplate`，4 个引导步骤，全部 5 个 Linux verifier primitives，`LinuxSandboxPolicy`，`CleanupLinuxWorkspace`，`ai_tutor_context`，无 LLM，无占位符）；`LabDraft` 新增 `ai_tutor_context: Optional[str] = None` 字段（K8s 路径向后兼容，默认 None）；`LabDraftGeneratorStub` 新增 `generate_linux()` 委托方法；`StubFeasibilityClassifier` 新增 4 组 Linux 信号 pattern（safe commands / unsafe commands / /etc write vectors / network required），MEDIUM 修复：/etc 写入向量覆盖 cp/mv/tee/sed -i/truncate/install；`StaticValidator._validate_linux()` 新增 `_check_linux_verifiers_present()` 检查；新增 `tests/test_labgen_linux_guided_practice_draft_template.py`（68 tests，5 类：模板生成/可行性分类/StaticValidator/Catalog隔离/K8s回归）；TOCTOU LOW 限制继承自 G-39，本任务未扩大攻击面；publish 始终被 `linux.publish_blocked_until_runtime` 阻断；K8s 路径零回归；LLM 调用 0；生产 VMID 500-599 未触碰；4065 tests，93.27% coverage；推荐下一步：Linux Runtime Adapter 实现或 Linux Internal Rehearsal Bridge
- feat(labgen): Linux Verifier Adapter Hardening v0.1 — LINUX_VERIFIER_ADAPTER_HARDENED_WITH_NOTES；关闭 TOCTOU MEDIUM（Spike 遗留）：三层防护（lstat() + _recheck_containment() + O_NOFOLLOW）；Codex P1 修复：lstat() 改为对原始未 resolve 路径执行（os.path.join(ws, rel_path)）而非 realpath 结果，彻底关闭 inside-workspace symlink 绕过（all symlinks reject fail-closed）；safety-reviewer MEDIUM 修复：list_files_recursive 与 no_residual_files 对齐（symlink-to-dir 计入结果）；dead code 清除；permission_error test 改为 monkeypatch mock（root 环境兼容）；MEDIUM 降为 LOW（中间路径组件 TOCTOU 无并发 learner 进程）；64 个新测试；K8s 路径零回归；Linux publish 仍 blocked；LLM 调用 0；生产 VMID 500-599 未触碰
- feat(labgen): Linux Verifier Adapter Spike v0.1 — LINUX_VERIFIER_ADAPTER_SPIKE_READY_WITH_NOTES；新增 `LinuxVerifyClientAdapter`（5 个 filesystem primitive：file_exists/directory_exists/file_content_matches/file_mode_matches/no_residual_files；无 shell 无 subprocess，全走 Python stdlib os/stat/open）；新增 `LinuxVerifierService`（workspace-scoped orchestration，session 查找 → path 预验证 → primitive dispatch → VerifyResult；content mismatch 强制 redact，host 绝对路径不泄漏）；`StepProgressionService` 新增 `linux_verifier_svc` 可选参数，LINUX domain draft → Linux verifier dispatch，K8s 路径零改动；`FailureReason` 新增 11 个 Linux 错误码（linux.workspace_not_found/not_active/path_escape/file_not_found/directory_not_found/content_mismatch/file_too_large/mode_mismatch/residual_files_found/verify_type_not_supported/verifier_not_configured）；路径安全：reject absolute / .. traversal / sibling prefix / symlink escape / forbidden host paths；48 新测试（test_labgen_linux_verifier_adapter_spike.py）；全量 3933 tests，93.38% coverage；K8s 路径零回归；Linux publish 仍 blocked；learner 不可见；LLM 调用 0；生产 VMID 500-599 未触碰；推荐下一步：Linux Guided Practice Draft Template 或 Linux Verifier Adapter Hardening
- feat(labgen): Linux Runtime Adapter Spike v0.1 — LINUX_RUNTIME_ADAPTER_SPIKE_READY_WITH_NOTES；新增 `LinuxWorkspaceManager`（per-session 目录隔离，`_ALLOWED_SANDBOX_ROOTS` 白名单，`os.sep` 前缀防护）/ `LinuxCommandExecutor`（allowlist 19 命令，DENIED_COMMANDS 40+ 命令，shell=False，无 metachar，路径参数验证）/ `LinuxCleanupAdapter`（shutil.rmtree + residual scan + taint-on-failure）/ `LinuxRuntimeAdapter`（enabled=False 默认关闭，feature flag）；新增 `NamespaceAdapterKind.LINUX`（selection service，production 模式下 blocking + build_adapter 硬 RuntimeError）；新增 `LinuxContainerLifecycleAdapter` 骨架（6 方法全 NotImplementedError）；`LinuxSandboxPolicy.allow_root/allow_network` 升级为 Pydantic field_validator 强制 False（model 层防护 + StaticValidator 纵深兜底测试 via model_construct）；修复 M-01 sibling session prefix confusion（trailing os.sep）、L-01 deep forbidden path、L-02 production adapter silent failure、L-03 backstop untested；88 新测试（test_labgen_linux_runtime_adapter_spike.py）；4 已有测试更新（model-layer enforcement）；全量 3885 tests，93.38% coverage；K8s 路径零回归；0 learner 暴露；生产 VMID 500-599 未触碰；Linux publish 仍 blocked；推荐下一步：Linux Verifier Adapter Spike（Task 3 of 7）
- feat(labgen): Linux Domain Contract Schema Extension v0.1 — LINUX_DOMAIN_CONTRACT_SCHEMA_READY_WITH_NOTES；新增 `LabDomainType` enum / `LinuxVerifyType` enum（5 primitives）/ `LinuxVerifyTemplate` model（workspace_relative_only 不变量）/ `LinuxSandboxPolicy` model（allow_root=False/allow_network=False 不变量）/ `CleanupLinuxWorkspace` model（taint_on_cleanup_failure 不变量）；更新 `LabDraft`（target_domain/linux_sandbox_policy/linux_cleanup）/ `Step`（linux_verify）；`StaticValidator` 分 K8s/Linux 两路（K8s 路径不变；Linux 路径 8 项检查 + publish 始终 blocked）；修复 H-01（`_pick_target_domain` 支持 LINUX）；修复 H-02（`StubFeasibilityClassifier` 新增 `_LINUX_SIGNALS` 7 条 pattern）；60 个新测试（test_labgen_linux_domain_schema.py）；Linux runtime 未实现；K8s 回归全通；3797 tests，93.40% coverage；0 LLM 调用；生产 VMID 500-599 未触碰
- docs(labgen): Linux Domain Proof Design Gate v0.1 — LINUX_DOMAIN_PROOF_DESIGN_READY_WITH_NOTES；K8s Trusted Reader Pilot（session a301676a）正式验收（LAB_CLOSED, cleanup_verified=True, step_1+step_2 PASS, observer 全程无卡顿）；输出 `LINUX_DOMAIN_PROOF_DESIGN_GATE_v0.1.md`（14 节：North Star 对齐 / K8s milestone closure / 可复用模块清单 / K8s-only coupling 清单 5-BLOCKER+4-HIGH+5-MEDIUM / Linux domain contract proposal / runtime strategy 3 options + 推荐 container-based sandbox / 首个 Linux lab 候选（Linux Files and Permissions Basics）/ verifier primitives 5 个 / cleanup strategy / feasibility gate / 7 步实现计划 / risks / 最终决策 / 推荐下一步）；更新 `PROJECT_NORTH_STAR_v0.1.md` 进度表（K8s Trusted Reader Pilot COMPLETE, Linux Design Gate COMPLETE, Linux Schema Extension NEXT）；更新 `staging_ops_ticket_status.md`（G-34 + G-35）；0 代码改动；0 LLM 调用；生产 VMID 500-599 未触碰；推荐下一步：Linux Domain Contract Schema Extension（Task 1 of 7）
- fix: `/app` 删除 VM 时未检查 LabGen 活跃 session，可能中途销毁正在使用中的 VM — 在 DELETE /api/vms/{id} 执行前调用 `LabSessionRepository.has_active_session_for_vm()`，有活跃 session 则返回 409 阻止删除；新增 `has_active_session_for_vm()` 方法及 9 个回归测试
- fix: 登录后忽略 `?next=` 参数，始终跳转到 `/app`，导致从 LabGen 实验链接跳登录后回到旧系统首页 — 已读取 `?next=` 参数并在登录成功后重定向到目标页面，兜底仍为 `/app`
- feat(labgen): Guided Practice Quality Iteration for Lab 5 v0.1 — GUIDED_PRACTICE_QUALITY_READY_FOR_TRUSTED_READER；Lab 5（cf019133）全部 [TODO] 占位替换为真实教学内容（title/description/step_1+2 why/observe/explain.concept/explain.observation/verify.notes/manual_review_required）；新增 `content.no_placeholders` StaticValidator 检查（publish_blocking 级别，防止未来 stub 内容误发布）；Reader path 重新验证通过（session 03e6a04b，step_1 FAIL→PASS，step_2 PASS，LAB_CLOSED cleanup_verified=True residual=0）；Catalog 仍为 5 个 lab，无 TODO；source_article_id 不暴露；27 个新测试（placeholder gate × 12 + admin PATCH integrity × 7 + reader regression × 8）；3728 tests，93.28% coverage；0 LabGen LLM 调用；生产 VMID 500-599 未触碰；推荐下一步：Article-linked Lab Pilot With Trusted Reader
- feat(labgen): Reader-facing Article CTA Dry Run v0.1 — READER_FACING_ARTICLE_CTA_DRY_RUN_READY_WITH_NOTES；完整 reader path 验证通过（CTA→catalog→Start Lab→step_1 configmap_exists PASS→step_2 namespace_exists PASS→Complete LAB_CLOSED cleanup_verified=True→residual=0）；Learner session（k8s_test，VM 401）使用已发布 article-linked lab（cf019133）；新增 3 个测试（test_published_article_linked_lab_allows_learner_precheck / test_published_article_lab_step_preview_has_do_instruction / test_published_article_lab_step_check_count_matches_verify_list）；3571 tests，93.26% coverage；0 LabGen LLM 调用（AI tutor 为独立 DeepSeek 已有功能）；生产 VMID 500-599 未触碰；Preflight 结论：kubeconfig IP 一致（172.16.100.153），无 K3s namespace 残留，无 tainted VM；NOTES：stub pipeline 生成 [TODO] 占位内容（MEDIUM），VM ownership 需 manual staging 配置（NOTE），step_2 verify manual_review_required=true（LOW）；推荐下一步：Guided Practice Quality Iteration
- feat(labgen): Admin-curated Article-linked Lab Publish Gate v0.1 — PUBLISH_GATE_READY；完整 article→rehearsal→publish pipeline 在 staging 执行通过（article_draft aa7c4a99 → LabDraft cf019133 → rehearsal session 0b0fb49b → rehearsal_completed=True → published → 5 labs in catalog）；新增 `rehearsal_required`/`rehearsal_completed` 字段到 `LabDraft`；`PublishDecisionService` rehearsal gate；`PublishService.publish()` 防御性 `RehearsalNotCompleted` 异常；`StepProgressionService.check_step()` 允许 INTERNAL_REHEARSAL session 检查 DRAFT 状态 lab；`_do_cleanup()` 仅在 `ready_to_complete=True` 的 INTERNAL_REHEARSAL session 后设置 `rehearsal_completed=True`；platform kubeconfig server 地址修正（stale IP in staging env files）；30 tests（test_labgen_article_publish_gate.py，含 2 个新增回归测试）；3631 tests，93.19% coverage；0 LLM 调用；生产 VMID 500-599 未触碰
- fix: test_adapter_status_production_safe_false_in_test_mode 在 host 环境有生产 LABGEN_RUNTIME_MODE 时错误失败 — 根因：config 模块 import 时固化值，monkeypatch.delenv 无效；改为 monkeypatch.setattr 直接 patch config 属性 — 补回归测试防止重现
- feat(labgen): Internal Rehearsal Bridge v0.1 — 新增 `POST /internal/rehearsal-sessions`（X-Admin-Token + require_admin_user 双重认证），允许 admin 对 DRAFT 状态的 article 生成 LabDraft 发起内部预演 session；session 标记 `session_type=INTERNAL_REHEARSAL`，不进入 learner catalog，不改变 publish_status；safety-reviewer 发现 2 HIGH + 2 MEDIUM 全部修复（learner GET/complete/abort 端点对 rehearsal session 返回 404 隐藏存在、`list_my_sessions` 过滤、catalog active-session-check 过滤、`run_precheck` rehearsal 污染修复、`build_snapshot` rehearsal 隔离）；7 个 failure code 新增；45 个新测试（含 5 个安全回归测试）；3668 tests，93.27% coverage；0 LLM 调用；生产 VMID 500-599 未触碰；新建 `docs/labgen/ARTICLE_TO_LAB_INTERNAL_REHEARSAL_BRIDGE_RESULT_v0.1.md`
- docs(labgen): K8s Article-to-Lab Internal Rehearsal to Publish Candidate v0.1 — PUBLISH_CANDIDATE_BLOCKED_BY_MISSING_REHEARSAL_BRIDGE；完整 pipeline（Article→Contract→AdminReview→Validator→LabDraft DRAFT）结构全通过；BLOCKER-001 发现：`LabSessionService.run_precheck()` 第 259 行要求 `publish_status=PUBLISHED`，而 `convert_to_lab_draft()` 产出始终为 DRAFT，发布到 learner catalog 被硬性禁止，因此无法在不破坏安全不变量的前提下创建真实 LabSession；lab_id `3d9e3331-...` 隔离于 /tmp，不在生产 `data/lab_drafts.json`；0 LLM 调用（evaluated_by=STUB）；0 代码改动；runtime/verifier/terminal 行为不变；生产 VMID 500-599 未触碰；5th lab 未发布；新建 `docs/labgen/K8S_ARTICLE_TO_LAB_INTERNAL_REHEARSAL_TO_PUBLISH_CANDIDATE_RESULT_v0.1.md`；更新 `deploy/labgen/staging_ops_ticket_status.md`（新增 G-29）；更新 `deploy/labgen/staging_infrastructure_checklist.md`（新增 G-29）；主口号从"读了能做，做了就懂"更正为"读了能练，练完即熟"（跨全部 labgen 文档）；推荐下一步：Internal Rehearsal Bridge Implementation
- docs(labgen): Project North Star Re-Alignment After Claude Reset v0.1 — NORTH_STAR_REALIGNED_WITH_NOTES；更新 `docs/labgen/PROJECT_NORTH_STAR_v0.1.md`（主口号改为"读了能做，做了就懂"；新增第 3 节 Admin-curated Article-to-Lab 当前阶段产品策略：admin 提供技术文章，reader 从 CTA 进入对应实验；新增第 4 节 Guided Practice Lab 定义——v0.1 产出 Guided Practice Lab 非 Assessment Lab，含左侧文档/步骤/可复制命令/预期输出/AI 助手、右侧 terminal 布局说明及设计原则；更新第 5 节 Canonical Flow 补充 Article Operability / Lab Feasibility Gate 和 Reader enters from article CTA 两个节点；新增第 12 节 AI Tutor Context（AI 助手可做/不可做清单）；更新第 13 节产品阶段（当前处于 Article-to-Lab pipeline build phase，real-human validation 已完成）；更新第 14 节 Decision Rule 补充 Guided Practice Lab / Admin Review bypass / Feasibility Gate bypass 三项检查；新增第 15 节 Article-to-Lab Follow-up Execution Checklist（18 项强制检查清单，覆盖产品对齐 / gate 保留 / 边界保留三维）；更新第 16 节里程碑表（K8s Article-to-Lab Draft Mode Implementation 和 Admin Review Rehearsal 标记 COMPLETE，Internal Rehearsal 标记 NEXT）；更新 `deploy/labgen/staging_ops_ticket_status.md`（新增 G-26 Draft Mode / G-27 Admin Rehearsal / G-28 North Star Re-Alignment 三行 gate 记录）；更新 `deploy/labgen/staging_infrastructure_checklist.md`（新增 G-25/G-26/G-27/G-28 四行；North Star Alignment Check 扩展为 18 项）；0 代码改动；0 LLM 调用；runtime/verifier/terminal 行为不变；生产 VMID 500-599 未触碰
- feat(labgen): K8s Article-to-Lab Admin Review Rehearsal v0.1 — K8S_ARTICLE_TO_LAB_ADMIN_REHEARSAL_PASSED_WITH_NOTES；4 input samples（directly_lab_ready / partially_lab_ready / not_lab_ready / sensitive content）全部验证正确；MEDIUM-001 修复：`PatchArticleDraftRequest` 新增 `source_grounding` / `required_runtime` / `verifier_candidates` 三字段（原缺失导致完整 pipeline 被阻断）；PATCH endpoint 改用 `model_fields_set + getattr` 保留 Pydantic model instance（避免 model_dump 将嵌套模型展平为 dict 导致 setattr 类型不匹配）；`_ALLOWED_UPDATE_KEYS` 新增 `source_grounding`；safety-reviewer B 类审查 PASS；新增 `tests/test_labgen_admin_rehearsal.py`（53 tests）；LOW-001 documented：not_lab_ready article APPROVE from DRAFT succeeds（validator check fires at APPROVED_FOR_STATIC_VALIDATION, not at APPROVE step），但 advance to internal rehearsal 被 409 阻断，发布路径有效封堵；NOTE-001 documented：user_confirmed_right_to_use / user_confirmed_no_secrets 默认 False，admin 必须显式传 True；3623 passed, 93.59% coverage；0 LLM 调用；生产 VMID 500-599 未触碰
- feat(labgen): Article-to-Lab Draft Mode Implementation v0.1 — admin-only stub/manual draft pipeline；新增 `backend/labgen/article_draft_repository.py`（ArticleDraftLabContract flock JSON 持久化）；新增 `backend/labgen/stub_feasibility_classifier.py`（确定性 stub 分类器，无 LLM，含 sensitive content 硬拒 / cloud domain 阻断 / DIRECTLY_LAB_READY / PARTIALLY_LAB_READY / NOT_LAB_READY 三档）；新增 `backend/labgen/article_draft_service.py`（服务层：create_draft / record_admin_decision / advance 状态机 / validate_draft / convert_to_lab_draft / delete_draft；ArticleDraftValidator guardrail 集成；状态转换 DRAFT→APPROVED_FOR_STATIC_VALIDATION→APPROVED_FOR_INTERNAL_REHEARSAL→APPROVED_FOR_PUBLISH_CANDIDATE；convert 产出 PublishStatus.DRAFT，不自动发布）；新增 `backend/labgen/article_draft_routes.py`（9 个 admin-only 端点：POST/GET/PATCH/review/validate/advance/convert/DELETE）；注册 article_draft_router 到 main.py；新增 `tests/test_labgen_article_draft_api.py`（60 tests）；原始文章文本不可持久化（仅内存处理，content_hash 存储）；sensitive content 检测（API key/token/password/private key 立即拒绝）；所有生成草稿均为 DRAFT 状态，不进入 learner catalog；0 LLM 调用；runtime/verifier/terminal 行为不变；生产 VMID 500-599 未触碰
- feat(labgen): Article-to-Lab MVP Contract Schema Gate v0.1 — ARTICLE_TO_LAB_SCHEMA_READY_WITH_NOTES；新增 `backend/labgen/article_models.py`（11 个 Pydantic model：ArticleSourceMetadata、FeasibilityResult、SafetyFlag enum×13、SourceGroundingSnippet、UnsupportedInference、TargetDomain enum×8、ArticleLabRuntimeRequirement、VerifierCandidate、ArticleDraftLabContract、AdminDecision、ArticleStoragePolicy）；新增 `ArticleDraftValidator`（`backend/labgen/static_validator.py`，15 个 guardrail check：rejected_feasibility_cannot_publish / partial_feasibility_cannot_publish / directly_ready_requires_admin_review / missing_source_grounding / unsupported_inference_high_blocker / no_raw_text_field / no_sensitive_grounding_excerpt / admin_approve_requires_confirmations / unknown_domain_cannot_publish / unsafe_verifier_cannot_publish / cleanup_strategy_required / llm_draft_cannot_bypass_review / cloud_domain_blocked_v1 / source_url_no_scraping / user_confirmations_required）；新增 `tests/test_labgen_article_schema.py`（116 tests）；schema 设计：原始文章文本不可持久化（model_validator 强制）；raw_text_persisted 默认 False；ArticleDraftLabContract 不等于 LabDraft（不同流水线阶段）；cloud domain 默认被 guardrail 阻断；linux/docker/networking/database/cicd schema-ready 但无实现；可进入实现阶段；0 LLM 调用；生产 VMID 500-599 未触碰；runtime/verifier/terminal 行为不变；新建 `docs/labgen/ARTICLE_TO_LAB_MVP_CONTRACT_SCHEMA_GATE_v0.1.md`；更新 PROJECT_NORTH_STAR_v0.1.md / staging_ops_ticket_status.md / staging_infrastructure_checklist.md
- docs(labgen): Article-to-Lab Implementation Prerequisites v0.1 — ARTICLE_TO_LAB_PREREQS_READY_WITH_NOTES；解决 Design Gate 中三个 go/no-go 阻断：N-01 RESOLVED（v0.1 stub mode，无 live LLM，`LLMProviderPort` 设计为抽象接口，T430 排除自托管，商业 API 推迟至 stub 验证后，fail-closed 规则覆盖所有 LLM 失败模式）；N-02 RESOLVED（ephemeral source text 策略，丢弃原文，仅持久化 content_hash + source_metadata + feasibility_result + source_grounding_snippets + draft contract，sensitive content 立即丢弃，access boundary 明确）；N-03 RESOLVED（user consent 每次提交必填，无长期原文，rejection metadata 30 天，audit 无限期 hash+decision，admin-only v0.1 著作权风险最低）；F.Implementation Preconditions 全部 READY；G.Decision：ARTICLE_TO_LAB_PREREQS_READY_WITH_NOTES；H.推荐下一步：Article-to-Lab MVP Contract Schema Gate（schema 锁定优先于 implementation）；更新 `docs/labgen/ARTICLE_TO_LAB_PIPELINE_DESIGN_GATE_v0.1.md`（N-01/N-02/N-03 标记 RESOLVED，M.3 go/no-go 表更新）；更新 `docs/labgen/PROJECT_NORTH_STAR_v0.1.md`（第 12 节新增 Prerequisites Gate 条目）；更新 `deploy/labgen/staging_ops_ticket_status.md`（新增 G-24）；更新 `deploy/labgen/staging_infrastructure_checklist.md`（新增 G-24）；新建 `docs/labgen/ARTICLE_TO_LAB_IMPLEMENTATION_PREREQUISITES_v0.1.md`；0 代码改动；0 LLM 调用；生产 VMID 500-599 未触碰
- docs(labgen): Article-to-Lab Pipeline Design Gate v0.1 — ARTICLE_TO_LAB_PIPELINE_DESIGN_READY_WITH_NOTES；完整设计 Article-to-Lab pipeline：Feasibility Gate（三档分类 Directly/Partially/Not Lab-Ready，含拒绝响应结构和"不允许"清单）；Draft Lab Contract schema（新增 source_grounding / unsupported_inferences / admin_decision / source_metadata / feasibility_result 字段，LLM 幻觉防护）；Admin Review flow（source grounding / unsupported inference / safety flag 必须显式确认）；StaticValidator 5 个新检查（source_grounding_present / no_unreviewed_inferences / admin_decision_approved / safety_flags_acknowledged / verifier_candidates_resolved）；Internal Rehearsal gate（每个 generated lab 在任何学员看到前必须 operator 通过）；Domain Adapter 接口设计（K8s 已完整实现；Linux / Docker / Networking / Database 接口已设计，v0.1 范围外）；Verifier Strategy（5 个 candidate states：reusable_existing / needs_parameterization / needs_new_primitive / not_verifiable / unsafe_to_verify；LLM 不得直接生成 verifier）；Cleanup / Credential lifecycle（所有 domain 通用，tainted resource handling）；Safety Policy（article-level flags / runtime policy / output safety / domain-specific rules）；MVP Scope：K8s Article-to-Lab Draft Mode（only k8s domain，only existing 4 verifier primitives，no auto-publish，admin review required，internal rehearsal required）；9 个 Open Questions（N-01 LLM provider / N-02 article storage / N-03 copyright 为 go/no-go blocker）；更新 `docs/labgen/PROJECT_NORTH_STAR_v0.1.md`（新增第 12 节 Design Gate Progress）；更新 `deploy/labgen/staging_ops_ticket_status.md`（新增 G-23）；更新 `deploy/labgen/staging_infrastructure_checklist.md`（新增 G-22 Small Customer Pilot BLOCKED + G-23 Pipeline Design READY）；0 代码改动；0 LLM 调用；生产 VMID 500-599 未触碰；新建 `docs/labgen/ARTICLE_TO_LAB_PIPELINE_DESIGN_GATE_v0.1.md`
- docs(labgen): Small Customer Pilot Execution v0.1 — SMALL_CUSTOMER_PILOT_BLOCKED；Pre-Pilot Gate 系统检查全部 PASS（backend healthy、VM 401 running、K3s ready、4 published labs、0 active sessions、0 lab namespaces、0 RoleBinding residuals、0 tainted VMs、0 learner credential residuals）；阻断原因：NO_SUITABLE_SMALL_CUSTOMER — 执行时点系统中 28 个注册用户均为测试/开发账号，无满足 Section 四标准的外部客户或可信小团队被识别和正式承诺；系统技术层面完全就绪，等待客户识别后即可执行；附加 NOTE：356 个 Python stub 草稿（2026-06-10 开发测试产生，全部 draft，不对用户可见）；LAB_START_FAILED 陈旧 session（k8s_test，K3s 无残留）；新建 `docs/labgen/SMALL_CUSTOMER_PILOT_RESULT_v0.1.md`；更新 `deploy/labgen/staging_ops_ticket_status.md`、`deploy/labgen/staging_infrastructure_checklist.md`；0 代码改动；0 LLM 调用；生产 VMID 500-599 未触碰
- fix: test_performance.py docs_client fixture 未 mock fetch_experiment_list，导致 /experiments 性能测试发起真实 Directus HTTP 调用（超过 200ms"纯内存端点"阈值，push 被阻断）；在 fixture 内 patch fetch_experiment_list 返回 None，使测试回归"无 I/O"预设 — 补回归测试防止重现
- docs(labgen): Small Customer Pilot Preparation Gate v0.1 — SMALL_CUSTOMER_PILOT_PREP_READY_WITH_NOTES；修复 VM ownership/learner assignment runbook 缺口（Runbook §K 新增六小节：为何 VM assignment 不可跳过、每学员启动前必做检查、ownership 转移命令、转移验证、422 no_vm_assigned 响应协议、小客户 pilot 逐会话 assignment 要求）；新建 `docs/labgen/SMALL_CUSTOMER_PILOT_PREPARATION_GATE_v0.1.md`（含 A~L 共十二节：Executive Summary、Pilot Scope、Customer Candidate Criteria、Pre-Pilot Technical Gate、Customer-Facing Onboarding Notice、Feedback Collection 12 题、Emergency Stop 18 个触发条件、North Star Alignment、Final Decision、Deliverables、Technical Self-Check、Next Step）；更新 `deploy/labgen/staging_ops_ticket_status.md`（新增 Round 2 + Prep Gate 条目）；更新 `deploy/labgen/staging_infrastructure_checklist.md`（新增 G-20 / G-21 gate 条目）；提交之前未追踪的 `docs/labgen/REAL_HUMAN_COHORT_ROUND2_RESULT_v0.1.md`；0 代码改动；0 LLM 调用；生产 VMID 500-599 未触碰
- docs(labgen): Project North Star Realignment v0.1 — 补充新增第 4 节 Article Operability / Lab Feasibility Gate；明确 Article-to-Lab 第一步必须先判断输入内容是否具备可操作性和可验证性，再决定是否生成 Draft Lab Contract；三档分类 Directly Lab-Ready / Partially Lab-Ready（返回 clarification，不允许伪造缺失内容）/ Not Lab-Ready or Reject（必须给出明确拒绝理由，禁止为了显得"智能"而硬编实验）；定位为 Article-to-Lab pipeline 的正式入口 gate，当前阶段只记录进 North Star 文档供后续设计参考，不要求现在完整实现自动判定；后续小节 5-11 顺延重新编号
- docs(labgen): Project North Star Realignment v0.1 — 新增 `docs/labgen/PROJECT_NORTH_STAR_v0.1.md`，明确 LabGen 项目结构性目标不是"固定 K8s 课程平台"，而是 Article-to-Lab / Technical Content-to-Experiment Platform（"读完即练，结果说话"）；当前 K8s 学习平台是第一个 domain proof，不是最终产品边界；架构原则 General Experiment Core + Replaceable Domain Contract，保留未来 Linux / 多领域迁移能力；更新 `docs/labgen/HOME_LAB_MVP_PILOT_GATE_RESULT_v0.1.md`、`SMALL_COHORT_FEEDBACK_TRIAGE_AND_PRODUCT_DECISION_v0.1.md`、`REAL_HUMAN_REVALIDATION_LABS_2_4_RESULT_v0.1.md`、`deploy/labgen/staging_ops_ticket_status.md`、`deploy/labgen/staging_infrastructure_checklist.md` 各追加一行 North Star 对齐引用；后两个 tracker 文档新增 North Star Alignment Check 小节；未修改任何 runtime / verifier / terminal / RBAC 行为；Real Human Cohort Round 2 当前 0 active session，未受影响
- feat(labgen): Real Human Re-validation for Labs 2–4 v0.1 — REAL_HUMAN_LABS_2_4_VALIDATED_WITH_NOTES；真实学员（learner-H1，用户本人）通过 kubectl web terminal 独立完成 Lab 2 ConfigMap Basics、Lab 3 Secret Basics、Lab 4 Deployment Basics 三个实验全流程；3 个 session 全部 LAB_CLOSED、cleanup_verified=True、0 残留；现场发现并修复 4 个真实 bug（Cloudflare CDN 缓存、verifier 凭证路径错配、verifier 配置函数用错、共享 VM 凭证 reclaim 互相冲掉，详见下方 fix 条目）；1 个 NOTE 待跟进（kubectl 报错信息不友好，未复现，不阻断）；3394 tests 通过，覆盖率 93.22%；0 次 LLM 调用；生产 VMID 500-599 未触碰；关闭此前 Real Human Learner Validation 报告中的 UX-H1（无 kubectl terminal）发现；新增 docs/labgen/REAL_HUMAN_REVALIDATION_LABS_2_4_RESULT_v0.1.md；建议下一步：Small Cohort Pilot 第二轮（真实学员覆盖全部 4 个实验）
- fix: Real Human Re-validation for Labs 2–4 v0.1 期间发现 — `LabSessionService._do_cleanup()` Phase 2 对每个 session 无条件调用 `credential_reclaimer.reclaim_for_vm(vm_id)`，对真正的 per-student ephemeral VM 模型正确，但 home_lab_mvp 共享/持久 staging VM（401）被多个学员顺序复用——Lab 2 cleanup 会把 Lab 3 需要的 verifier 凭证一并删除，导致下一个 session 第一步就 `credential_missing`；复用既有的 `VM_CLEANUP_EXEMPT_IDS` 豁免模式，给 `LabSessionService` 新增 `credential_reclaim_exempt_vm_ids` 参数，`_do_cleanup()` Phase 2 对豁免 VM 跳过 reclaim；`routes.py::get_session_service()` 接入 `config.VM_CLEANUP_EXEMPT_IDS`；`/etc/labgen/home_lab_mvp.env` 新增 `VM_CLEANUP_EXEMPT_IDS=401` — 补回归测试防止重现（tests/test_labgen_verifier_credential_reclaim.py::TestSharedVMCredentialReclaimExemption）
- fix: Real Human Re-validation for Labs 2–4 v0.1 期间发现 — Cloudflare 边缘缓存 `/js/*` `/css/*` 静态资源默认 ~4 小时（`cache-control: max-age=14400`），导致 a924a4c 修复部署到磁盘后用户长时间仍拿到旧版 JS（`cf-cache-status: HIT`）；origin 已加 `NoCacheStaticFiles`（`Cache-Control: no-cache, must-revalidate`）但 Cloudflare 似乎按自身规则覆盖该头——当前无 Cloudflare API/dashboard 权限去配置正确的缓存规则，临时方案是 script 标签加 `?v=` 版本号绕过缓存键（`labgen-session.html` 已加 `?v=2`）；后续每次 JS 改动需同步递增版本号，直到拿到 CF 控制台权限做永久修复
- fix: Real Human Re-validation for Labs 2–4 v0.1 期间发现 — labgen-session.html 的 kubectl web terminal 在真实 LAB_ACTIVE session 下不出现（之前所有内部 rehearsal/API 测试均未发现该问题）；根因是 `labgen-session-init.js::syncTerminal` 检查 `snapshot.lab_session_status` 字段，但 `/api/lab-sessions/:id/snapshot` 实际返回的 `LearnerSessionSnapshot` 模型字段名是 `session_state`，导致 isActive 永远为 false — 提取纯函数 `isSessionActive()` 到无 DOM 依赖的 `frontend/js/labgen-session-state.js`，改为读取正确字段 — 补回归测试防止重现（tests/frontend/test_session_init.mjs）
- fix: VM_CLEANUP_EXEMPT_IDS 解析未剥离行内 `#` 注释，导致 systemd EnvironmentFile 加载的值（含注释原文）被 `.isdigit()` 过滤为空集合，保护完全失效，VM 401 被 auto-cleanup 删除两次 — 提取 `_parse_exempt_vm_ids()`，剥离 `#` 后缀；`.env` 注释改为独立行；safety-reviewer 发现 MEDIUM（部分 token 解析失败时静默丢弃无告警）已修复——任意 token 不可解析即记录 WARNING 并列出被丢弃的具体值 — 补回归测试防止重现
- feat(labgen): Terminal Post-Integration Runtime & Quality Hardening Gate v0.1 — TERMINAL_RUNTIME_HARDENED_WITH_NOTES；coverage 93.21%（3381 tests）；ConfigMap lab E2E rehearsal 9/9 PASS（4 forbidden commands blocked，5 allowed commands execute）；3 bugs found+fixed（V1Subject kubernetes-v36 removal、-nkube-system bypass、staging VM auto-cleanup）；VM_CLEANUP_EXEMPT_IDS 引入保护 staging K3s VM 401；新增 result artifact docs/labgen/TERMINAL_POST_INTEGRATION_RUNTIME_HARDENING_RESULT_v0.1.md
- fix: auto_cleanup_task VM_CLEANUP_EXEMPT_IDS 保护 staging VM（401）不被 delete_vm 删除也不被 untrack — staging K3s 平台 VM 被加入生产 VMTracker 后反复被 auto-cleanup 删除 — 补回归测试防止重现
- fix(labgen): kubectl_executor -nkube-system（无空格形式）绕过 namespace override 检查 — 修正 regex 为 `\s-n\s*\S` 覆盖两种形式 — 补回归测试防止重现
- fix(labgen): learner_credentials._ensure_rolebinding 使用 RbacV1Subject（kubernetes v36 删除 V1Subject）— 补回归测试防止重现
- feat(labgen): Terminal Post-Integration Runtime & Quality Hardening Gate v0.1 — coverage 从 90.26% 提升至 93.19%；新增 test_labgen_terminal_coverage.py（51 tests）覆盖 execute() subprocess 路径、learner_credentials K8s API mock（SA/Role/RoleBinding/token/kubeconfig）、lab_kubectl_ws WebSocket 命令循环（blocked/allowed/idle/session-ended）、lab_session_repository CRUD；关键安全路径：ownership mismatch 拒绝、session non-active 拒绝、forbidden command blocked、secret -o yaml blocked、config view blocked、namespace badge 正确传递
- feat(labgen): Learner kubectl Web Terminal Integration Gate v0.1 — LEARNER_KUBECTL_TERMINAL_READY；新增 `/ws/lab-kubectl/{session_id}` WebSocket 端点（5 层鉴权：cookie → token → session lookup → ownership → LAB_ACTIVE）；新增 `backend/labgen/learner_credentials.py`（per-session SA/Role/RoleBinding + token，kubeconfig 仅存服务端，never log）；新增 `backend/labgen/kubectl_executor.py`（命令白名单 + token 列表 output-format 检查，shlex.split + asyncio subprocess，无 shell=True，64KB 输出上限，30s timeout）；`LearnerSessionSnapshot` 新增 `namespace` 字段（terminal badge 显示）；`LearnerSessionStepStatus` 新增 `step_do`/`step_commands` 字段（仅当前步骤填充，`{{lab_namespace}}` 自动替换）；新增 `frontend/js/labgen-kubectl-terminal.js`（LabKubectlTerminal 类，xterm.js v5.3.0，本地行编辑，安全提示）；更新 `frontend/labgen-session.html` 加入 terminal panel；更新 `labgen-session-init.js` 注入 terminal lifecycle；safety-reviewer 发现 2 个 BLOCKER 均已修复：(1) output format bypass（-o=yaml/-ojson/-o jsonpath/-o go-template/--output yaml 均被 token-based `_check_output_format()` 拦截）；(2) asyncio.get_event_loop() → get_running_loop()；新增 3 个测试文件（kubectl_executor / learner_credentials / lab_kubectl_ws），含所有 bypass variant 测试；3328 tests PASS；90.26% coverage
- feat(labgen): Real Human Learner Validation v0.1 — REAL_HUMAN_LEARNER_VALIDATED_WITH_NOTES；1 名真实人类学员（learner-H1，有 K8s 使用经验）独立完成 Lab 1（Kubernetes Basics）全流程：独立导航 catalog → 读 lab 描述 → Start Lab → Check Current Step（PASS）→ Complete Lab（LAB_CLOSED, cleanup_verified=True）；Lab 2（ConfigMap Basics）aborted — 学员理解步骤但无终端环境可执行 kubectl；核心 UX findings：Lab 1 步骤清晰（"类似于沙箱"，概念理解正确）、verifier PASS 反馈清晰（"隔离环境创设成功"）、cleanup 行为被学员自主注意到（"完成后实验环境销毁成功"）、整体感受"教我知识"、有终端愿意继续用；HIGH finding UX-H1：无 kubectl 终端 → Labs 2–4 对所有学员完全阻断（非概念问题，是基础设施缺口）；MEDIUM finding UX-M1：catalog 入口（/labgen-catalog.html）从 /app 不可发现；ops 发现：vm_tracker 有幻象 VM 400（不在 Proxmox），需 operator 清理并重新分配 VM 401；24/24 技术自检 PASS；0 LLM 调用，0 代码修改，0 production VMID 触碰；3216 tests PASS 93.13%（基线不变）；推荐下一步：集成 kubectl web terminal（复用现有 xterm.js + WebSocket 基础设施）→ 再次 validate Labs 2–4 → Small Customer Pilot Preparation Gate
- feat(labgen): Small Cohort Feedback Triage & Product Decision v0.1 — SMALL_COHORT_TRIAGED_NEEDS_ITERATION；对 Small Cohort Pilot v0.1 进行完整 triage（sections A-M）；核心发现：所有 9 次 session（含 3 cohort + 6 prior pilots）均为 operator-executed，无真实人类学员参与 → FEEDBACK_INSUFFICIENT_FOR_CUSTOMER_PILOT_DECISION；技术面 100% stable（6/6 LAB_CLOSED, 0 residuals, 0 BLOCKER/HIGH/MEDIUM）；lab-level findings: 4 labs 技术层面全 PASS（namespace/configmap/secret/deployment），UX/concept clarity 无法评估；ops findings: Runbook J.2-J.6 可执行，per-lab verifier re-init 已文档化，operator burden 合理；security findings: 0 BLOCKER/HIGH/MEDIUM/LOW；issue triage: BUG-001（vm_tracker datetime）已修复；FEEDBACK-001/002（无真实用户数据）open；decision matrix: Small Customer Pilot Prep / Fifth Lab Design / LLM Live Gate / Cloud Staging 全部 DEFER（均依赖真实用户反馈）；明确拒绝：直接进入 customer pilot、LLM live gate、fifth lab 设计；explicitly accepted: small cohort iteration = minor docs update only；recommended next step: 招募 1-2 名真实人类学员，独立使用前端，填写 feedback template Sections 3-10，再次 triage；新增 docs/labgen/SMALL_COHORT_FEEDBACK_TRIAGE_AND_PRODUCT_DECISION_v0.1.md；更新 SMALL_COHORT_PILOT_RESULT_v0.1.md（Section M）/ SMALL_COHORT_READINESS_GATE_v0.1.md / staging_ops_ticket_status.md / staging_infrastructure_checklist.md；docs-only triage，无代码修改，无 runtime session，无 LLM 调用，37/37 技术自检 PASS；3216 tests PASS 93.13%（基线不变）
- feat(labgen): Small Cohort Pilot v0.1 — SMALL_COHORT_PILOT_COMPLETED_WITH_NOTES；3 trusted cohort users (cohort-user-A/B/C) 顺序完成共 6 个 lab sessions（User A: Lab1+Lab2, User B: Lab3+Lab4, User C: Lab1+Lab4）；所有 6 sessions LAB_CLOSED cleanup_verified=True 0 residuals；每轮 J.2/J.3/J.5/J.6 operator checklist 全部 PASS；0 emergency stop；0 concurrent sessions；verifier 凭证每次 lab session 后 reclaim/re-init 行为已确认；4 published labs 全部端到端验证；secret_exists feedback 无 value/base64 泄露；deployment_ready feedback 无 raw workload 泄露；RBAC drift 无回归；ops 发现：verifier creds 在每次 lab session cleanup 后 reclaim → 下一个 lab 开始前必须重新 init（每次 lab 前，不只是每次 user 前）；顺带修复 pre-existing bug vm_tracker datetime offset-naive/aware 不兼容；新增 docs/labgen/SMALL_COHORT_PILOT_RESULT_v0.1.md；决策：SMALL_COHORT_PILOT_COMPLETED_WITH_NOTES — 允许进入下一阶段
- fix: vm_tracker 三个方法（get_all_tracked_vms/get_vm_age_minutes/get_expired_vms）datetime.fromisoformat() 返回 aware datetime 与 naive datetime.now() 相减抛 TypeError — 补回归测试防止重现（小组 pilot 期间在 auto-cleanup task 日志中发现）
- feat(labgen): Small Cohort Readiness Gate v0.1 — SMALL_COHORT_READY_WITH_NOTES；8 名 pilot 用户跨 4 个 published labs，所有 session LAB_CLOSED cleanup_verified=True 0 residuals；0 BLOCKER/HIGH/MEDIUM open（全部在前序 gate 中解决）；新增 docs/labgen/SMALL_COHORT_READINESS_GATE_v0.1.md（sections A-O，34/34 技术自检 PASS）；新增 docs/labgen/SMALL_COHORT_FEEDBACK_TEMPLATE_v0.1.md（13 section 反馈模板，完整 sanitization checklist）；HOME_LAB_MVP_OPS_RUNBOOK_v0.1.md 新增 Section J（Small Cohort Pilot Procedure：pre-cohort precheck/per-user start/complete checklist/residual check/emergency stop/pause-resume/max size/no-concurrency-increase）；更新 EIGHTH_PILOT_USER_DEPLOYMENT_LAB_RESULT_v0.1.md / DEPLOYMENT_FEEDBACK_TRIAGE_v0.1.md / HOME_LAB_MVP_PILOT_GATE_RESULT_v0.1.md（均新增 follow-up/confirmation section）；更新 staging_ops_ticket_status.md + staging_infrastructure_checklist.md（SMALL_COHORT_READY_WITH_NOTES）；无代码修改；无 runtime session；precheck 全清（0 active, 0 lab-* namespaces, 0 tainted VMs）；5 NOTEs（均为 home_lab_mvp 结构性约束，已接受）；3213 tests PASS；93.13% coverage；Small Cohort Pilot v0.1 现已解锁（3–5 trusted users, sequential, max 1 active session）
- feat(labgen): Eighth Trusted Pilot User on Deployment Lab v0.1 — EIGHTH_PILOT_USER_DEPLOYMENT_LAB_ONBOARDED；learner_session_snapshot.py 修复：PASS check_summary.safe_message 现在携带 VerifyResult.detail（deployment_ready PASS = "Deployment hello-deployment is available with 1 ready replica in your isolated namespace. Kubernetes has created a Pod for this workload."）；_build_step_statuses 对 completed 步骤也调用 _check_summary_for_step 以展示最近完成步骤的 PASS detail；_check_summary_for_step 内部 filter results by current_ids（防御性修复，解决 safety-reviewer MEDIUM 发现）；labgenViews.js _renderCheckSummary msg 条件移除 !passed 限制（PASS 时也显示 safe_message）；新增 2 个 Python 测试（completed step PASS detail + fallback）；新增 1 个前端测试（deployment detail 在 passed summary 中可见）；real gate：pilot-user-08，session c225c518，VM 401（labgen-home-k3s-staging-01），Deployment Basics（e52b8b80）；Step 1（namespace_exists: PASS，detail="Your isolated namespace is active on the cluster."）+ Step 2（deployment_ready: PASS，1/1 replicas ready，detail="Deployment 'hello-deployment' is available with 1 ready replica in your isolated namespace. Kubernetes has created a Pod for this workload."）；snapshot safe_message 端到端验证：Step 2 check_summary.safe_message 含完整 deployment detail；RBAC drift fix 从第 7 用户 gate 保持稳定（无 403）；complete → LAB_CLOSED，cleanup_verified=True；12/12 residuals PASS（0 namespace, 0 Deployment/Pod, 0 tainted VM）；31/31 技术自检 PASS；3213 tests PASS；93.13% coverage；新增 docs/labgen/EIGHTH_PILOT_USER_DEPLOYMENT_LAB_RESULT_v0.1.md；Small Cohort Readiness Gate OR Fifth Lab Design Gate 现已解锁
- feat(labgen): Deployment Feedback Triage & Iteration v0.1 — DEPLOYMENT_FEEDBACK_TRIAGED_WITH_ITERATION；系统 triage Deployment workload lab 首次真实用户结果（0 BLOCKER、0 HIGH、2 LOW iterated、1 NOTE resolved）；迭代：(1) verifier.py _make_detail DEPLOYMENT_READY PASS 消息改为"is available with 1 ready replica in your isolated namespace. Kubernetes has created a Pod for this workload."（传达 replica count 和 Pod 概念）；FAIL 消息改为包含 name/replica/approved image/timing 四项可操作指引；(2) lab step 2 do 字段增加"Do not change the replica count or image"约束和重试 hint；(3) HOME_LAB_MVP_OPS_RUNBOOK_v0.1.md 新增 Section I（RBAC-DRIFT-001 记录：create+409-skip 根因、replace_cluster_role 行为说明、operator re-init precheck 命令）；新增 1 个回归测试（test_deployment_ready_fail_detail）；safety-reviewer PASS_WITH_NOTES → LOW（pass-case 已有 assert result.passed is True，reviewer 因只看 diff 未见，确认无需修改）；内部排练：session 4981c171（pilot-user-07，Deployment Basics，service restart 后新代码），Step 1（namespace_exists: PASS）+ Step 2（deployment_ready: PASS，新 detail 验证正确），complete → LAB_CLOSED，cleanup_verified=True，0 residuals，0 backend errors；3211 tests PASS；93.12% coverage；新增 docs/labgen/DEPLOYMENT_FEEDBACK_TRIAGE_v0.1.md；Eighth Pilot User on Deployment Lab OR Fifth Lab Design Gate 现已解锁
- feat(labgen): Seventh Trusted Pilot User on Deployment Lab v0.1 — SEVENTH_PILOT_USER_DEPLOYMENT_LAB_ONBOARDED；pilot-user-07 通过真实 learner frontend 完整走通第四个 pilot lab「Kubernetes Deployment Basics: Run Your First Workload」（lab_id=e52b8b80）；precheck 发现 ClusterRole RBAC drift（旧 create+409-skip 从不更新 live rules：stale get verb、namespaces、endpoints）；修复：改用 replace_cluster_role（PUT 语义，总是应用最新规则）+ 404 fallback to create；manifest 收紧：移除 get 动词（所有资源）、移除 namespaces、移除 endpoints；新增 12 个测试（TestClusterRoleManifestGuardrail 7个 + replace 模式 + 404-fallback + cross-path parity test）；safety-reviewer PASS_WITH_NOTES → LOW finding（manifest/SDK 对象无跨路径一致性测试）立即修复（test_sdk_object_matches_manifest_string）；VM 401 ownership：pilot-user-06 → pilot-user-07；pre-onboarding gate 13/13 PASS；frontend catalog 显示 4 labs；session 4e0c2f20；Step 1（namespace_exists: PASS）+ Step 2（deployment_ready name=hello-deployment: PASS，1/1 replicas ready，detail='Deployment "hello-deployment" is running with 1/1 replicas ready.'）两步均一次通过；deployment RBAC fix（apps/deployments list+watch, no get）在真实用户路径验证；Deployment feedback 安全验证 10/10 PASS；complete → LAB_CLOSED，cleanup_verified=True；residual check 16/16 PASS（0 lab-* namespaces, 0 Deployment/ReplicaSet/Pod, 0 RoleBinding, 0 ClusterRoleBinding, 0 tainted VMs, 0 LLM calls，production VMID 500-599 UNTOUCHED）；技术自检 31/31 PASS；3210 tests PASS，93.11% coverage；新增 docs/labgen/SEVENTH_PILOT_USER_DEPLOYMENT_LAB_RESULT_v0.1.md
- feat(labgen): Deployment Lab Design Gate v0.1 — DEPLOYMENT_LAB_READY；设计并发布第四个 pilot lab「Kubernetes Deployment Basics: Run Your First Workload」（lab_id=e52b8b80，2步：namespace_exists + deployment_ready name=hello-deployment，image=nginx:1.25-alpine，estimated_duration=20min）；StaticValidator 14/14 PASS；publish decision ALLOWED；learner catalog 4 published labs；internal frontend rehearsal（pilot-user-06，VM 401）：LAB_CLOSED，cleanup_verified=True，0 residuals；Step 1（namespace_exists: PASS）+ Step 2（deployment_ready: PASS，detail="Deployment \"hello-deployment\" is ready in your namespace."）两步均一次通过；verifier creds path fix（复制至 /var/lib/labgen-staging/verifier-credentials/{vm_id}/）；constraint compliance PASS（0 LLM、production VMID 未触碰、无 ClusterRoleBinding）；新增 docs/labgen/DEPLOYMENT_LAB_DESIGN_RESULT_v0.1.md；先决修复（本次一并 commit）：OCI Accept header + library/ whitelist prefix，deployment_ready list-only RBAC；技术自检全通过
- fix(labgen): deployment_ready verifier 改用 list_namespaced_deployment（最小权限）；ClusterRole apps 规则收紧为 deployments list+watch only（移除 get 及 daemonsets/statefulsets/replicasets）；更新 7 个现有测试 + 新增 4 个回归测试（list-not-get/no-get-verb-in-ClusterRole）— 补回归测试防止重现
- fix(labgen): image existence check false-negative on OCI multi-arch images — _default_http_get 增加 OCI-compatible Accept headers；image_whitelist.json 中 nginx/busybox 改为 library/ 前缀路径（registry 实际存储路径）；新增 3 个回归测试（TestOCIAcceptHeader 类）防止重现 — 补回归测试防止重现
- feat(labgen): Sixth Trusted Pilot User on Third Lab v0.1 — SIXTH_PILOT_USER_THIRD_LAB_ONBOARDED；pilot-user-06 通过真实 learner frontend 完整走通第三个 pilot lab「Kubernetes Secret Basics: Protect Your First Configuration」（lab_id=d9f44383）；Runbook precheck 20/20 PASS；verifier re-init host-side gen=2（QEMU-agent 未使用）；RBAC 确认：secrets list+watch only，无 get；pre-onboarding gate 13/13 PASS；frontend catalog 显示 3 labs（internal smoke lab 不可见）；session 38bb1a9e；Step 1（namespace_exists: PASS，detail="Your isolated namespace is active on the cluster."）+ Step 2（secret_exists name=my-app-secret: PASS，detail='Secret "my-app-secret" was found in your isolated namespace. The verifier confirmed the Secret object exists without reading its value.'）两步均一次通过；Secret feedback 安全验证 11/11 PASS（无 Secret value/base64/namespace/token/kubeconfig/raw exception 泄露）；RBAC 修复（第三 lab 设计门中发现并修复）在真实用户路径验证通过（无 403）；complete → LAB_CLOSED，cleanup_verified=True；residual check 15/15 PASS（0 lab-* namespaces, 0 RoleBinding, 0 tainted VMs, 0 LLM calls，production VMID 500-599 UNTOUCHED）；0 代码修改（纯 ops gate）；技术自检 31/31 PASS；新增 docs/labgen/SIXTH_PILOT_USER_THIRD_LAB_RESULT_v0.1.md；更新 deploy/labgen/staging_ops_ticket_status.md（G-22 SIXTH_PILOT_USER_THIRD_LAB_ONBOARDED）；3186 tests 93.13%（基线不变）；Seventh User OR Deployment Lab Design Gate 现已解锁
- feat(labgen): Third Lab Design Gate v0.1 — THIRD_PILOT_LAB_READY；设计并发布第三个 pilot lab「Kubernetes Secret Basics: Protect Your First Configuration」（lab_id=d9f44383，2步：namespace_exists + secret_exists name=my-app-secret，dummy value only，estimated_duration=15min）；发现并修复真实 bug：ClusterRole `lab-verifier-namespace-readonly` 缺少 `secrets` 资源，导致 secret_exists check 403（根因：secret_exists verifier type 已实现但 RBAC 从未更新）；safety-reviewer 审查后进一步收紧权限：secrets 独立规则，仅 `list, watch`（不含 `get`，防止读取 .data）；增强 SECRET_EXISTS detail 消息（pass 增加"without reading its value"教学说明）；新增 5 个回归测试（ClusterRole 含 secrets/secrets-no-get/manifest-no-get/secret_exists_fail_detail/cluster_role_includes_secrets_resource）；StaticValidator 14/14 PASS；publish decision ALLOWED；learner catalog 3 published labs；internal frontend rehearsal：LAB_CLOSED，cleanup_verified=True，0 residuals；safety-reviewer APPROVED（MEDIUM 修复后）；3186 tests PASS；93.13% coverage；0 LLM 调用；production VMID 500-599 UNTOUCHED；QEMU-agent verifier path 未使用；新增 docs/labgen/THIRD_PILOT_LAB_DESIGN_RESULT_v0.1.md；Sixth Trusted Pilot User on Third Lab Gate 现已解锁
- feat(labgen): Fifth Trusted Pilot User on Second Lab v0.1 — FIFTH_PILOT_USER_SECOND_LAB_ONBOARDED；pilot-user-05 通过真实 learner frontend 完整走通第二个 pilot lab「Kubernetes ConfigMap Basics」；Runbook precheck 19/19 PASS；pre-onboarding gate 20/20 PASS；Step 1（namespace_exists: PASS，detail="Your isolated namespace is active on the cluster."）+ Step 2（configmap_exists name=my-app-config: PASS，detail='ConfigMap "my-app-config" was found in your isolated namespace. Your Kubernetes resource was created successfully.'）两步首次均一次通过；改善后的 verifier feedback 前端可见，安全验证 PASS（无 namespace/token/kubeconfig/raw exception 泄露）；complete → LAB_CLOSED，cleanup_verified=True；residual check 11/11 PASS（0 lab-* namespaces, 0 RoleBinding, 0 tainted VMs, 0 LLM calls, production VMID 500-599 UNTOUCHED）；verifier creds 以 initialize_verifier_for_vm_host_side 初始化（gen=1），session 结束后正常 reclaim；QEMU-agent verifier path 未使用；0 新增代码；技术自检 27/27 PASS；新增 docs/labgen/FIFTH_PILOT_USER_SECOND_LAB_RESULT_v0.1.md；Third Lab Design Gate 现已解锁
- feat(labgen): Second Lab Feedback Triage & UX Iteration v0.1 — SECOND_LAB_FEEDBACK_TRIAGED_WITH_ITERATION；对第二个 multi-step pilot lab（ConfigMap Basics）首次真实用户结果进行系统 triage（0 BLOCKER、0 HIGH、1 MEDIUM、3 LOW、4 NOTE）；ISS-01 MEDIUM：configmap_exists success 返回空 detail，改为 learner-facing 确认消息；实施最小 verifier feedback 迭代：VerifierService 新增 `_make_detail(vtype, name, passed) -> str` 静态方法，dispatch 路径的 VerifyResult 现在返回非空 detail（6 种 verify type 均有 pass/fail 消息）；security 约束保留：_fail() 安全拒绝路径 detail 仍为空，消息不含 session.namespace；新增 11 个回归测试（TestMakeDetail 类，含 namespace-in-detail 安全回归、cluster_scope 安全路径、6 个 verify type 各自的 pass/fail 断言）；内部排练（pilot-user-04，ConfigMap Basics，Step 2 detail 验证：'ConfigMap "my-app-config" was found in your isolated namespace. Your Kubernetes resource was created successfully.'）：LAB_CLOSED，cleanup_verified=True，0 residuals；新增 docs/labgen/SECOND_LAB_FEEDBACK_TRIAGE_v0.1.md；更新 staging_ops_ticket_status.md（G-19 SECOND_LAB_FEEDBACK_TRIAGED_WITH_ITERATION）；更新 staging_infrastructure_checklist.md（G-19）；3181 tests PASS；93.13% coverage；verifier.py 99% coverage；0 LLM 调用；production VMID 500-599 UNTOUCHED；第五用户和第三 lab 设计现已解锁
- feat(labgen): Fourth Trusted Pilot User on Second Lab v0.1 — FOURTH_PILOT_USER_SECOND_LAB_ONBOARDED；pilot-user-04（staging 账号）通过真实 learner frontend 完整走通第二个 pilot lab「Kubernetes ConfigMap Basics: Store Your First Config」；Runbook precheck 20/20 PASS；pre-onboarding gate 19/19 PASS；frontend path 13/13 PASS；Step 1（namespace_exists: passed）+ Step 2（configmap_exists name=my-app-config: passed）两步首次均一次通过；complete → LAB_CLOSED，cleanup_verified=True；residual check 11/11 PASS（0 lab-* namespaces, 0 RoleBinding, 0 tainted VMs, 0 LLM calls，production VMID 500-599 UNTOUCHED）；verifier creds 以 initialize_verifier_for_vm_host_side 重新初始化（session 前 gen=1），session 结束后正常 reclaim；QEMU-agent verifier path 未使用；0 新增代码；技术自检 21/21 PASS；新增 docs/labgen/FOURTH_PILOT_USER_SECOND_LAB_RESULT_v0.1.md；更新 deploy/labgen/staging_ops_ticket_status.md（G-18 FOURTH_PILOT_USER_SECOND_LAB_ONBOARDED）；测试基线不变
- feat(labgen): Second Pilot Lab Design Gate v0.1 — SECOND_PILOT_LAB_READY；设计并发布第二个 pilot lab「Kubernetes ConfigMap Basics: Store Your First Config」（lab_id=b0b97742，2步：namespace_exists + configmap_exists，无镜像，estimated_duration=15min）；configmap_exists verifier 首次端到端验证；StaticValidator 14/14 PASS；publish decision ALLOWED；learner catalog 2 published labs；Playwright headless frontend rehearsal 31/31 PASS（login→catalog→detail→start→step1 check→ConfigMap create→step2 check→complete→LAB_CLOSED, cleanup_verified=True, 0 namespace residuals, 0 tainted VMs）；VM 401 重建（Runbook D.5，clone from 101，staging range 400-499，etcd reset，K3s fresh start，host-side verifier init）；新增 docs/labgen/SECOND_PILOT_LAB_DESIGN_RESULT_v0.1.md；更新 deploy/labgen/staging_ops_ticket_status.md（SECOND_PILOT_LAB_READY）；更新 deploy/labgen/staging_infrastructure_checklist.md；0 LLM 调用，production VMID 500-599 未触碰，无代码修改（configmap_exists 为已有 verifier type）；测试基线不变
- feat(labgen): Third Trusted Pilot User Gate v0.1 — THIRD_PILOT_USER_ONBOARDED；pilot-user-03（staging 账号）通过真实 learner frontend 首次完整走通 pilot lab（步骤通过 + Complete + LAB_CLOSED）；Playwright headless smoke：19 PASS 0 FAIL 0 NOTE；全链路：login→catalog（1 lab only）→lab detail（Start enabled）→session→check-step（namespace_exists PASS）→Complete→cleanup（LAB_CLOSED, cleanup_verified=True）；VM 401 重建（config file 丢失，按 Runbook Section D.5 恢复：clone from template 101, staging range 400-499, etcd reset, hostname fix, K3s fresh start, verifier creds gen=1 via initialize_verifier_for_vm_host_side）；首次完整 completion 路径验证（第二 pilot 只走 abort 路径）；所有 residual check 全清（0 lab-* namespaces, 0 RoleBinding, 0 tainted VMs, 0 LLM calls, production VMID 500-599 untouched）；新增 docs/labgen/THIRD_PILOT_USER_ONBOARDING_RESULT_v0.1.md；更新 deploy/labgen/staging_ops_ticket_status.md（G-17 THIRD_PILOT_USER_ONBOARDED）；更新 deploy/labgen/staging_infrastructure_checklist.md（G-17）；测试基线不变 (3170 tests, 93.13% coverage)；无代码修改
- feat(labgen): Home-Lab Ops Runbook Hardening v0.1 — OPS_RUNBOOK_HARDENED；新增 docs/labgen/HOME_LAB_MVP_OPS_RUNBOOK_v0.1.md（A-H 共 8 节：scope/critical rules/verifier init/VM recovery/pilot ops/emergency stop/cloud portability/OPS-INIT-001 resolution）；明确规定 home_lab_mvp 必须使用 initialize_verifier_for_vm_host_side（非 QEMU-agent 路径），并说明原因（QEMU-agent 创建 kubeconfig 指向 127.0.0.1:6443 宿主机不可达）；禁止在 home_lab_mvp 中调用 initialize_verifier_for_vm；新增 tests/test_home_lab_ops_runbook_guardrails.py（31 guardrail tests：runbook 存在/host-side 函数名/QEMU-agent 禁止/platform kubeconfig/VMID 边界/non-HA 声明/无 TODO-FIXME/无 secret 泄露/OPS-INIT-001 已文档化/cloud portability/emergency stop/staging pool）；更新 SECOND_PILOT_USER_ONBOARDING_RESULT_v0.1.md（OPS-INIT-001 从 open→RESOLVED）；更新 PILOT_FEEDBACK_TRIAGE_v0.1.md（OPS-INIT-001 已记录）；更新 HOME_LAB_MVP_STAGING_PROFILE_v0.1.md（新增 I-A 节引用 runbook）；更新 staging_ops_ticket_status.md（新增 G-16 OPS_RUNBOOK_HARDENED）；更新 staging_infrastructure_checklist.md（新增 G-16）；第三个 pilot 用户 onboarding 现已解锁
- feat(labgen): Second Trusted Pilot User Gate v0.1 — SECOND_PILOT_USER_ONBOARDED；pilot-user-02（staging 账号）通过真实 learner frontend 完整走通 pilot lab；Playwright headless smoke：23 PASS 0 FAIL 0 NOTE；全链路：login→catalog（1 lab only）→lab detail（Start enabled）→session→check-step→abort→cleanup（LAB_CLOSED, cleanup_verified=True）；确认 catalog 只展示 pilot lab（e5b5aa73 smoke lab 已隐藏）；无 CSP 错误（Cloudflare beacon 由 CSP 正确拦截）；residual check 全清（0 active sessions、0 lab namespaces、0 tainted VMs、production VMID 500-599 UNTOUCHED）；发现并记录 1 个 ops gap：home_lab_mvp 需调用 `initialize_verifier_for_vm_host_side`（非 QEMU-agent 路径）确保 verifier kubeconfig endpoint 正确（172.16.100.147 非 127.0.0.1）；staging 重建：VM 401 从 template 101 full clone 重建，etcd reset，K3s fresh start，hostname 修正为 labgen-home-k3s-staging-01，verifier creds gen=2；pilot-user-02 账号创建 + VM 401 分配；新增 docs/labgen/SECOND_PILOT_USER_ONBOARDING_RESULT_v0.1.md；更新 deploy/labgen/staging_ops_ticket_status.md 和 staging_infrastructure_checklist.md；无 LLM 调用，无生产 VM 修改；测试基线不变 (3139 tests, 93.13% coverage)
- feat(labgen): Frontend Learner Pilot Smoke v0.1 — FRONTEND_LEARNER_SMOKE_PASSED_WITH_NOTES；Playwright headless 实浏览器全链路 smoke（catalog→detail→start→session→check→abort/cleanup）16 PASS 0 FAIL；CSP 修复（FE-BUG-01）：3 个 labgen HTML 的 inline `<script type="module">` 提取到外部 JS 文件（labgen-catalog-init.js / labgen-lab-init.js / labgen-session-init.js）；labgenViews.js 7 个字段名错误全修（FE-BUG-02~08，含 session_state/title/can_check_current_step/runtime_summary.*/step.status/step.is_current/step.check_summary）；lab detail 2 个 eligibility 字段错误修复（FE-BUG-09：is_eligible→is_startable；FE-BUG-10：runtime_checks_deferred 从 issues 数组读取；FE-BUG-11：reasons→issues）；backend POST /api/lab-sessions vm_id 改为 Optional + 自动发现（FE-BUG-12）；staging K3s VM 401 重建（clone VM 101，pool k8s-netlab-staging，4096MB，vmbr1，172.16.100.147）；verifier creds 初始化（gen=2）；smoke-user-01 分配 VM 401；cleanup_verified=True；production VMID 500-599 UNTOUCHED；3139 tests PASS；93.13% coverage；6 notes（均已分析，无 blocker）；新增 docs/labgen/FRONTEND_LEARNER_PILOT_SMOKE_RESULT_v0.1.md；前端测试 124/125 pass（1 个预存 LLM provider bug 与本次无关）
- feat(labgen): Pilot Feedback Triage v0.1 — PILOT_FEEDBACK_TRIAGED；对 First Pilot User Onboarding v0.1 进行完整事后分析；0 BLOCKER、0 HIGH；3 个 MEDIUM bug 已全部修复（含回归测试）：namespace_exists 403（改 list_namespaced_config_map）、env SA 名不一致（修正 home_lab_mvp.env）、cleanup timeout 不足（config.py+routes.py 透传）；2 LOW open（同步 30s cleanup blocking API、VM 400 orphan tracker entry）；5 NOTE open（前端完全未测试、用户反馈不足、仅 namespace_exists verifier 端到端验证、env 与 config 漂移风险、cloud portability 未验证）；FEEDBACK_INSUFFICIENT：本轮 pilot 为 operator API 模拟，无真实用户前端交互；下一步推荐：onboard 第二个 pilot 用户前须先做前端 smoke test；新增 docs/labgen/PILOT_FEEDBACK_TRIAGE_v0.1.md；更新 deploy/labgen/staging_ops_ticket_status.md、staging_infrastructure_checklist.md（G-13 PILOT_FEEDBACK_TRIAGED）；不开放第二用户、不启用 LLM、不调整并发限制
- feat(labgen): First Pilot User Onboarding v0.1 — FIRST_PILOT_USER_ONBOARDED_WITH_NOTES；pilot-user-01（sanitized，1 用户，1 lab，1 session）完整走通 LabGen lab flow（LAB_CLOSED，cleanup_verified=True）；in-session 发现并修复 3 个 bug：(1) namespace_exists() 使用 cluster-scoped read_namespace() 导致 step check 403（已改为 list_namespaced_config_map namespace-scoped）；(2) home_lab_mvp.env LABGEN_K8S_VERIFIER_SA_NAME 错误设为 labgen-verifier（已修为 lab-verifier 与 config.py 默认值对齐）；(3) HTTP API get_session_service() 未透传 ns_delete_max_retries=15/ns_delete_poll_interval=2.0 导致 cleanup false negative（已通过 config.py 新增配置并 routes.py 透传）；每个 bug 均先写失败回归测试再修代码；新增 docs/labgen/FIRST_PILOT_USER_ONBOARDING_RESULT_v0.1.md；更新 deploy/labgen/staging_ops_ticket_status.md（新增 G-12 FIRST_PILOT_USER_ONBOARDED_WITH_NOTES）；更新 deploy/labgen/staging_infrastructure_checklist.md（状态升级至 FIRST_PILOT_USER_ONBOARDED_WITH_NOTES）；session: 4c63b475-480c-4ef1-81f0-df2f48ae5a1a；namespace: lab-4c63b475-480c-4ef1-81f0-df2f48ae5a1a；verifier result: namespace_exists PASS；cleanup: LAB_CLOSED, cleanup_verified=True；post-pilot 残留检查全清；production VMID 500-599 UNTOUCHED；3137 tests PASS；93.12% coverage；safety-reviewer APPROVED
- fix(labgen): namespace cleanup false negative 重现——HTTP API 路径 get_session_service() 未将 ns_delete_max_retries/ns_delete_poll_interval 透传给 LabSessionService，导致使用 5×1s=5s 窗口（K3s 异步删除需 30s）；新增 config.py LABGEN_NS_DELETE_MAX_RETRIES=15 / LABGEN_NS_DELETE_POLL_INTERVAL_S=2.0；routes.py 透传到 LabSessionService；新增回归测试 TestSessionServiceRetryConfig（2 tests）；28 tests PASS
- fix(labgen): namespace_exists() 改用 list_namespaced_config_map（namespace-scoped）代替 read_namespace（cluster-scoped）——后者需 ClusterRoleBinding 但 verifier 只有 per-session RoleBinding，导致 step check 403 Forbidden；新增回归测试：test_does_not_use_read_namespace / test_propagates_403_forbidden；现有 3 个 TestNamespaceExists 测试同步更新（用 list_namespaced_config_map 断言）；40 tests PASS
- feat(labgen): First Pilot Lab Release & Onboarding Gate v0.1 — PILOT_LAB_READY_WITH_NOTES；首个真实 pilot lab 发布：「Kubernetes Basics: Your Isolated Lab Environment」（lab_id=67fca5e4，1步 namespace_exists 验证，无镜像，无外部网络，estimated_duration=10min）；完整 publish gate：StaticValidator 14/14 PASSED；内部 smoke lab（e5b5aa73）unpublish 确保 learner catalog 只显示 pilot lab；internal runtime rehearsal 11步全通过（VM 401 K3s）：seed_draft/init_verifier(gen=1)/build_services/create_session(LAB_ACTIVE)/namespace_exists(PASS)/check_step(all_passed=True,ready_to_complete=True)/session_snapshot/complete_session(LAB_CLOSED,cleanup_verified=True)/namespace_deleted/no_tainted_vm/residual_check；staging 产物全清除（verifier creds/sessions/drafts）；VM 401 stop；新增 docs/labgen/FIRST_PILOT_LAB_RELEASE_RESULT_v0.1.md；新增 docs/labgen/FIRST_PILOT_USER_ONBOARDING_v0.1.md；更新 deploy/labgen/staging_ops_ticket_status.md；更新 deploy/labgen/staging_infrastructure_checklist.md；tech blocker 自检 10项全通过；3133 tests PASS；93.12% coverage（未改代码）
- feat(labgen): Verifier Client Path Smoke v0.1 — SMOKE PASSED；完整验证链路：PlatformVerifierInitializer（platform kubeconfig → K3s lab-verifier SA/ClusterRole/TokenRequest）→ VerifierCredentialStore（staging /var/lib/labgen-staging/verifier-credentials/）→ LabSessionService.create_session（StubVMTracker + K3sNamespaceLifecycleAdapter：namespace lab-{uuid} 在 VM 401 K3s 创建）→ StepProgressionService.check_step（K8sVerifierClientAdapter.namespace_exists via verifier kubeconfig）→ LabSessionService.complete_session（namespace 删除 + cleanup_verified=True）→ namespace_deleted 确认；K3s 异步删除需 2s/retry×15（总计 30s 窗口）；staging cred root /var/lib/labgen-staging/verifier-credentials/（chmod 700）；smoke 用 StubVMTracker（无需真实 Proxmox VM）；新增 scripts/labgen_verifier_client_path_smoke.py（7 函数：preflight/seed_smoke_draft/init_verifier_creds/build_services/run_smoke/cleanup_staging/main）；新增 tests/test_labgen_verifier_client_path_smoke.py（29 tests，全静态，无真实 K8s 调用）；VM 401 smoke 后仍运行（停止：qm stop 401）；3133 tests PASS；93.12% coverage
- feat(labgen): Home-Lab MVP Pilot Gate v0.1 — PILOT_GATE_READY_WITH_NOTES；VM 400 清理确认（qm config 400 → not found）；完整 residual check（staging pool 仅 VM 401、K3s 无 lab- namespace 残留、无 RoleBinding 残留、无 verifier credential 残留、无 tainted VM、lab_sessions=0、smoke-admin user 已删除、production VMID 500-599 未触碰）；K3s list/read namespace stale cache 行为确认为 K8s 已知行为（非 cleanup bug）；新增 `docs/labgen/HOME_LAB_MVP_PILOT_GATE_RESULT_v0.1.md`（A smoke summary / B VM 400 cleanup / C full residual check / D verifier client note / E accepted MVP risks / F pilot scope / G runtime policy + emergency stop / H customer pilot boundaries / I monitoring plan / J pre-pilot required actions / K next steps / L technical self-check）；更新 staging_infrastructure_checklist.md（G-11 PILOT_GATE_READY_WITH_NOTES）；更新 staging_ops_ticket_status.md（新增 Pilot Gate 行）；Notes：0 published labs（需发布 ≥1 个 curated lab 后方可 onboard 用户）；verifier client 未 exercise（pilot labs 须用 verify:[] 直至 Verifier Client Path Smoke v0.1 通过）
- feat(labgen): Home-Lab Runtime Secrets Injection & Controlled Runtime Session Smoke v0.1 — RUNTIME_SESSION_SMOKE_PASSED_WITH_NOTES；完整 smoke 链路：VM 400 create（staging pool 400-499）→ LAB_ACTIVE（namespace lab-7dadd2cf on VM 401 K3s）→ step-1 check（all_passed=true）→ LAB_CLOSED（cleanup_verified=true）→ namespace 删除确认（K3s 无 lab- namespace 残留）；5 个 bug 在 smoke 过程中发现并修复（见 Section C of result doc）；smoke admin/draft/sessions/audit 清除完毕；VM 400 停止（需手动 qm destroy 400）；新增 `docs/labgen/CONTROLLED_HOME_LAB_RUNTIME_SESSION_SMOKE_RESULT_v0.1.md`；更新 staging_ops_ticket_status.md（全部 6 tickets VERIFIED）；更新 staging_infrastructure_checklist.md（G-9 K3S_SMOKE_PASSED，G-10 RUNTIME_SESSION_SMOKE_PASSED_WITH_NOTES）；3076 tests PASS；93.27% coverage
- fix(labgen): namespace cleanup false negative on async K3s deletion — `_do_cleanup` now retries `is_namespace_deleted` up to N times with configurable poll interval (default 5×1s) instead of single immediate check after `delete_namespace`; K3s namespace deletion is async, the namespace enters Terminating state before gone; `LabSessionService.__init__` adds `ns_delete_poll_interval` and `ns_delete_max_retries` params (default 1.0s/5); `_DelayedDeleteAdapter` stub + 1 regression test added to `test_labgen_lab_completion.py`; also adds `Proxmox Pool.Allocate` ACL grant for `k8s-netlab-staging` pool (user + token) to fix staging VM creation 403; fixes `LABGEN_K8S_NAMESPACE_ALLOWED_PREFIXES=lab-stg-` over-restriction in staging env (changed to `lab-` to match session service's `lab-{uuid}` format); 3076 tests PASS; 93.27% coverage
- fix(labgen): home_lab_mvp runtime secrets injection — add `home_lab_mvp` to `_SAFE_RUNTIME_MODES` in `labgen_production_preflight.py`（与 provisioning_validate 已有的集合对齐）；在 `labgen_staging_provisioning_validate._check_secret_keys_not_active()` 中将 home_lab_mvp profile 的 secret-in-file 检查从 BLOCK 降级为 WARNING（home_lab_mvp 无外部 secret manager，chmod 600 repo-external 文件是 MVP 级 credential store）；add `LABGEN_LLM_PROVIDER_MODE=fake_only` 到 staging env；4 个回归测试覆盖新行为（home_lab_mvp preflight PASS / home_lab_mvp secret-in-file WARNING / non-home_lab_mvp 仍 BLOCK）；intake gate 通过 READY_TO_RERUN_CONTROLLED_STAGING_TRIAL；6/6 ops tickets VERIFIED；SECRET_INJECTION_READY；3075 tests PASS；93.26% coverage
- feat(labgen): Home-Lab Staging K3s VM Bootstrap v0.1 — Claude Code 作为 dev+ops 执行完整 home-lab staging K3s VM bootstrap；只读预检确认 VM 101（template: 1，stopped，local-lvm ~1.7TB），VMID 400-499 空闲，无 k8s-netlab-staging pool；创建 `k8s-netlab-staging` pool（隔离于生产 k8s-netlab）；full clone VM 101 → VMID 401 `labgen-home-k3s-staging-01`（32 GiB, local-lvm），内存从 2048 升至 4096 MB；启动 VM 401，K3s v1.34.4+k3s1 预装（template 内置），node k8s-template Ready；K3s API 在 172.16.100.147:6443 （`*:6443`）监听，HTTP 401 从宿主机确认可达；SCP kubeconfig（不打印内容），server URL 从 127.0.0.1 更新为 VM IP，写入 `/etc/labgen/home_lab_mvp.kubeconfig`（chmod 600）；创建 `/etc/labgen/home_lab_mvp.env`（chmod 600，repo 外，含真实 kubeconfig path、`LABGEN_RUNTIME_MODE=home_lab_mvp`、`LABGEN_K8S_NAMESPACE_ALLOWED_PREFIXES=lab-stg-`、verifier 配置、resource limits）；non-write precheck：Phase 0-2 全部 PASS；write smoke（`--allow-k8s-write`）：全部 11 phase PASS，`K3S_SMOKE_PASSED`，`cleanup_confirmed: true`；残留检查：`lab-stg-` namespace NONE，kube-system RoleBinding NONE；新增 `docs/labgen/HOME_LAB_K3S_VM_BOOTSTRAP_RESULT_v0.1.md`；更新 `docs/labgen/CONTROLLED_K3S_ADAPTER_SMOKE_RESULT_v0.1.md`（由 K3S_SMOKE_BLOCKED_BY_MISSING_KUBECONFIG 升级为 K3S_SMOKE_PASSED，全量 11 phase 输出）；更新 `deploy/labgen/staging_ops_ticket_status.md`（OPS-K3S-001 BLOCKED_WITH_EVIDENCE → VERIFIED；K3S smoke gate → [x] READY）；更新 `deploy/labgen/staging_infrastructure_checklist.md`（F-1/F-2 resolved；K-1~K-11 全部 [x] PASS）；runtime start NOT EXECUTED；proxmox_called: false（仅 clone/pool 创建，非 LabGen 调用）；registry_called: false；llm_called: false；无 production VM/pool 修改；无 kubeconfig/credential 入库；LIVE_TRIAL_BLOCKED 维持（下一门：Controlled Home-Lab Runtime Session Smoke v0.1）；3071 tests PASS；93.26% coverage（仅 ops bootstrap + docs，无代码变更）
- feat(labgen): Home-Lab Kubeconfig Injection & K3s Adapter Smoke Execution v0.1 — Claude Code acting as ops 检查 home-lab 环境中所有 kubeconfig 来源；确认 VM 101 为 Proxmox template（template: 1，无法启动），无 K3s binary，无运行中 K3s VM（VMID 500-599 均停止），无 /root/.kube / /etc/k3s / /etc/rancher；final decision: K3S_SMOKE_BLOCKED_BY_MISSING_KUBECONFIG（单一 blocker：ops 需先 provision staging K3s VM）；新增 `FINAL_BLOCKED_MISSING_KUBECONFIG = "K3S_SMOKE_BLOCKED_BY_MISSING_KUBECONFIG"` 常量 + `_is_kubeconfig_only_blocker()` 函数至 smoke helper（kubeconfig 为 sole blocker 时输出更具体决策，其余 BLOCKED 情形保持 FINAL_BLOCKED）；exit code 2 同时覆盖两种 BLOCKED 决策；新增测试 TestIsKubeconfigOnlyBlocker（7 tests）+ 3 个新 TestRunSmokeBlocked tests + 1 个 TestMain test = 共 10 个新测试（总计 69 tests）；新增 `docs/labgen/CONTROLLED_K3S_ADAPTER_SMOKE_RESULT_v0.1.md`（result artifact：B kubeconfig search result 表格/C precheck JSON output/D write smoke not executed/E audit confirmation/F evidence sanitization/G final decision/H unblock path/I technical debt self-check/J next steps）；更新 `docs/labgen/CONTROLLED_K3S_ADAPTER_SMOKE_v0.1.md`（Section G：由 K3S_SMOKE_BLOCKED 改为 K3S_SMOKE_BLOCKED_BY_MISSING_KUBECONFIG；新增 ops kubeconfig search result；新增 result artifact 引用；Section I: 更新 test count）；更新 staging_ops_ticket_status.md（K3S smoke gate 文字由 BLOCKED → BLOCKED_BY_MISSING_KUBECONFIG；引用 result artifact）；更新 staging_infrastructure_checklist.md（G-9 状态更新）；更新 CONTROLLED_STAGING_TRIAL_v0.1.md（K3s smoke 状态更新 + result artifact 链接）；更新 HOME_LAB_MVP_STAGING_PROFILE_v0.1.md（Section J 新增 ops search 结论）；无 K3s write，无 namespace 创建，无 Proxmox/registry/LLM 调用，LIVE_TRIAL_BLOCKED 维持不变；3070 tests PASS；93.26% coverage
- feat(labgen): Controlled K3s Adapter Smoke v0.1 — 新增 `scripts/labgen_controlled_k3s_adapter_smoke.py`（10 阶段 namespace lifecycle smoke helper；`--allow-k8s-write` 显式授权；4 种 final decision；cleanup 在 finally 强制执行；adapter 可注入；无 Proxmox/registry/LLM/runtime start）；新增 `tests/test_labgen_controlled_k3s_adapter_smoke.py`（57 tests；全使用 StubNamespaceLifecycleAdapter，无真实 K8s 调用）；新增 `docs/labgen/CONTROLLED_K3S_ADAPTER_SMOKE_v0.1.md`（Scope/Preconditions/Phases/Evidence Rules/Execution/Current Result：K3S_SMOKE_BLOCKED）；precheck 结果：K3S_SMOKE_BLOCKED（kubeconfig placeholder，no K3s write）；更新 staging checklist/ticket status/HOME_LAB_MVP/CONTROLLED_STAGING_TRIAL/PORTABILITY/PROVISIONING 各文档；不连接 K3s/Proxmox/registry，LIVE_TRIAL_BLOCKED 维持不变；3059 tests PASS；93.26% coverage
- feat(labgen): Lightweight Same-Proxmox Home-Lab Staging Profile v0.1 — 新增 `docs/labgen/HOME_LAB_MVP_STAGING_PROFILE_v0.1.md`（A scope/B hardware reality/C same-Proxmox isolation model：隔离边界表格 10 维度/K3s 同集群与独立集群两种选项/ACCEPTED_MVP_RISK 声明/D resource limits：MAX_TOTAL_VMS=3/VM concurrency constraint/emergency kill switch/E portability boundary：migration path to cloud/F accepted MVP risks：10 条 RISK 明细/G exit criteria to cloud：7 个触发阈值/H config keys/I verification commands/J unblock path 13 步）；更新 `deploy/labgen/.env.staging.example`（LABGEN_RUNTIME_MODE=production → home_lab_mvp；移除 stale NotImplementedError 注释；启用并注释 LABGEN_K8S_PLATFORM_KUBECONFIG_PATH（secret manager placeholder）；新增 8 个 K8s env key：LABGEN_K8S_IN_CLUSTER=false / LABGEN_K8S_CONTEXT / LABGEN_K8S_API_TIMEOUT_SECONDS / LABGEN_K8S_NAMESPACE_ALLOWED_PREFIXES=lab-stg- / LABGEN_K8S_VERIFIER_SA_NAME / LABGEN_K8S_VERIFIER_SA_NAMESPACE / LABGEN_K8S_VERIFIER_ROLE_NAME / LABGEN_K8S_VERIFIER_ROLEBINDING_NAME；VM_ID_MIN/VM_ID_MAX 改为 placeholder（非生产 500/599）；PROXMOX_POOL 改为 k8s-netlab-staging；VM_TEMPLATE_ID 改为 placeholder（非 VM 101）；APP_PORT=8001；MAX_TOTAL_VMS=3；标注 ACCEPTED_MVP_RISK）；更新 `deploy/labgen/.env.production.example`（移除 stale "K3sNamespaceLifecycleAdapter is currently a skeleton" 注释；新增 8 个 K8s env key；LABGEN_K8S_IN_CLUSTER=false/LABGEN_K8S_NAMESPACE_ALLOWED_PREFIXES=lab- 等）；新增 `tests/test_home_lab_mvp_profile_guardrails.py`（57 tests：home_lab_mvp stub blocked/kubeconfig required/no Proxmox import in adapter/no Cloudflare import/no hardcoded IP/cloud profile in-cluster+kubeconfig/dev profile not misidentified/env example placeholder safety/staging pool/admin token/in_cluster false/staging namespace prefix/VMID range placeholder/same-Proxmox risk documented/profile doc exists+accepted risks+exit criteria+non-HA/core modules no proxmox import/no cloudflare import/no hardcoded 172.16.100/production example no NotImplementedError/has new K8s keys）；更新 staging docs for same-Proxmox approach：STAGING_ENVIRONMENT_PROVISIONING_v0.1.md（Section D 新增 same-Proxmox isolation table + `pvesh create /pools` 命令 + ACCEPTED_MVP_RISK 声明）、STAGING_OPS_HANDOFF_v0.1.md（blocker list 更新：F-1=kubeconfig-only，标注 CODE BLOCKER RESOLVED；same-Proxmox host acceptable 注释；infra table 更新 K3s 同集群选项 + staging pool 创建命令）、OPS_PROVISIONING_TICKET_PACK_v0.1.md（OPS-K3S-001 标注 adapter fully implemented）、staging_ops_ticket_status.md（OPS-K3S-001 notes: CODE BLOCKER RESOLVED；OPS-PROXMOX-001 notes: same-Proxmox acceptable）、staging_infrastructure_checklist.md（F-1 更新：CODE BLOCKER RESOLVED；F-2 更新：same Proxmox host acceptable + pool 创建命令）；不连接真实 K3s/Proxmox/registry，无 runtime start，无 namespace 创建，无 secret 注入，LIVE_TRIAL_BLOCKED 维持不变
- fix(labgen): Codex P1 — RuntimeAdapterSelectionService.select() 在 kubeconfig 和 in_cluster 同时设置时立即报 blocking issue，防止 production_safe=True 但 build_adapter() crash 的分裂行为；补 7 个回归测试覆盖 home_lab_mvp/cloud/production 三个 profile 的互斥场景及正向路径（kubeconfig-only / in_cluster-only / 两者皆缺）
- feat(labgen): Portable K3s Namespace Lifecycle Adapter v0.1 — 实现完整 `K3sNamespaceLifecycleAdapter`，消除 `NotImplementedError` skeleton；适配器面向 Kubernetes API，可迁移至 EKS/ACK/任意 K8s，不绑定 Proxmox/T430/Cloudflare Tunnel；新增 `NamespaceSafetyValidator`（namespace 名称安全验证：DNS label/系统 namespace 阻断/allowed prefix/unsafe chars）；新增 `K8sAdapterConfig` dataclass（kubeconfig path 或 in-cluster 二选一，构造即验证）；新增 `PlatformKubernetesClientLoader`（injectable，支持 kubeconfig path + in-cluster config）；raw Kubernetes exception body 永不暴露（仅用 status/reason）；RoleBinding namespace-scoped，无 ClusterRoleBinding；`RuntimeMode` 新增 `home_lab_mvp`（T430 home-lab，stub 禁止，kubeconfig 必填）和 `cloud`（EKS/ACK，支持 in-cluster config，stub 禁止）；`RuntimeAdapterSelectionService.select()` 新增 `k8s_in_cluster` 参数，cloud profile 支持 in-cluster；`build_adapter()` 新增可选 `adapter_config` 参数，构造时传入 `K8sAdapterConfig`；`FailureReason` 新增 5 个 stable 机器码（NAMESPACE_STUCK_TERMINATING/K8S_API_UNAVAILABLE/NAMESPACE_CONFIG_MISSING/NAMESPACE_INVALID_NAME/NAMESPACE_ADAPTER_CONFIG_UNSAFE）；`config.py` 新增 8 个 K8s 配置 env key（LABGEN_K8S_IN_CLUSTER/LABGEN_K8S_CONTEXT/LABGEN_K8S_API_TIMEOUT_SECONDS/LABGEN_K8S_NAMESPACE_ALLOWED_PREFIXES/LABGEN_K8S_VERIFIER_SA_NAME/LABGEN_K8S_VERIFIER_SA_NAMESPACE/LABGEN_K8S_VERIFIER_ROLE_NAME/LABGEN_K8S_VERIFIER_ROLEBINDING_NAME）；V1Subject → RbacV1Subject 兼容 kubernetes-client/python 36.x；新增 tests/test_k3s_namespace_lifecycle_adapter.py（adapter/safety/config/no-kubectl/lazy-init，共 ~60 tests）；新增 tests/test_namespace_lifecycle_portability.py（profile 选择/同一 adapter 代码/依赖隔离/in-cluster 支持）；新增 tests/test_lab_session_k3s_namespace_integration.py（session start→create/failure block/cleanup→delete/stuck terminating/profile stub 阻断）；新增 docs/labgen/DEPLOYMENT_PORTABILITY_STRATEGY_v0.1.md（A home-lab MVP reality/B deployment profiles dev+home_lab_mvp+cloud/C anti-lock-in rules/D migration path/E replaceable adapters/F config keys）；不连接真实 K3s/Proxmox/registry，无 LLM 调用，无 runtime start；2938 tests PASS；93.26% coverage
- feat(labgen): Staging Infra Bootstrap Execution v0.1 — Claude Code 作为 dev+ops 尝试 bootstrap 真实 staging infra；环境探测确认当前执行环境为生产 Proxmox 主机（pve）；`kubectl` 未安装，无 `~/.kube/`，无 staging K3s 集群；Proxmox 仅有 `k8s-netlab`/`containerlab`/`opsreplay` 三个生产 pool，无 staging-only pool，VMID 500-599 为生产范围；现有 registry（`registry-mirror` 172.16.100.1:5000）为生产 registry，无 staging 隔离；无 secret manager（vault/op/aws/doppler 均未安装）；无 `.env.staging`；final decision：STAGING_INFRA_BOOTSTRAP_BLOCKED；新增 `docs/labgen/STAGING_INFRA_BOOTSTRAP_BLOCKER_v0.1.md`（A Final Decision / B Operator / C Reason / D Missing Required Access 表格 9 行×6列 / E Why No Further Dev Work Should Proceed / F Exact Unblock Request 9 条可复制清单 / G Commands to Rerun After Unblock）；新增 `docs/labgen/STAGING_INFRA_BOOTSTRAP_EXECUTION_RESULT_v0.1.md`（A run metadata / B environment probe results 7项 / C per-infrastructure status 8行 / D per-ticket execution attempt 6行 / E secret handling result / F final decision / G technical self-check 7项 / H next step）；更新 `deploy/labgen/staging_ops_ticket_status.md`（Gate State 表格新增 staging infra bootstrap 行：STAGING_INFRA_BOOTSTRAP_BLOCKED）；无 runtime start，无 namespace 创建，无 VM 创建，无 secret 注入，无真实 secret 打印，无生产环境修改；所有 6 个 ops tickets 维持 BLOCKED_WITH_EVIDENCE 不变；LIVE_TRIAL_BLOCKED 维持不变
- feat(labgen): Staging Ops Ticket Execution v0.1 — Claude Code 作为 dev+ops 尝试执行全部 6 个 staging ops tickets；确认无真实 `.env.staging` 文件、无真实 staging infra/secrets；运行 `scripts/labgen_ops_ticket_verify.py --env-file deploy/labgen/.env.staging.example --all --json`（exit 1）确认 6/6 tickets BLOCKED；更新 `deploy/labgen/staging_ops_ticket_status.md`（全部 TODO→BLOCKED_WITH_EVIDENCE，新增 Evidence Path/Last Verification/Unblock Path）；新增 `docs/labgen/STAGING_OPS_TICKET_EXECUTION_RESULT_v0.1.md`（A run metadata / B ops env availability assessment / C ticket verification run + full JSON output / D per-ticket results 6×blocking reason+evidence+required action+rollback / E summary table / F downstream gate status / G final decision OPS_TICKETS_BLOCKED / H security assertions / I technical self-check / J unblock path / K no code changes required）；final decision：OPS_TICKETS_BLOCKED；secret injection NOT READY；intake gate NOT READY；live trial NOT PERMITTED；runtime start NOT EXECUTED；无 namespace 创建，无 VM 创建，无 verifier credential 创建，无 K3s/Proxmox/registry 连接，无真实 secret 处理，无 secret 打印；不新增 backend API，不新增 frontend 页面，不修改 Contract v0.1；LIVE_TRIAL_BLOCKED 维持不变
- feat(labgen): Ops Provisioning Ticket Pack v0.1 — 新增 6-ticket ops provisioning pack（OPS-K3S-001/OPS-AUTH-001/OPS-PROXMOX-001/OPS-PROXMOX-002/OPS-VM-001/OPS-REGISTRY-001）；新增 scripts/labgen_ops_ticket_verify.py（per-ticket 静态验证 wrapper：import verify_secret_injection 复用 per-key 分类逻辑；6 ticket → env key 映射；VERIFIED/BLOCKED 状态；exit 0/1/2；--all/--ticket/--json；无网络调用，无 secret 打印）；新增 tests/test_labgen_ops_ticket_verify.py（66 tests：per-ticket VERIFIED happy path / per-ticket BLOCKED missing+placeholder+empty（18 parametrized）/ format checks ADMIN_TOKEN/VM_REGISTRY_MIRROR / --all flag / exit codes / JSON schema stable / secret 值不打印（ssh credential/admin token/proxmox secret）/ no-network-calls / human output / TicketPackVerifyResult model）；新增 docs/labgen/OPS_PROVISIONING_TICKET_PACK_v0.1.md（A Current Blocked State / B Ticket Overview 表格 / C Per-ticket Details：6 tickets × Purpose/Required input/Source of truth/Injection mechanism/Security requirements/Validation command/Expected pass/Fail conditions/Rollback/Evidence/Notes / D Execution Sequence / E Verification Commands / F Security Assertions）；新增 deploy/labgen/staging_ops_ticket_status.md（6 tickets 状态跟踪表：Ticket ID/Owner/Status TODO/Env Key/Evidence Path/Last Verification/Last Decision/Notes；Verification Commands 模板；Gate State 表格；Dependency Graph）；更新 docs/labgen/STAGING_OPS_HANDOFF_v0.1.md（B scripts table 新增 ticket verify wrapper；I checklist 新增 I-16b ticket pack verification step）；更新 docs/labgen/OPS_SECRET_INJECTION_VERIFICATION_RESULT_v0.1.md（H Next Steps 新增 ticket pack 引用）；更新 docs/labgen/OPS_STAGING_INTAKE_VERIFICATION_RESULT_v0.1.md（H Next Steps 新增 ticket pack 引用）；更新 deploy/labgen/staging_infrastructure_checklist.md（Phase 6 新增 V-0c ticket pack verify；Phase 7 新增 G-0c gate；helper table 新增 ticket verify wrapper）；不执行 runtime start，不连接 K3s/Proxmox/registry，不打印 secret，不新增 backend API；live trial 维持 LIVE_TRIAL_BLOCKED
- feat(labgen): Ops Secrets Injection Verification v0.1 — 新增 scripts/labgen_ops_secret_injection_verify.py（ops-facing 逐 key 注入状态验证工具：检查 7 个 required staging secret keys；状态标签 PRESENT_REDACTED/MISSING/PLACEHOLDER/EMPTY/INVALID_FORMAT；格式校验 ADMIN_TOKEN≥32chars/VM_REGISTRY_MIRROR http(s)://；production-marker 警告不阻断；决策 SECRET_INJECTION_READY/SECRET_INJECTION_BLOCKED；exit 0/1/2；无网络调用，无 secret 值打印，不读 kubeconfig 内容；import _parse_env_file from provisioning validator）；新增 tests/test_labgen_ops_secret_injection_verify.py（52 tests：例文件→BLOCKED / all ready→READY / MISSING / PLACEHOLDER acknowledged / PLACEHOLDER value / EMPTY / INVALID_FORMAT min32 / INVALID_FORMAT no-scheme / https:// passes / secret 值不泄露（human+JSON+kubeconfig path+VM password）/ production-marker warning 不阻断 / JSON schema stable / CLI exit codes / no network）；执行验证：deploy/labgen/.env.staging.example → SECRET_INJECTION_BLOCKED（6/7 keys 仍为 placeholder，PROXMOX_TOKEN_ID PRESENT_REDACTED）；新增 docs/labgen/OPS_SECRET_INJECTION_VERIFICATION_RESULT_v0.1.md（A metadata / B per-key status 7 rows / C final decision / D blocking keys deduplicated / E warnings / F security assertions / G unblock checklist G-1~G-7 / H next steps）；更新 deploy/labgen/staging_infrastructure_checklist.md（status 改为 SECRET INJECTION BLOCKED；Phase 6 新增 V-0b secret injection verify 行；Phase 7 新增 G-0b gate 行；helper table 新增 secret injection verify 行）；更新 docs/labgen/CONTROLLED_STAGING_TRIAL_LIVE_RUN_RESULT_v0.1.md（unblock checklist 新增 secret injection [x] 条目，维持 LIVE_TRIAL_BLOCKED）；更新 docs/labgen/OPS_STAGING_INTAKE_VERIFICATION_RESULT_v0.1.md（H Next Steps 第 3 步改为先跑 secret injection verify）；更新 docs/labgen/STAGING_OPS_HANDOFF_v0.1.md（新增 I-16a secret injection step；I-20 前置条件更新；Notes 更新 commit 引用）；不执行 runtime start，不连接 K3s/Proxmox/registry，不打印 secret，不新增 backend API，不新增 frontend 页面，live trial 维持 LIVE_TRIAL_BLOCKED
- feat(labgen): Staging Intake Gate Execution \& Live Trial Rerun Decision v0.1 — intake gate 已执行（2026-06-12），decision: BLOCKED_MISSING_INPUTS；6 个必填 staging secret 均为 placeholder（LABGEN_K8S_PLATFORM_KUBECONFIG_PATH / ADMIN_TOKEN / PROXMOX_HOST / PROXMOX_TOKEN_SECRET / VM_SSH_PASSWORD / VM_REGISTRY_MIRROR）；新增 docs/labgen/OPS_STAGING_INTAKE_VERIFICATION_RESULT_v0.1.md（A run metadata / B phase results Phase 0-5 / C final decision / D blocking issues deduplicated / E warnings / F security assertions / G unblock checklist / H next steps）；更新 deploy/labgen/staging_infrastructure_checklist.md（status 改为 INTAKE GATE BLOCKED）；更新 docs/labgen/CONTROLLED_STAGING_TRIAL_LIVE_RUN_RESULT_v0.1.md（intake gate 执行结果记入 unblock checklist，维持 LIVE_TRIAL_BLOCKED）；不执行 live trial rerun，不调 runtime start，不创建 namespace，不连接 K3s/Proxmox/registry，不打印 secret；docs-only + result artifact；pre-commit PASS
- feat(labgen): Ops Staging Intake Verification Gate v0.1 — 新增 scripts/labgen_ops_staging_intake_verify.py（统一 pre-flight gate：orchestrate 4 个现有验证脚本，输出单一 READY_TO_RERUN_CONTROLLED_STAGING_TRIAL / BLOCKED_* 决策；Phase 0 env file 可读性；Phase 1 check_missing_inputs；Phase 2 validate_env_file；Phase 3 production preflight（env_context 注入）；Phase 4 dry_run offline；Phase 5 safe GET diagnostics（6 个 endpoint，forbidden path guard）；决策优先级 MISSING>CONFIG>SECRET_LEAK>UNREACHABLE>READY；secret 值永不打印，只报 key name；fail-closed：unreachable diagnostics 或疑似 secret 响应均为 BLOCKED；所有 sub-helpers 可注入，HTTP client 可注入；exit 0=READY，exit 1=BLOCKED，exit 2=invocation error）；新增 tests/test_labgen_ops_staging_intake_verify.py（34 tests：全决策路径/phase skip/secret 不泄露/JSON schema stable/单元测试）；新增 docs/labgen/OPS_STAGING_INTAKE_VERIFICATION_v0.1.md；更新 deploy/labgen/staging_infrastructure_checklist.md；更新 docs/labgen/STAGING_OPS_HANDOFF_v0.1.md；更新 docs/labgen/CONTROLLED_STAGING_TRIAL_LIVE_RUN_RESULT_v0.1.md；更新 docs/labgen/CONTROLLED_STAGING_TRIAL_v0.1.md；更新 docs/labgen/STAGING_ENVIRONMENT_PROVISIONING_v0.1.md；不新增 backend API，不新增 frontend 页面，不执行 runtime start，不连接 K3s/Proxmox/registry；≥92% coverage
- feat(labgen): Staging Ops Handoff Package v0.1 — 新增 ops 可执行交接包，将 LIVE_TRIAL_BLOCKED 原因转化为可操作任务；新增 docs/labgen/STAGING_OPS_HANDOFF_v0.1.md（A Current Status + blocked inputs 列表 + reference table；B What Dev Has Delivered：5 个脚本/测试数/fail-closed 保证；C What Ops Must Provide：infra/secrets/images 完整列表；D Required Secrets Inventory 表格：7 行 × 9 列（key/config/phase/owner/source/injection/validation/must-not-log/status）；E Minimal Provisioning Sequence：Phase 0-8 含 validation 命令；F Validation Commands Reference：static + live 两类命令模板，全部 placeholder；G Expected Pass Criteria：10 项验收条件；H Abort Criteria：8 条终止条件；I Handoff Checklist：20 行表格含 owner/due/evidence/validation/status；无真实 IP/secret/kubeconfig）；新增 scripts/labgen_staging_missing_inputs.py（ops-facing missing inputs summary wrapper：import _parse_env_file + _PLACEHOLDER_RE from provisioning validator；6 个 InputGroup：k3s/auth_admin/proxmox/vm_access/registry/storage；8 个 blocking keys；_is_input_ready()：absent/empty/pure-placeholder/embedded-placeholder 均为 missing；MissingInputsResult to_dict()；human_output / json_output；exit 0=no missing / 1=blocking / 2=file-error；--env-file/-e/--json/--quiet；不连接网络，不打印 secret 值）；新增 tests/test_labgen_staging_missing_inputs.py（65 tests：A staging example 6 missing / B complete fake env 0 missing / C placeholder/commented/embedded/absent reasons / D empty string missing / E secret value never in output（result dict/human/json/key name）/ F JSON schema stable（top-level/overall/missing_count/groups/items structure）/ G grouping correctness（per-group key placement/empty groups/count sum）/ H exit codes（0/1/2）/ I no network calls；_is_input_ready 单元测试）；更新 deploy/labgen/staging_infrastructure_checklist.md（header 状态标记 FIRST LIVE RUN BLOCKED；新增 Blocking Items from First Live Run 快速参考表 + missing inputs helper 命令；Phase 5 新增 Required before rerun? + Secret injected? 两列；Phase 6 新增 V-0 missing inputs helper + Validated by script? 列；Phase 7 新增 G-0 gate；Maps to helpers 表格新增 missing inputs helper 行）；更新 4 个现有文档（CONTROLLED_STAGING_TRIAL_v0.1.md / STAGING_ENVIRONMENT_PROVISIONING_v0.1.md / PRODUCTION_DEPLOYMENT_PREP_v0.1.md / CONTROLLED_STAGING_TRIAL_LIVE_RUN_RESULT_v0.1.md）新增 ops handoff + missing inputs helper 引用；live trial 状态维持 LIVE_TRIAL_BLOCKED（不声明 passed / production-ready-live）；2693 tests PASS / 94.08% coverage
- feat(labgen): Controlled Staging Trial Live Run v0.1 — 执行首次 controlled staging live run；全部工具正常运行（fail-closed，无伪成功）；最终决策 LIVE_TRIAL_BLOCKED（staging 环境未 provisioned，缺失 4 项 secrets：kubeconfig / ADMIN_TOKEN / PROXMOX_TOKEN_SECRET / VM_SSH_PASSWORD）；新增 docs/labgen/CONTROLLED_STAGING_TRIAL_LIVE_RUN_RESULT_v0.1.md（A Run Metadata：commit/date/operator/tools/no live runtime actions；B Preconditions Result：B-1/B-2/B-3/B-4/B-11/B-12 BLOCKED，B-5/B-6/B-7/B-8/B-9/B-10/B-13 PASS；C Phase Results：Phase 0 provisioning validation WARNING，Phase 1 production preflight BLOCKING，Phase 2 staging dry run BLOCKING，Phase 3 safe diagnostics BLOCKING，Phase 4-10 NOT EXECUTED；D Final Decision：LIVE_TRIAL_BLOCKED；E Evidence：secret leakage PASS / namespace lifecycle summary / credential reclaim summary / no audit events；F Follow-up Items：7 个 blockers + unblock checklist；G Quality Gate：2628 tests PASS / 94.08% coverage）；更新 CONTROLLED_STAGING_TRIAL_v0.1.md Current Status（记录首次 live run 日期 + BLOCKED 状态 + result artifact 引用）；更新 STAGING_ENVIRONMENT_PROVISIONING_v0.1.md 头部状态（记录 LIVE_TRIAL_BLOCKED）；更新 PRODUCTION_DEPLOYMENT_PREP_v0.1.md Section J Step 2（记录 live run result + 7 项 blocking inputs）；所有脚本 fail-closed，无伪 success，无 secret 泄露，无真实 K3s/Proxmox/registry 调用
- feat(labgen): Staging Environment Provisioning Plan v0.1 — 新增 docs/labgen/STAGING_ENVIRONMENT_PROVISIONING_v0.1.md（A scope/B required infrastructure/C K3s requirements（RBAC/namespace/rolebinding/kubeconfig injection）/D Proxmox requirements（pool/VMID/expiry/taint）/E image registry requirements（whitelist/readiness/credentials）/F storage requirements（persistence/backup/credential root）/G secrets and config（全 placeholder，区分 required/disabled/conditional）/H network and security boundaries（backend→K3s/registry/Proxmox/frontend/no-public-admin/egress-LLM-disabled）/I provisioning checklist 14 rows/J trial unblock criteria 10 items；引用 provisioning validator）；新增 deploy/labgen/staging_infrastructure_checklist.md（可填写表格；Phase 0-7 共 44 项：prerequisites/K3s/Proxmox/image registry/storage/secrets/validation/trial gate；Item/Owner/Required/Config Key/Validation Command/Status/Result/Notes；无真实 secret，全 placeholder；映射到 .env.staging.example；映射到 dry run/trial helper check 项）；新增 scripts/labgen_staging_provisioning_validate.py（静态 env file 校验器：解析 active KEY=VALUE + acknowledged `# KEY=<placeholder>` 注释；9 项检查：required active keys / namespace adapter not stub / LLM live disabled / demo seed disabled (demo mode) / credential root safe / session TTL valid / runtime mode valid / secret keys not active / real secret patterns (sk-ant-/sk-proj-/-----BEGIN/JWT) ；fail-closed；不连接 K3s/Proxmox/registry；不读 secret 文件内容；不打印 secret 值；JSON + text 两种 output；exit 0=pass / 1=blocking / 2=file-not-found；--env-file/-e/--json/--quiet 四个 flag）；新增 tests/test_labgen_staging_provisioning_validate.py（82 tests：parser/template-passes/required-keys/namespace-adapter/llm-live/demo-seed/credential-root/session-ttl/secret-not-active/real-secret-pattern/runtime-mode/JSON-schema/CLI/no-network-calls；secret 值不在任何 message/JSON/human output 中出现；网络调用 socket patch 验证无网络请求）；更新 docs/labgen/STAGING_DEPLOYMENT_DRY_RUN_v0.1.md Section G next step（新增 provisioning plan Step 1 引用）；更新 docs/labgen/CONTROLLED_STAGING_TRIAL_v0.1.md（header commit 更新为 fd544e3，trial status table 新增 provisioning plan/checklist/validator 三行，live execution 前置条件更新为"provisioning checklist Phase 7 gate 完成"）；更新 docs/labgen/PRODUCTION_DEPLOYMENT_PREP_v0.1.md Section J（新增 Step 1 provisioning plan + provisioning validator 引用）；不连接真实 K3s/Proxmox/registry；不推送镜像；不创建真实 secret；不新增 backend API；不新增 runtime 状态；不新增 audit event type；2546 passed baseline；safety-reviewer PASS；Codex PASS
- feat(labgen): Controlled Staging Trial v0.1 — 新增 docs/labgen/CONTROLLED_STAGING_TRIAL_v0.1.md（A scope/B preconditions 13 项/C trial phases Phase 0-8/D pass-fail criteria 10 rows/E evidence collection template/F abort+rollback plan；明确边界：TOOLING_READY，live execution BLOCKED by missing staging environment，不声明 production live）；新增 deploy/labgen/staging_trial_checklist.md（空白模板，含 precondition sign-off + Phase 0-8 各阶段 check table，无真实 secret/kubeconfig/cluster token）；新增 scripts/labgen_controlled_staging_trial.py（复用 dry run helper + preflight；phases: preflight/diagnostics/runtime/expiry/cleanup；默认 diagnostics-only 无 POST；--allow-runtime-start/--allow-timeout-expiry/--allow-cleanup-check 三个 runtime flag 分别 gate POST；adapter_safety_gate：production_safe=false 或 stub → blocking；llm_live_gate：live_enabled=true → blocking；runtime start gated by both gates；expiry 只用 dry_run=true；POST 响应过 sensitive scanner；HttpGetFn+HttpPostFn 均可注入；不打印 secret；--staging-lab-draft-id/--staging-session-id 缺失时 fail with clear message；--staging-user-session 通过 env var；execution_mode 字段写入 JSON output）；新增 tests/test_labgen_controlled_staging_trial.py（58 tests：A default-mode-no-POST/B no-destructive-without-flags/C runtime-start-flag/D missing-required-args/E secret-leak-detection/F unsafe-adapter-blocking/G llm-live-blocking/H JSON-schema-stable/I HTTP-client-injection/J expiry-phase/K cleanup-phase/L no-secrets-printed/M env-context-integration）；更新 STAGING_DEPLOYMENT_DRY_RUN_v0.1.md Section G next-step 引用；更新 PRODUCTION_DEPLOYMENT_PREP_v0.1.md Section J controlled staging trial；更新 PRODUCTION_READINESS_RC_v0.1.md Section I trial tooling status；tooling ready，live execution blocked pending staging K3s + Proxmox + registry + storage inputs；2484 passed baseline
- feat(labgen): Staging Deployment Dry Run v0.1 — 新增 docs/labgen/STAGING_DEPLOYMENT_DRY_RUN_v0.1.md（A scope/B env assumptions/C checklist 10 phases/D pass-fail criteria/E result template/F rollback criteria；明确边界：非 production deployment、非 K3s E2E、非 live traffic）；新增 deploy/labgen/.env.staging.example（由 production example 派生，staging-safe placeholder values，SESSION_COOKIE_SECURE=false 可接受警告）；新增 scripts/labgen_staging_dry_run.py（复用 preflight；支持 --env-file / --base-url / --json / --allow-demo-seed-check；6 项安全 GET probe + sensitive response scan（sk-ant-/sk-proj-/-----BEGIN/kubeconfig fields）；demo seed 仅 GET 验证 gate 不运行 seed；不调 destructive endpoints；不调 real LLM；admin token 不打印；HttpGetFn injectable；offline/online 两模式）；新增 tests/test_labgen_staging_dry_run.py（51 tests：env parsing/missing file/preflight integration/JSON schema stability/no secrets printed/safe GET only/destructive endpoints never called/sensitive scan/network error handling/env file integration）；更新 PRODUCTION_DEPLOYMENT_PREP_v0.1.md Section I staging dry run 引用；更新 PRODUCTION_READINESS_RC_v0.1.md Section H staging dry run 里程碑；2484 passed；94.08% coverage；safety-reviewer PASS；Codex PASS
- feat(labgen): Production Deployment Preparation v0.1 — LabGen 进入"可准备外部部署"阶段（RC_READY_WITH_NOTES 不变）；新增 docs/labgen/PRODUCTION_DEPLOYMENT_PREP_v0.1.md（A 部署范围/B 必要服务/C 环境配置/D fail-closed 保证矩阵/E 部署 checklist 10 阶段/F 运维 runbook/G 回滚计划/H 非阻断 gap 说明）；新增 deploy/labgen/.env.production.example（带注释占位符 template，无真实 secret）；新增 scripts/labgen_production_preflight.py（只读静态 config 检查器：12 项检查，production+stub→blocking，credential root 相对路径→blocking，/tmp 路径→blocking，live LLM→warning，ADMIN_TOKEN 长度/存在→blocking；--json 模式；exit 0=pass，nonzero=blocking；无网络调用，无 secret 输出）；新增 tests/test_labgen_production_preflight.py（48 tests：production-like config pass / stub-in-production blocking / invalid mode/adapter / unsafe credential roots / live LLM warning / demo seed gated / secret values not printed / no network calls / JSON schema stable / config TTL fail-closed）；RC test file 扩展 Section H admin diagnostics consistency（+19 tests：adapter-status/llm-status/contract-pack/expire-sessions admin-only/200/no-secrets/required-fields/dry-run-no-mutation）；config safety bug fix：LABGEN_VERIFIER_CREDENTIAL_ROOT 和 LABGEN_LAB_SESSION_TTL_MINUTES 从硬编码改为可配置 env var（credential root 接入 VerifierCredentialStore in routes.py + vm_manager.py；TTL 接入 VMExpiryService in routes.py；TTL<1 在 config.py 启动时 fail-closed）；更新 docs/labgen/PRODUCTION_READINESS_RC_v0.1.md（Section G 记录部署准备里程碑）；更新 docs/labgen/MVP_ENGINEERING_CONTRACT_AUDIT_v0.1.md（Update banner）；preflight tests 48/RC tests 67 全部通过；safety-reviewer PASS
- feat(labgen): Production Readiness RC Gate v0.1 — RC verdict RC_READY_WITH_NOTES；新增 docs/labgen/PRODUCTION_READINESS_RC_v0.1.md（A 基线/B blocker closure matrix/C non-blocking gap 分析/D RC 决策/E smoke test 覆盖/F 安全摘要）；更新 docs/labgen/MVP_ENGINEERING_CONTRACT_AUDIT_v0.1.md（Gap 1-4 标记 DONE，补充 closure commit/evidence/tests，Summary 新增 production blockers closed 段落，§11/§12 里程碑行更新为已完成状态）；新增 tests/test_labgen_production_readiness_rc.py（46 tests；A admin read/B publish pipeline/C learner catalog/D session lifecycle/E expiry dry-run/F admin-only enforcement/G LLM output safety；不调真实 K3s/LLM/外部网络；sensitive data guard on all response types）；2363 passed；94.09% coverage；safety-reviewer PASS；Codex PASS
- feat(labgen): LAB_TIMEOUT VM Tracker Expiry Integration v0.1 — Gap 4 修复：LAB_TIMEOUT 接入 vm_tracker expiry；新增 backend/labgen/vm_expiry.py（VMExpiryIssue/VMExpiryResult Pydantic 模型；VMExpiryService：find_expired_sessions() 基于 session.started_at + TTL 纯读检测，expire_sessions() 遍历执行 timeout_session() + 收集结果，fail-closed per-session（单个异常不中断批量），injectable clock 支持确定性测试，dry_run=True 不改变状态；response 不返回 kubeconfig/namespace/credential/raw exception/traceback；DEFAULT_SESSION_TTL_MINUTES=30）；LabSessionService 新增 timeout_session()（LAB_ACTIVE→LAB_TIMEOUT→_do_cleanup()；failure_reason=lab_timeout 在 success path 保留，cleanup failure path 由 NAMESPACE_CLEANUP_FAILED 覆盖；idempotent：已在 _ALREADY_ENDED_STATES 直接返回；不新增 audit event type，cleanup 结果通过现有 CLEANUP_SUCCESS/CLEANUP_FAILED/VM_TAINTED 表达；不误标为 LAB_ABORTED/LAB_COMPLETED）；新增 _ALREADY_ENDED_STATES frozenset；LabSessionRepository 新增 list_all()；FailureReason 新增 LAB_TIMEOUT stable 机器码；learner_session_snapshot.py _build_session_issues() 新增 LAB_TIMEOUT issue（failure_reason=lab_timeout 时显示 safe 超时提示）；POST /api/labgen/runtime/expire-sessions（admin-only，ExpireSessionsRequest: dry_run/limit；ExpireSessionsResponse: expired/cleaned/failed/tainted IDs + issue_count + checked_at；不返回内部敏感字段）；get_expiry_service() dep factory；api_contract.py 新增 expire-sessions 端点 + note；新增 tests/test_labgen_vm_expiry.py（A 检测/B 成功/C 清理失败/D limit+dry_run/E 边界，共 30 tests；vm_expiry.py 100% coverage）；新增 tests/test_labgen_lab_timeout_integration.py（A 状态机/B cleanup/C audit/D endpoint/E snapshot/F contract/G regression，共 44 tests）；production blockers 全部关闭（Gap 1-4 已完成）；2317 passed；94.00% coverage
- feat(labgen): Runtime Precheck Conditions 4+6 Hardening v0.1 — Gap 3 修复：补齐 Contract §11 VM_PRECHECK_RUNNING conditions 4（前一个 namespace 处于 Terminating 状态超过 5 分钟）和 6（前一个 lab 声明了 cluster_scoped_resources 且 cleanup_verified=False）；新增 backend/labgen/runtime_precheck.py（RuntimePrecheckCondition stable enum: PRIOR_NAMESPACE_TERMINATING / PRIOR_CLUSTER_SCOPED_NOT_CLEANED；RuntimePrecheckStatus: PASSED/BLOCKED；RuntimePrecheckIssue: code/message/severity/condition/source/path；RuntimePrecheckResult: status/passed_conditions/blocked_conditions/issues/checked_at；_sanitize_message() 剥离 Bearer/JWT/base64/api_key/password/private_key/traceback；RuntimePrecheckService.check() 聚合全部 condition 结果；fail-closed：任意 condition 异常返回 BLOCKED 安全 issue，不暴露 exception message/traceback/kubeconfig）；NamespaceLifecyclePort 新增抽象方法 is_namespace_stuck_terminating()（检查 K8s namespace 是否处于 Terminating 超过 threshold_seconds）；StubNamespaceLifecycleAdapter 新增 stuck_terminating_namespaces 可配置 set 参数；K3sNamespaceLifecycleAdapter 骨架新增对应 NotImplementedError；FailureReason 新增 3 个 stable 机器码：RUNTIME_PRECHECK_FAILED / RUNTIME_PRECHECK_CONDITION_4_FAILED / RUNTIME_PRECHECK_CONDITION_6_FAILED；LabSessionService.__init__ 新增可选 runtime_precheck 参数；create_session() 在 run_precheck() 之后、image check 之前调用 RuntimePrecheckService.check()：BLOCKED→创建 LAB_START_FAILED session（failure_reason=单条时用具体 code，多条用 RUNTIME_PRECHECK_FAILED）+ 安全 LAB_START_FAILED audit event（metadata 仅含 blocked_conditions/issue_codes，无 kubeconfig/credential/traceback/raw exception）+ 不创建 namespace/rolebinding/verifier credential/不触发 VM init；runtime_precheck=None 时向后兼容跳过检查；routes.py get_session_service() 注入 RuntimePrecheckService；新增 tests/test_labgen_runtime_precheck.py（A Condition4/B Condition6/C Multi+Schema/D Sanitizer，共 ~42 tests）；新增 tests/test_labgen_runtime_start_precheck.py（B Start integration/C Safety/D Regression，共 ~25 tests）；production blockers 仅余 Gap 4（LAB_TIMEOUT 接入 vm_tracker expiry）；safety-reviewer PASS；≥92% coverage
- feat(labgen): Verifier Credential Reclaim Hardening v0.1 — Gap 2 修复：VM/session reclaim 时 verifier credential 文件现在被强制删除；新增 VerifierCredentialReclaimer（verifier_credentials.py）：reclaim_for_vm() 实现 vm_id numeric 校验 + resolve 边界检查（确保路径在 credential root 内）+ symlink 拒绝 + missing dir 幂等成功 + OSError 结构化 failure（不泄露 path/content/raw exception）；新增 CredentialReclaimResult dataclass；LabSessionService.__init__ 新增可选 credential_reclaimer 参数（None = 向后兼容，现有测试不受影响）；_do_cleanup() 重构为两阶段：Phase 1 namespace deletion（原有行为保留）+ Phase 2 credential reclaim（credential 删除失败 → LAB_CLEANUP_FAILED + mark_vm_tainted，与 namespace cleanup failure 相同 policy）；两阶段均成功才设 LAB_CLOSED + cleanup_verified=True；namespace failure 优先于 credential failure 作为 session failure_reason；audit metadata 仅含 cleanup_phase="verifier_credential_reclaim"（不含 path/content/kubeconfig/token/traceback）；failure_reason 使用稳定机器码：VERIFIER_CREDENTIAL_RECLAIM_FAILED / VERIFIER_CREDENTIAL_PATH_UNSAFE / VERIFIER_CREDENTIAL_DELETE_FAILED（新增至 FailureReason enum）；routes.py get_session_service() 注入 VerifierCredentialReclaimer(VerifierCredentialStore())；no-namespace session 也执行 credential reclaim；learner snapshot LAB_CLEANUP_FAILED 显示 CLEANUP_FAILED issue + safe failure_reason（无 credential path 泄露）；新增 tests/test_labgen_verifier_credential_reclaim.py（A reclaimer unit/B complete integration/C abort integration/D no-namespace/E audit/F learner snapshot/G regression，35 tests）；safety-reviewer PASS；≥92% coverage
- feat(labgen): Production Runtime Adapter Selection Hardening v0.1 — Gap 1 修复：StubNamespaceLifecycleAdapter 不再是 production 默认值；新增 backend/labgen/runtime_adapter_selection.py（RuntimeMode enum: test/dev/demo/production；NamespaceAdapterKind enum: stub/k8s；RuntimeAdapterSelectionIssue/RuntimeAdapterSelectionResult dataclass；RuntimeAdapterSelectionService.select() 纯函数（无 K8s 调用，无网络，无 kubeconfig 读取）：production+stub→STUB_ADAPTER_IN_PRODUCTION blocking；production+k8s+无 kubeconfig→K8S_ADAPTER_NOT_CONFIGURED blocking；production+k8s+kubeconfig→production_safe=True；dev/test/demo+stub→NON_PRODUCTION_STUB_ALLOWED warning；非法 mode/kind→INVALID_* blocking；build_adapter() 返回对应 adapter 实例，不校验安全性（守卫在 create_session）；create_from_config() 从 config 模块读取）；backend/config.py 新增 LABGEN_RUNTIME_MODE（default: dev）/ LABGEN_NAMESPACE_ADAPTER（default: stub）/ LABGEN_K8S_PLATFORM_KUBECONFIG_PATH（default: 空）；backend/labgen/failure_reasons.py 新增 ADAPTER_UNSAFE_IN_PRODUCTION；backend/labgen/lab_session_service.py LabSessionService.__init__ 新增 adapter_selection 可选参数，create_session() 守卫：任意 blocking adapter-selection issue（含 INVALID_RUNTIME_MODE/INVALID_ADAPTER_KIND/STUB_ADAPTER_IN_PRODUCTION/K8S_ADAPTER_NOT_CONFIGURED）立即返回 LAB_START_FAILED，不执行 precheck/namespace/rolebinding/verifier credential 创建，不暴露 config 细节，产生 LAB_START_FAILED audit event（修复 codex P1：之前仅检查 production mode，LABGEN_RUNTIME_MODE=prod 拼写错误产生 INVALID_RUNTIME_MODE blocking 但 fallback 到 dev mode，守卫不触发，现改为检查任意 blocking issue 以覆盖所有非法配置）；backend/labgen/routes.py get_session_service() 改用 RuntimeAdapterSelectionService.create_from_config() 选取 adapter 并注入 selection result（旧 StubNamespaceLifecycleAdapter() 硬编码已删除）；新增 GET /api/labgen/runtime/adapter-status（admin-only，只读，不创建 namespace/rolebinding/session/audit event，不返回 kubeconfig/service_account_token/cluster_endpoint_secret/raw_env_value/namespace_credential/stack_trace，返回 runtime_mode/namespace_adapter_kind/production_safe/issues/warnings/checked_at）；backend/labgen/api_contract.py 新增 runtime/adapter-status 端点 + sensitive_field_policy 补充 namespace_credential/runtime_adapter_config/cluster_endpoint_secret/service_account_token/kubeconfig_content/raw_env_value + runtime_adapter_status_dev_stub example + runtime adapter notes；labgenClient.js 新增 PATHS.runtimeAdapterStatus + getAdapterStatus()；labgenViews.js 新增 renderAdapterStatus()（production unsafe 显示 warning banner，non-prod stub 显示提示，issue list 含 code/severity/message，不渲染 kubeconfig/token/raw env）；labgen-dev.html 新增 Runtime Adapter Status 区块（admin-only，Fetch Status 按钮）；新增 tests/test_labgen_runtime_adapter_selection.py（A selection logic/B build_adapter/C severity/D config/E guard integration，共 ~37 tests）；新增 tests/test_labgen_runtime_adapter_routes.py（A HTTP endpoint/B response safety/C regression，共 ~25 tests）；更新 contract pack endpoint count: 17→18，example count: 18→19；safety-reviewer PASS；codex PASS（含 P1 修复）；≥92% coverage
- fix(hooks): 排除 tests/ 目录的 API key 扫描——测试 fixture 中的占位符密钥（fake/test/mock）不再触发 pre-commit/pre-push 安全检查误报
- feat(labgen): Real LLM Provider Adapter v0.1 — 新增 backend/labgen/llm_openai_compatible.py（OpenAICompatibleProviderConfig / validate_openai_compatible_config：base_url 校验 https/blocked scheme/空 host；OpenAICompatibleLLMProviderAdapter：injectable HTTP client，call_generate() 构造 chat completions payload，严格处理 401/429/500/timeout，不返回 api_key/Authorization/raw HTTP body；parse_provider_response()：只接受 JSON object，可选提取 fenced code block，拒绝 natural language，strip prohibited fields，force publish_status=draft；build_openai_compatible_adapter_from_env() 工厂）；新增 backend/labgen/llm_prompt_builder.py（build_generation_messages/build_repair_messages：JSON-only 系统指令，Contract v0.1 安全摘要，禁止 chain-of-thought/secrets/kubeconfig/publish 请求；sanitize_user_input 截断 + 禁用 pattern 消除；prompt builder 输出不进 API response 不进 log）；backend/labgen/llm_provider_boundary.py 新增 LLMProviderMode.LIVE_ENABLED + LLMProviderBoundaryService.live_enabled 属性 + _live_response()（调 OpenAICompatibleLLMProviderAdapter，OpenAICompatibleProviderError 捕获转 rejected_reason，不 500）+ live_adapter_config_issues；backend/labgen/llm_generation.py _generate_result() 扩展：boundary_active 包含 live_enabled 条件，live provider failure 不创建 draft 不产生 runtime audit event；backend/config.py 新增 LABGEN_LLM_OPENAI_BASE_URL / LABGEN_LLM_OPENAI_MODEL / LABGEN_LLM_OPENAI_API_KEY（默认空，不记录 api_key 值）；routes.py LLMProviderStatusResponse 新增 configured_model/base_url_origin/generation_supported/repair_supported/config_issues 字段（repair_supported=False OUT_OF_SCOPE v0.1）；POST /api/labgen/llm-provider/validate-config（admin-only，local 校验，不发网络请求，不验证 api_key 真实有效性，不返回 api_key 值）；api_contract.py 新增 validate-config 端点 + llm_provider_status_live_enabled + llm_provider_validate_config_invalid 两个 example + sensitive_field_policy 补充 authorization/authorization_header/provider_raw_response/prompt_text/hidden_prompt；labgenClient.js 新增 PATHS.llmProviderValidateConfig + validateProviderConfig()；labgenViews.js renderLLMProviderStatus 扩展：live_enabled badge/generation+repair badges/config_issues block/configured_model+base_url_origin 行（不渲染 api_key/raw response/Authorization header）；labgen-dev.html 新增 Validate Config 区块（admin-only，仅显示 config_issues，不显示 api_key）；新增 tests/test_labgen_llm_prompt_builder.py（42 tests）；新增 tests/test_labgen_openai_compatible_provider.py（52 tests：config validation/parse_provider_response/adapter call）；新增 tests/test_labgen_llm_provider_live_config.py（42 tests：live mode switching/generation integration/status API/validate-config API/contract pack）；更新 tests/test_labgen_llm_provider_routes.py（schema + example count）；safety-reviewer PASS；≥92% coverage
- docs(labgen): Contract v0.1 gap audit — 新增 docs/labgen/MVP_ENGINEERING_CONTRACT_AUDIT_v0.1.md（全量审计报告：B 里程碑矩阵 / C API 矩阵 / D 安全矩阵 / E 8 个剩余 gap 清单 / F 下一步建议；DONE 35 / PARTIAL 5 / MISSING 2 / OUT_OF_SCOPE 4）；fix(tests): 修复 tests/frontend/test_views.mjs 中 BLOCKED_DECISION fixture 使用已废弃的 blocked_reasons[] 字段（应为 decision.issues[] + severity:error），全量前端测试 118/118 通过
- feat(labgen): LLM Provider Boundary Hardening v0.1 — 新增 backend/labgen/llm_provider_boundary.py（LLMProviderName enum: FAKE/OPENAI/ANTHROPIC/GEMINI/CUSTOM；LLMProviderMode enum: DISABLED/FAKE_ONLY/DRY_RUN/LIVE_DISABLED；LLMProviderConfig 无 API key 字段；LLMProviderRequest/Response/Error/SafetyPolicy；DryRunLLMProviderAdapter 7 种注入模式：valid_candidate/invalid_candidate/malformed/timeout/provider_disabled/safety_rejection/sensitive_output，不调网络，不读 API key；LLMProviderBoundaryService.call() 按 mode dispatch，FAKE_ONLY→模板路径，DRY_RUN→dry_run_adapter，LIVE→permanently disabled；create_from_env() 工厂，非法 mode/name 安全 fallback；_redact/_redact_warnings 清除 Bearer/JWT/base64/api_key/traceback；raw_output_available 恒为 False）；backend/config.py 新增 LABGEN_LLM_PROVIDER_MODE/NAME/TIMEOUT_MS/MAX_OUTPUT_TOKENS（默认 fake_only/fake/30000/4096，上限 clamped）；backend/labgen/llm_generation.py LabDraftGenerationService 新增可选 provider_boundary 参数（_generate_result() 分发；DRY_RUN mode 时调用 boundary 获取 candidate_json，仍走 Pydantic+StaticValidator；DryRunTimeoutSimulated 转换为 DraftGenerationRejected；默认行为不变）；GET /api/labgen/llm-provider/status（admin-only，返回 LLMProviderStatusResponse：provider_name/mode/live_enabled=false/dry_run_available/timeout_ms/max_output_tokens/safety_policy_summary/warnings）；POST /api/labgen/llm-provider/dry-run（admin-only，inject_mode 白名单 7 项，不创建 draft，不持久化，DryRunTimeoutSimulated→timeout_simulated）；api_contract.py 新增两端点 + api_key/provider_trace_id/chain_of_thought 到 sensitive_field_policy + llm_provider_status + llm_provider_dry_run_disabled 两个 example + provider note；labgenClient.js 新增 PATHS.llmProviderStatus/llmProviderDryRun + getLLMProviderStatus()/runProviderDryRun()；labgenViews.js 新增 renderLLMProviderStatus()（mode badge/live_enabled/dry_run badge/safety summary/warnings；不渲染 API key/raw output/hidden prompt）；labgen-dev.html 新增 LLM Provider Status section（Fetch Status + Dry-Run inject_mode 选择器 + Run Dry-Run）；新增 tests/test_labgen_llm_provider_boundary.py（A Config/B Boundary/C DryRun/D Generation/E Sanitizer/F Request/G Regression，共 ~48 tests）；新增 tests/test_labgen_llm_provider_routes.py（D Status/D2 DryRun/E ContractPack/G Regression，共 ~36 tests）；新增 tests/frontend/test_llm_provider_dev_view.mjs（renderLLMProviderStatus + client methods + security，共 ~34 tests）；safety-reviewer PASS；≥92% coverage
- feat(labgen): Image Readiness Publish Gate Integration v0.1 — 新增 backend/labgen/image_readiness.py（ImageReadinessStatus enum：READY/BLOCKED/UNRESOLVED/NOT_FOUND/CHECK_FAILED/UNKNOWN；ImageReadinessIssue/ImageReadinessResult/ImageReadinessService；纯函数，不调网络，从 draft.image_resolution 推导状态；BLOCKED>UNRESOLVED>NOT_FOUND 优先级；checks_deferred 当 RESOLVED image existence_check_passed=None；exception → CHECK_FAILED 安全回退，不暴露 raw exception）；DraftPreviewSnapshot 新增 image_readiness: Optional[ImageReadinessResult] 字段，is_publishable 同时 gate on validator + image_readiness.status==READY；DraftPreviewService 接受 image_readiness_svc 依赖注入（None 时向后兼容）；PublishDecisionService 将 image_readiness.issues 映射到 decision issues（source=image_readiness），image readiness 阻断时追加 IMAGE_READINESS_BLOCKED 顶层摘要；PublishBlockReasonCode 新增 IMAGE_READINESS_BLOCKED；get_preview_service/get_decision_service 注入 ImageReadinessService()；api_contract.py 更新 preview example 增加 image_readiness 字段、新增 publish_decision_blocked_image example、sensitive_field_policy 新增 registry credential/token/auth 条目；labgenViews.js renderAdminDraftView 新增 Image Readiness 区块（status badge/counts/issues，无 registry URL/credential 暴露，learner pages 不含 admin-only image details）；新增 tests/test_labgen_image_readiness.py（A empty/B blocked/C unresolved/D not_found/E ready/F deferred/G mixed/H counts/I safety/J check_failed，共 40 tests）；新增 tests/test_labgen_image_publish_gate.py（A snapshot/B decision blocked/C decision allowed/D publish endpoint/E no force bypass/F learner privacy/G contract pack/H regression，共 42 tests）
- feat(labgen): Image API v0.1 — 新增 POST /api/images/resolve（批量解析镜像意图→内网 registry，admin-only，不运行 existence check）和 POST /api/images/check-existence（批量检查 resolved 镜像在内网 registry 是否存在，BLOCKED/UNRESOLVED 直接穿透，admin-only）；image_router 注册至 main.py；新增 tests/test_labgen_image_routes.py（A Auth guard / B resolve / C check-existence / D Contract 共 30 tests）
- feat(labgen): Demo Scenario Seed Pack v0.1 — 8 个确定性场景（A PYTHON_BASICS_PUBLISHED / B HTTP_API_BASICS_PUBLISHED / C DATA_TRANSFORM_IMAGE_BLOCKED_DRAFT / D PYTHON_IMAGE_UNRESOLVED_DRAFT / E HTTP_IMAGE_NOT_FOUND_DRAFT / F RUNTIME_ACTIVE_SESSION / G RUNTIME_READY_TO_COMPLETE_SESSION / H RUNTIME_CLEANUP_FAILED_TAINTED_SESSION）；backend/labgen/demo_seed.py：DemoSeedScenarioId enum / DemoSeedResult / DemoSeedRequest / DemoNextStep；DemoSeedService 不调 LLM/K8s/VM；image blocked 场景直接注入 ImageResolutionResult（BLOCKED/UNRESOLVED/NOT_FOUND），_validate_and_block_draft() 复用 StaticValidator 确保 publish_status=PUBLISH_BLOCKED；published 场景经 PublishService 完整 publish；runtime sessions 直接构造 LabSessionState 写入 repo；reset=True 只删 demo-owned IDs；upsert idempotency；不产生 kubeconfig/token/secret/password/raw_model_output；LabDraftRepository + LabSessionRepository 新增 delete() 方法；POST /api/labgen/demo/seed（admin-only，DEMO-ONLY 标注）；frontend: seedDemoData() + PATHS.demoSeed + renderDemoSeedResult() + labgen-dev.html Demo Scenario Entry Points 区块（READY/BLOCKED/UNRESOLVED/NOT_FOUND/Runtime Sessions 分类链接）；api_contract.py demo_seed_success example（13th）；新增 tests/test_labgen_demo_seed.py（含 TestImageReadinessScenarios 10 tests，共 ~52 tests）；新增 tests/test_labgen_demo_flow_smoke.py；新增 tests/frontend/test_demo_seed_view.mjs（8 scenarios + new IDs + checked_at，共 35 tests）；safety-reviewer PASS；≥92% coverage
- feat(labgen): Minimal Frontend Shell v0.1 — 新增 frontend/js/labgenSecurity.js（sanitizeDisplayText/escapeHtml/assertNoSensitiveDisplayData；JWT/base64/Bearer pattern redaction；严格 keyword blocklist；测试专用 assertNoSensitiveDisplayData 支持注入哨兵值验证）；frontend/js/labgenClient.js（LabGenClient ES module，15 方法全部对齐 contract pack 路径；LabGenApiError 结构化错误含 status/code/message/issues；credentials:same-origin，无 localStorage；mockFetch 注入接口供测试）；frontend/js/labgenViews.js（5 个纯渲染函数返回 HTML 字符串，全部 _safe()=escapeHtml+sanitizeDisplayText，无 DOM 副作用，可被 node:test 直接断言；不暴露 kubeconfig/verifier_credential/raw_model_output/hidden_prompt/provider_metadata；action_availability 严格控制按钮 disabled/enabled）；5 个 HTML 页面（labgen-admin/catalog/lab/session/dev.html；每页 /api/auth/me auth guard；admin 页额外 is_admin 检查；404 不暴露 unpublished 状态；dev 页标注 DEV-ONLY 不入生产导航）；backend/main.py 新增 5 个静态文件路由（与现有 admin/login.html 模式一致，路径不含用户输入，无 path traversal 向量）；frontend/package.json（type:module 供 node:test 加载 ESM）；tests/frontend/（node:test，59 tests：test_security.mjs 20/test_client.mjs 12/test_views.mjs 27；覆盖 PATHS 对齐/安全辅助函数/错误结构/publish-blocked issues/eligibility/action_availability/safety E2E 注入验证）；safety-reviewer PASS；1720 backend tests，94.27% coverage
- feat(labgen): Contract Pack response_model binding hardening — 新增两项 TestEndpointInventory 强约束测试：test_response_model_classes_exist_in_backend（每个 response_model 字符串必须解析为真实 Pydantic 类，防止模型重命名静默失效）；test_response_model_matches_openapi_schema_ref（response_model 必须与 FastAPI 实际注册的 OpenAPI $ref 一致，list[X] 类型自动剥离检查 items.$ref）；1720 tests，94.27% coverage
- feat(labgen): Frontend Integration Contract Pack v0.1 — 新增 backend/labgen/api_contract.py（ApiContractCategory/ApiContractEndpoint/ApiContractExample/SensitiveFieldPolicy/ApiContractPack 五个模型；build_contract_pack() 纯函数返回完整契约包，包含 14 个核心端点定义、11 个 deterministic golden examples、统一 sensitive field policy、known_deferred_runtime_checks 列表）；GET /api/labgen/contract-pack（admin-only，只读，不 publish/start/create session/audit event）；tests/labgen_contract_helpers.py（assert_contract_response_safe helper，覆盖 JWT/base64/token/secret/credential 泄漏检测，对齐现有 mvp_runtime_smoke 规则）；新增 tests/test_labgen_api_contract_pack.py（A contract pack API / B endpoint inventory / C OpenAPI sanity / D golden examples / F regression，共 ~52 tests）；新增 tests/test_labgen_frontend_contract_smoke.py（E 全链路 smoke：generate→preview→publish decision→publish→catalog→eligibility→start→snapshot→step check→snapshot→complete→audit events，每步 assert_contract_response_safe，共 ~13 tests）；safety-reviewer 待审
- feat(labgen): Learner Runtime Session Snapshot API v0.1 — 新增 backend/labgen/learner_session_snapshot.py（LearnerSessionIssue/LearnerSessionCheckSummary/LearnerSessionStepStatus/LearnerSessionRuntimeSummary/LearnerSessionActionAvailability/LearnerSessionSnapshot/LearnerSessionListItem 七个模型；LearnerSessionSnapshotService.list_my_sessions/build_snapshot 两方法；只读：不创建 session/step check/complete/abort/cleanup/audit event；owner 或 admin 可访问，stranger 403；session 不存在 404；全部字符串经 sanitize_text 处理；不暴露 namespace/vm_id/kubeconfig/verifier credential/raw exception；step status 从 completed_step_ids/current_step_index 推导（passed/available/locked）；check_summary 仅对 LAB_ACTIVE session 的当前 step 显示，verify_id 匹配防止跨 step 泄漏；action_availability 从现有服务层规则推导 can_check/can_complete/can_abort；failure_reason 使用 FailureReason 稳定机器码；cleanup_failed 时设 tainted=True + CLEANUP_FAILED issue）；GET /api/lab-sessions（已认证用户，返回自己 session 的 list[LearnerSessionListItem]，支持 status/lab_id 过滤）；GET /api/lab-sessions/{id}/snapshot（owner 或 admin，返回 LearnerSessionSnapshot）；fix(sanitizer): sanitize_text 的 traceback 正则增加单行变体覆盖（原模式要求 newline 后才匹配，单行 traceback 会泄漏）；safety-reviewer PASS（MEDIUM 均为 pre-existing：404/403 oracle 与已有 get_lab_session 一致，session_repo 创建模式与 catalog service 一致）；新增 tests/test_labgen_learner_session_snapshot.py（57 tests：A session list/B snapshot happy path/C step progression/D completion availability/E cleanup failure/F permissions/G safety/H regression + service unit tests）；1654 tests，94.15% coverage
- feat(labgen): Learner Lab Catalog & Start Eligibility API v0.1 — 新增 backend/labgen/learner_catalog.py（LearnerLabCatalogItem/LearnerLabDetail/LearnerLabStepPreview/LearnerLabEligibility/LearnerLabEligibilityIssue 模型；LearnerCatalogService.list_published_labs/get_published_lab_detail/evaluate_start_eligibility 三方法；只暴露 PUBLISHED labs，missing 和 unpublished 统一 404 不泄露草稿存在；全部字符串含 step_id 经 sanitize_text 处理；evaluate_start_eligibility 只读：不创建 session/audit event/namespace/VM 操作；published_at=None 避免暴露 admin 编辑时间；validator_results 为空时视为 VALIDATION_FAILED 而非静默重跑）；新增 GET /api/labs（返回 list[LearnerLabCatalogItem]）、GET /api/labs/{lab_id}（返回 LearnerLabDetail 含 start_eligibility）、GET /api/labs/{lab_id}/start-eligibility（返回 LearnerLabEligibility）三端点，均要求登录、admin 也返回 learner-safe 视图；get_catalog_service per-request factory 确保 test override 传播；EligibilityIssueCode 覆盖 VALIDATION_FAILED/IMAGE_STATUS_UNKNOWN/RUNTIME_CHECKS_DEFERRED/SESSION_ALREADY_ACTIVE；is_startable=true 时追加 RUNTIME_CHECKS_DEFERRED warning（image TTL recheck 延迟至 start 时）；LabDraftRepository 新增 list_all()；safety-reviewer PASS（2 MEDIUM 已修复：step_id sanitize + published_at 移除 admin 编辑时间；1 LOW 已修复：空 validator_results → blocking）；新增 tests/test_labgen_learner_catalog.py（57 tests：A catalog list/B detail/C eligibility/D permissions/E safety/F regression/G service unit）；1597 tests，94.09% coverage
- feat(labgen): Admin Publish Decision Gate v0.1 — 新增 backend/labgen/publish_decision.py（PublishDecisionStatus/PublishBlockReasonCode/PublishDecisionIssue/PublishDecision 模型；PublishDecisionService.evaluate：只读不修改 draft、调用 DraftPreviewService 保持与 preview 一致的 validator + issue mapping、source "static_validator"→"validator" 规范化、BLOCKED 时自动追加 VALIDATION_FAILED/PREVIEW_NOT_PUBLISHABLE 顶层摘要 code）；GET /api/labgen/drafts/{lab_id}/publish-decision（admin-only，只读，404 on missing，返回 PublishDecision）；POST /api/labgen/drafts/{lab_id}/publish 强化（decision gate 前置：BLOCKED → 409 含 PublishDecision JSON；ALLOWED → 原有 PublishService 流程；不允许 force-publish bypass）；get_decision_service 使用 Depends(get_repository) 确保 test repo override 正确传播；preview 与 publish-decision issue codes/severities 一致性保证；全部 response leaf value sanitize_text 处理，不返回凭据/traceback/raw exception；新增 tests/test_labgen_publish_decision.py（A~H 8 类场景）
- feat(labgen): Draft Preview & Review Snapshot API v0.1 — 新增 backend/labgen/draft_preview.py（DraftPreviewSourceInfo/DraftPreviewCheck/DraftPreviewStep/DraftPreviewIssue/DraftPreviewSummary/DraftPreviewSnapshot 六个模型；DraftPreviewService.build_snapshot 只读快照服务：inline validator re-run（不持久化）、sanitize_text 内联应用于每个字符串字段（先 sanitize 后截断防止凭据跨边界）、is_publishable 从 validator results 推导、source_type 固定 "unknown"（LabDraft 不存储来源信息）、repair_applied/has_repair_history 固定 None/False（同理）；sanitize_value 递归 dict/list/string sanitizer（exported 供外部调用）；GET /api/labgen/drafts/{lab_id}/preview 端点：admin-only、read-only、404 on missing、DraftPreviewSnapshot response；新增 tests/test_labgen_draft_preview.py（74 tests：A 快乐路径/B manual draft/C validator issue preview/D repair metadata/E 权限/F safety redaction/G regression 不 publish 不 start 不 audit/H sanitize_value 单元测试/I DraftPreviewService 服务层单元测试）；safety-reviewer PASS（2 WARNING 已修复：先 sanitize 后 slice + sanitize_value 文档化）；1483 tests，94.01% coverage
- feat(labgen): AI Draft Review & Repair Loop v0.1 — 新增 backend/labgen/draft_repair.py（DraftReviewIssue/DraftReviewResult/DraftRepairRequest/DraftRepairResult/LabDraftRepairPort/DeterministicFakeDraftRepairAdapter 含 success/still_invalid/malformed/sensitive 四种注入模式）；llm_generation.py 扩展（DraftRepairResultView 安全视图剔除 repaired_candidate、generate_with_repair 8 步 repair loop、review_candidate 无副作用复审方法、Bearer pattern 排序修复防 kv_pat 吞掉 Bearer 导致 value 泄漏、adapter 异常 fallback 持久化初始 draft 不 500）；POST /api/lab-drafts/generate 新增 enable_repair/max_repair_attempts 请求字段 + review_result/repair_result/repair_attempted/repair_applied 响应字段；response 中 repaired_candidate 不暴露（DraftRepairResultView 剔除）；新增 tests/test_labgen_draft_repair.py（~162 tests：A~I 全部场景含 adapter_error fallback + Bearer 顺序安全 + repaired_candidate 不泄漏）；更新 tests/test_labgen_llm_generation.py（154 tests）；safety-reviewer PASS（2 MEDIUM 已修复）；1409 tests，93.77% coverage
- feat(labgen): Generation Template Pack v0.1 — 新增 backend/labgen/generation_templates.py（GenerationTemplateId enum、GenerationTemplate ABC、GenerationTemplateRegistry、GenerationTemplateSelectionResult；PYTHON_BASICS/HTTP_API_BASICS/DATA_TRANSFORM_BASICS 三个模板，每个含 3 步教学闭环：why/do/observe/explain/verify；关键词评分选模板（word-boundary regex）；precedence tiebreak；prompt injection 无效）；llm_generation.py 扩展（LabDraftGenerationResult 新增 template_id、GenerateLabDraftResponse 新增 selected_template_id、FakeDraftGenerationAdapter 切换为基于 Registry 的 candidate 生成、generate_and_create 返回 4-tuple、build_candidate_summary 增加 objective_count+template_id）；POST /api/lab-drafts/generate response 增加 selected_template_id；新增 tests/test_labgen_generation_templates.py（~48 tests）；safety-reviewer PASS
- feat(labgen): LLM Draft Generation Contract Adapter v0.1 — 新增 backend/labgen/llm_generation.py（LabDraftGenerationPort 接口、LabDraftGenerationBody/Request/Result/GenerateLabDraftResponse 模型、FakeDraftGenerationAdapter 确定性 fake generator 含 valid/invalid/malformed/sensitive/rejected 五种注入模式、LabDraftGenerationService 编排 adapter→Pydantic parse→StaticValidator→persist、_sanitize_warnings credential 重定向、build_candidate_summary 安全摘要）；POST /api/lab-drafts/generate 端点（认证用户可调用，不自动 publish，不自动 start session，response 不含 raw_model_output）；新增 tests/test_labgen_llm_generation.py（53 tests，覆盖 happy path/invalid candidate/malformed/prompt injection resistance/sensitive data redaction/permissions/regression contract）；safety-reviewer PASS
- feat(labgen): Backend MVP Runtime Smoke Contract Hardening — 新增 tests/test_labgen_mvp_runtime_smoke.py（41 tests）；覆盖完整 happy path（create→validate→publish→start→step check→complete→audit）、lab start failure paths（namespace/rolebinding/image unresolved）、step check failure path（verifier 返回 failed/credential missing）、complete blocked（ready_to_complete=False → 409）、abort path（success + cleanup failure + VM taint）、permission smoke（owner/admin/stranger/404）、response schema stability assertions（7 个关键 API 字段级断言）、sensitive data no-leak assertions（assert_no_sensitive_runtime_data helper）；发现并修复 _MemDraftRepo JSON roundtrip bug（model_copy+dict 绕过 Pydantic 验证导致 AttributeError）；1191 tests，93.46% coverage，safety-reviewer PASS
- feat(labgen): Minimal Runtime Audit Log — 新增 RuntimeAuditEventType enum（9 种事件类型）和 RuntimeAuditEvent 模型；backend/labgen/runtime_audit.py（RuntimeAuditRepository append-only flock JSON + RuntimeAuditService）；lab_session_service/step_progression_service 可选注入 audit_svc，记录 lab_start_success/failed、complete、abort、cleanup_success/failed、vm_tainted、step_check_passed/failed；GET /api/lab-sessions/{id}/audit-events 端点（owner/admin 访问）；routes.py 注入 RuntimeAuditService；新增 tests/test_labgen_runtime_audit.py（28 tests）；1150 tests，93.40% coverage
- feat(labgen): Runtime Failure Reason Normalization — 新增 backend/labgen/failure_reasons.py（FailureReason enum，20 个稳定机器码）；lab_session_service/verifier/routes 全部硬编码字符串替换为 FailureReason.*.value；VerifyResult 新增 failure_reason 字段（与 error_code 同值，向后兼容）；StepCheckResponse 新增 failure_reason 字段；VerifierService._fail() 同时填充 error_code + failure_reason；新增 tests/test_labgen_failure_reasons.py（36 tests）；1122 tests，93.34% coverage
- feat(labgen): VM Taint & Cleanup Failure Recovery Policy — VMTrackerPort 新增 is_vm_tainted() 抽象方法；StubVMTracker 改为内存 taint set；RealVMTracker 持久化 taint 到 data/tainted_vms.json；run_precheck() 加 precheck.vm_tainted 检查；_do_cleanup() 设 failure_reason="namespace_cleanup_failed" + cleanup_verified 字段；run_cleanup() 对所有 _TERMINAL_STATES 幂等返回（防止重复 mark_vm_tainted）；LabSessionState 新增 cleanup_verified 字段；新增 tests/test_labgen_vm_taint_recovery.py（22 tests）；1086 tests，93.27% coverage
- feat(labgen): Lab Completion Hardening — complete_session() 加 ready_to_complete 前置守卫（未完成步骤时 LabNotReadyToComplete → HTTP 409 lab_not_ready_to_complete）；abort 不受影响；complete/abort 互斥由 _TERMINAL_STATES 保证；cleanup 失败进入 LAB_CLEANUP_FAILED + mark_vm_tainted；新增 tests/test_labgen_lab_completion.py（25 tests：服务层守卫、互斥、cleanup 链、HTTP 端点）；1064 tests，93.62% coverage，safety-reviewer PASS
- feat(labgen): Step check → VerifierService → K8sVerifierClientAdapter 端到端集成测试 — 29 个新测试覆盖：factory 收到 kubeconfig 内容、{{lab_namespace}} 解析、credential missing 无 admin fallback、all-pass 才推进、任一 fail/error 不推进、最后一步 ready_to_complete+LAB_ACTIVE 不自动 cleanup、SECRET_EXISTS 用 list 不 read、不支持类型 runtime 拒绝；1039 tests，93.47% coverage，step_progression_service.py/verifier.py 均 100%
- feat(labgen): K8sVerifierClientAdapter — 实现真实 K8s verifier client（KubernetesApiFactory + K8sVerifierClientAdapter + K8sVerifierClientFactory）；支持 namespace_exists/pod_running/deployment_ready/service_exists/configmap_exists/secret_exists；secret_exists 仅 list 不 read；K8s 404 → False；其他异常传播由 VerifierService 转 error_code；routes.py get_verifier_service 接入真实 factory；kubernetes==36.0.2 加入 requirements.txt；新增 test_labgen_k8s_verifier_client.py
- feat(labgen): Lab Step Progression API — POST /api/lab-sessions/{id}/steps/{step_id}/check；StepProgressionService（owner 校验、仅 LAB_ACTIVE、仅当前 step、VerifyTemplate 从 published draft 读取不接受客户端注入、全部 verify passed 才推进 current_step_index、最后一步 passed 设 ready_to_complete=true、不自动 complete 不触发 cleanup、保存 last_verify_results）；VerifyResult 从 verifier.py 迁移到 models.py 避免循环导入；LabSessionState 新增 current_step_index/completed_step_ids/ready_to_complete/last_verify_results 字段；37 个新测试，全量 972 tests，93.35% coverage，step_progression_service.py 100%，safety-reviewer PASS
- feat(labgen): /internal/verifier/check skeleton — 新增 VerifierService（session 守卫：仅 LAB_ACTIVE、namespace 必须匹配 session.namespace 或 {{lab_namespace}}、cluster_scope 拒绝、无 admin kubeconfig fallback）；K8sVerifierClientPort 抽象 + FakeK8sVerifierClient（可配置响应）；VerifyResult 模型；支持 namespace_exists/pod_running/deployment_ready/service_exists/configmap_exists/secret_exists；其余类型返回 verify_type_not_implemented；POST /internal/verifier/check（X-Admin-Token 隔离，不修改 session state）；48 个新测试，全量 935 tests，覆盖率 93.23%，verifier.py 100%
- feat(labgen): Lab Session Namespace RoleBinding — NamespaceLifecyclePort 新增 ensure_verifier_rolebinding/verifier_rolebinding_exists；StubNamespaceLifecycleAdapter 扩展 rolebinding 行为（rolebindings_created 记录）；K3sNamespaceLifecycleAdapter 加入骨架方法；create_session 在 namespace verified 后进入 VERIFIER_BINDING_CREATING 阶段（create+verify，失败→LAB_START_FAILED(verifier_rolebinding_create_failed/verify_failed)，不进入 LAB_ACTIVE）；VERIFIER_BINDING_CREATING 加入 _ACTIVE_STATES 防重复会话
- feat(labgen): Verifier Identity VM Integration — 实现 VerifierIdentityManager（ensure_verifier_identity/export_verifier_kubeconfig/run_smoke_test）；_kubectl_apply_cmd（python3 subprocess 幂等写入）创建 lab-verifier SA + ClusterRole（无 ClusterRoleBinding/RoleBinding）；_build_kubeconfig 含 YAML 注入防护（server/ca/token 三正则验证）；AgentVMCommandExecutor（QEMU agent 轮询）含命令白名单（kubectl/python3 -c）+ vm_id 数字验证；initialize_verifier_for_vm（ensure→export→smoke 三步，kubeconfig 不写日志）；59 个新测试，全量 862 tests，覆盖率 93.17%
- feat(labgen): Verifier Credential Lifecycle Skeleton — 新增 VerifierCredentialStore（creds/vm_creds/{vm_id}/，原子写，chmod 700/600，vm_id 数字验证防路径穿越）；VerifierCredentialMetadata 从 models.py 统一导入（含 k3s_endpoint/credential_generation/expires_at）；VMCommandExecutorPort + CommandResult + StubVMCommandExecutor（记录调用，可配置返回值）；SmokeCheckResult/VerifierSmokeTestResult 模型；VerifierIdentityManager 骨架（ensure_verifier_identity/export_verifier_kubeconfig/run_smoke_test 均 NotImplementedError）；kubeconfig 内容不写入日志；namespace lifecycle 与 verifier kubeconfig 完全隔离；45 个新测试，全量 806 tests，覆盖率 92.56%
- feat(labgen): Namespace Lifecycle Adapter — 新增 NamespaceLifecyclePort 抽象（create_namespace/namespace_exists/delete_namespace/is_namespace_deleted）；StubNamespaceLifecycleAdapter（可配置，测试专用）；K3sNamespaceLifecycleAdapter 骨架；create_session 加入 NAMESPACE_CREATING 阶段（create+verify，失败→LAB_START_FAILED(namespace_create_failed)）；_do_cleanup 改用 delete+is_namespace_deleted 二次确认（任一失败→LAB_CLEANUP_FAILED+mark_vm_tainted）；verifier kubeconfig 与 namespace lifecycle 完全隔离；17 个新测试，全量 761 tests，覆盖率 92.70%
- feat(labgen): Lab Session Image TTL Recheck — create_session 在 precheck 通过后执行 IMAGE_CHECK_RUNNING 阶段；UNRESOLVED/BLOCKED 图像 → LAB_START_FAILED(image_unresolved)；TTL 过期则重新调 registry existence check，失败 → LAB_START_FAILED(image_unavailable)；recheck 后更新 draft.image_resolution；LabSessionState 新增 failure_reason 字段；11 个新测试
- feat(labgen): Lab Session 状态机骨架 — LabSessionRepository、LabSessionService（VMTrackerPort + NamespaceInspector 端口抽象、RealVMTracker 真实所有权校验、StubNamespaceInspector MVP）；6 条本地 precheck；POST/GET/complete/abort /api/lab-sessions；POST /internal/lab-sessions/{id}/cleanup（admin token 隔离）；46 个新测试，全量 733 tests，覆盖率不低于 92%
- feat(labgen): Publish API — POST /api/labgen/drafts/{id}/publish；PublishService 始终重跑 StaticValidator + registry existence check；publish_blocking → 409，无 blocking → 200 + published；21 个新测试（service unit + endpoint），全量测试覆盖率不低于 92%
- feat(labgen): Admin Review Diff — PATCH 自动记录字段级 diff（AdminReviewDiffRepository，append-only，data/lab_review_diffs.json）；GET /api/labgen/drafts/{id}/diffs 端点；26 个新测试（repository + API），全量 666 tests，覆盖率 92.95%
- feat(labgen): Draft API MVP — POST/GET/PATCH/validate 四个端点；LabDraftRepository（flock JSON 存储）；LabDraftGeneratorStub（无 LLM 占位生成）；validate 自动推导 pollution_level/shared_namespace_candidate/publish_status；PATCH 阻止直接设置 published；52 个新测试（API + repository），全量 640 tests，覆盖率 92.91%
- feat(labgen): 新增 ImageResolver 模块 — backend/labgen/image_resolver.py，实现 RequestedImage 解析、白名单意图映射、blocking 检查（latest/无tag/外部registry/未知registry）、registry existence check（可 mock HTTP）、needs_recheck TTL 判断；62 个新测试全部通过
- feat(labgen): 新增 LabGen 模块基础层 — backend/labgen/models.py（14 个 Pydantic 模型，含 schema_version 字段）和 backend/labgen/static_validator.py（StaticValidator，13 项 publish-gate 检查，自动推导 pollution_level / shared_namespace_candidate）；config/image_whitelist.json；91 个新测试全部通过，labgen 覆盖率 models 100% / validator 98%
- fix: landing.html/article.html 内联脚本提取为外部 JS 文件，修复 CSP 阻断导致文章列表不渲染的 bug；修复 onclick= 内联事件改用事件委托
- fix: showSuccess/showError 改用非阻塞 toast 通知 — 替换 alert()，loading modal 重绘不再被阻塞
- fix: 移除 ipwho.is 第三方 IP 地理位置查询 — 学生 IP 不再发往外部服务（隐私保护），admin 面板 login_location 字段始终为 null
- feat: 公开文章发布系统 — Directus 新增 articles/comments 集合，后端 articles_routes.py 提供公开读/登录后评论 API，新建 landing.html 公开首页（文章列表 + Hero）和 article.html 文章详情+评论页，/ 改为公开落地页，/app 为实验室入口，登录后跳转 /app
- feat(T2-5): 前端学员账号登录 — 新增 /api/auth/directus-login 端点，通过 Directus 凭证认证后签发本地 session cookie；login.html 新增「学员账号」tab，auth.js 重构 tab 切换逻辑
- feat(T2-6): 实验内容从 Directus CMS 读取 — 新增 backend/directus_client.py（fetch_experiment_list/fetch_experiment_detail/directus_auth_login），docs_routes.py 优先从 Directus 获取，本地 Markdown 文件兜底；contract test 移除内部字段 filename 的契约约束
- feat(T2-8): 新增 23 个测试（test_directus_client + docs_routes/auth_routes Directus 集成），425 tests，覆盖率 92%
- feat(T2-1): 部署 Directus 11 + PostgreSQL 16 — docker-compose.directus.yml，内网 127.0.0.1:8055，admin 凭证在 .env.directus
- feat(T2-2+T2-3): Directus Schema + 数据迁移 — experiments 集合 10 字段，导入 11 个网络实验 + 5 个部署案例（含 content+background），迁移 12 个用户，配置 public read policy
- feat(T2-4): FastAPI 双认证支持 — auth_directus.py 验证 Directus Bearer token（60s 缓存），auth_deps.py 优先 Bearer 兜底 session cookie，config.py 新增 DIRECTUS_URL + DIRECTUS_TOKEN_CACHE_TTL
- fix: drawer 点击无反应 — 移除 display:none inline 依赖，改为纯 CSS transform 控制抽屉可见性，terminal connect/disconnect 只控制 toggle 按钮显隐和 isOpen 状态同步
- fix: drawer CSS 缓存 + 连接前隐藏 — CSS 链接加版本号强制刷新，drawer/backdrop 初始 display:none，仅在 terminal.connect() 后显示，disconnect() 时收回
- refactor: 实验学习台改为 Drawer 布局 — 终端全宽，实验文档以固定覆盖层从左侧滑入，新增 drawer.js 控制开关，terminal.js 暴露 fit() 方法，CSS transition 结束后自动 refit xterm，Esc/backdrop 点击关闭，移动端 drawer 全屏宽度
- feat: 三栏 AI 助教布局 — 实验学习台改为三栏（30% 文档 | 25% 背景+AI对话 | 45% 终端），新增案例背景介绍（5个部署案例均已录入），集成 DeepSeek SSE 多轮对话助教，backend/ai_tutor_routes.py 提供 /api/ai/chat 端点，前端 ai-tutor.js 实现流式渲染
- feat: 补全 11 个网络实验背景介绍 — docs_routes.py 每个实验加 background 字段（架构图 + 核心概念 + 设计原因），API 响应新增 background 字段，前端已可自动渲染
- refactor: AI 助教布局重构 — 去掉独立中栏，改为文档栏内三视图切换（实验文档/实验背景/AI助教），终端恢复 62% 宽度，界面不再拥挤
- fix: 对齐官方 K8s 文档——D01 使用官方 gb-frontend:v5 + gb-redis-follower:v2（真实 PHP 留言板），D02 加 Recreate 策略 + Headless Service，D04 精简回官方原版，D05 对齐 development/production 命名空间 + context 切换 + serve_hostname 镜像
- feat: 新增 registry-custom（端口 5001）支持非 docker.io 镜像推送；vm registries.yaml 增加 us-docker.pkg.dev 和 registry.k8s.io 镜像配置；config 增加 VM_REGISTRY_MIRROR_CUSTOM 字段
- fix: D01 Guestbook 补完整主从架构 — 1 Leader + 2 Follower（--replicaof），独立 Service 实现读写分离，补 Step 4 验证 Follower 拒绝写入、Step 5 验证负载均衡
- docs: 里程碑3 完成全部 5 个部署案例 — D02（MySQL + PVC + Secret，数据持久化验证）、D04（CronJob 定时任务，concurrencyPolicy/超时/手动触发）、D05（多命名空间隔离，跨 namespace DNS + NetworkPolicy + ResourceQuota）；同步更新 deployments_routes.py D02 标题和 README 状态
- docs: 里程碑2 部署案例首批内容 — 新增 D01（留言板 Guestbook，nginx + redis:7-alpine，多副本 Deployment + ClusterIP Service）和 D03（蓝绿部署与滚动更新，nginx:1.24/1.25，Service 选择器切换 + rollout history/undo），两案例均可独立使用 kubectl apply 直接运行
- feat: 里程碑1 部署案例基础设施 — 新增 backend/deployments_routes.py（/api/deployments），注册路由到 main.py，前端加 Tab 切换（网络实验/部署案例），docs.js 扩展 mode 属性和 switchMode()，补 tests/test_deployments_routes.py（10个测试，覆盖率93%），redis:7-alpine 已在 registry mirror
- docs: 重写 docs/experiments/README.md — 对齐实际文件内容（11个实验全部完成），修正旧版规划中的标题/状态错误，按 Phase 分组，补充各实验前置条件和学习目标摘要
- docs: 新增 docs/DEPLOYMENT-CASES-DEV-GUIDE.md — 部署案例栏目完整开发指南，含 5 个案例规划、后端路由设计、前端 Tab UI 方案、测试要求和实施计划
- feat: 双层审查体系 — safety-review-policy.md 重构为 safety-reviewer（第一层）+ Codex gpt-5.3-codex（第二层）；pre-push hook 集成 Codex 独立审查（BLOCKER 自动阻断，quota 问题跳过不阻断）
- refactor: pre-commit hook 加 CHANGELOG 机械门禁（有代码变更未更新 CHANGELOG 则阻断 commit）；bug-fix.md / feature.md 重构为"必做 + 延伸阅读"结构；修复精简过度问题——safety-reviewer 恢复为 A/B 类变更条件必做（非可选），防止安全审查质量衰减
- test: 补全 storage_utils 锁模式和嵌套路径测试 — 验证读用 LOCK_SH、写用 LOCK_EX、父目录自动创建
- chore: 对齐 opsreplay 最佳实践 — settings.json 加 deny 列表、CI 覆盖率门禁提升至 90%、mypy 加严 warn_return_any/check_untyped_defs、新增 smoke-test skill 和 Makefile

### Fixed
- test: 补全 storage_utils/rate_limiter/auth_deps 未覆盖路径 — 覆盖率 88.84% → 91.19%（storage_utils 错误路径全覆盖、rate_limiter is_over_limit/record 独立单元测试、auth_deps invalid session/optional 依赖新增 test_auth_deps.py）
- fix: auth_routes.py register 接口速率限制仅在注册成功时记录，注册失败（用户名已存在）不消耗限速槽 — 允许攻击者无限探测用户名；将 rate_limiter.record() 移至 register_user() 调用之前，无论结果均记录；补回归测试防止重现
- fix: middleware.py JsonFormatter.format() 缺少 default=str — 当 extra 包含非 JSON 可序列化字段（如 datetime）时 json.dumps 抛 TypeError，Python logging 静默丢弃整条记录；补回归测试防止重现
- fix: websocket.py get_vm_ip/sync_vm_password 直接在 event loop 调用阻塞 Proxmox I/O — 提取同步 helper，通过 run_in_executor + wait_for(timeout) 包裹，防止终端建立期间阻塞所有请求；SSHTerminal.connect() 的 invoke_shell() 同样移入 executor；修复 invoke_shell 失败时 SSH 连接泄漏；补4条回归测试防止重现
- fix: smart_logger.py generate_report() 调用 print() 破坏结构化 JSON 日志一致性 — 改为 self.logger.info()；补回归测试防止重现
- fix: DELETE /api/vms/{id} 403 响应泄露 VM owner 用户名 — 从 detail 中移除 owner 用户名（信息泄露），owner 仍记录在服务端日志；补回归测试防止重现
- refactor: 移除 auth.py 中从未被调用的 _save_users / _save_sessions 死代码（全部写操作均走 safe_update_json）
- fix: test_serial_second_creation_blocked_by_quota 死锁 — iter([...]) side_effect 耗尽抛 StopIteration，Python 3.7+ 禁止将 StopIteration 设入 asyncio.Future，导致 run_in_executor await 永不返回；改为 side_effect 函数避免抛 StopIteration；同时补充第 3 次 list_vms 调用（req1-_find_available_vm_id 遗漏）
- feat: 引入 safety-reviewer subagent — 9 项检查清单 + A/B/C 分级触发 + 硬/软失败策略 + 外部升级条件
- fix: create_vm 配额检查前对账 tracker 与 Proxmox，自动清除孤儿条目，彻底杜绝假性 429 配额超限（孤儿 VM 回归，出现两次）；补回归测试防止重现
- fix: 点击"终端"按钮无反应 — terminal.js 在 terminal-section 仍为 display:none 时调用 xterm.js open()/fitAddon.fit()，导致终端无法初始化；改为先 remove('hidden') 再 open/fit；无后端回归测试（纯前端 bug）
- fix: CSP style-src 缺 'unsafe-inline' 导致 Tailwind Play CDN 样式注入被拦截 — 页面所有布局/颜色/尺寸完全失效，SVG 图标全宽渲染；script-src 保持严格；补回归测试防止重现
- fix: HEAD /api/health 返回 405 导致 UptimeRobot 监控持续误报 — 新增 HEAD 路由返回 200 空响应；补回归测试防止重现
- fix: admin_routes.py GET /api/admin/status 当 sessions.json 存在损坏条目（缺 username/created_at/expires_at）时整个端点 500 崩溃 — 改为跳过损坏条目并记录 warning，正常条目继续返回；补回归测试防止重现
- fix: main.py auto_cleanup_task 匹配 "VM 不存在" 时 warning 日志缺失原始 Proxmox 错误串 — 加入 reason 字段便于跨版本调试；补回归测试防止重现

- fix: 登录/注册 IP 提取忽略 Cloudflare Tunnel 转发头 — _get_client_ip() 在受信任代理（loopback）连接时优先读 CF-Connecting-IP/X-Forwarded-For，防止速率限制所有用户共享同一个 IP 桶；补 6 条回归测试防止重现
- refactor: api_health_check() 去掉重复 Proxmox version.get() 调用（connect_proxmox 内部已验证，改为直接调 connect_proxmox）
- refactor: 删除 get_node_status() 死代码及其 3 个测试（proxmox_api.py，无 API endpoint 无调用者）
- feat: SecurityHeadersMiddleware 新增 Permissions-Policy: geolocation=(), camera=(), microphone=() 头，限制浏览器敏感 API 访问

### Fixed
- fix: change_password/reset_password 后未作废现有 session — 密码变更后立即清除该用户所有 session，防止泄露的 cookie 在 24h 内继续有效；补回归测试防止重现
- refactor: 删除 start_vm() 死代码 — 无 API endpoint 无调用点，vm_manager.py 覆盖率从 72% 升至 90%
- refactor: _find_available_vm_id() 和 api_health_check() 改走 run_in_executor，避免同步 Proxmox HTTP 请求阻塞 asyncio 事件循环
- refactor: FastAPI app version 从 "0.1.0" 改为 "1.1.0"，与 git tag 一致
- refactor: CORS allow_headers 移除 Authorization（本应用不使用该 header）

### Fixed
- fix: 注册接口无速率限制 — POST /register 加 3次/IP/60s 限制，防 bcrypt CPU 耗尽 DoS；补回归测试防止重现
- fix: SmartLogger 重复创建同名实例会累积 FileHandler 泄漏 FD — _setup_logging() 先 close/remove 旧 handler 再添新 handler；补回归测试防止重现
- fix: CI ADMIN_TOKEN 仅 14 字符，与 config.py ≥32 校验不符 — 改为 38 字符占位值
- fix: app.js vm.name + admin.js login_ip/login_location 直接插入 innerHTML — 加 escapeHtml() 防第三方数据注入
- fix: DOMPurify 加入 marked.js 渲染管道 — index.html 引入 DOMPurify@3.2.6（SRI sha384），docs.js renderMarkdown() 用 DOMPurify.sanitize() 包裹输出，防御 Markdown 注入的非脚本 payload（meta refresh 等）
- fix: bootstrap.sh + cleanup_test_vms.sh 改用 set -euo pipefail，与其余脚本保持一致，管道失败不再静默忽略
- fix: SSHTerminal.receive() 中 asyncio.get_event_loop() 改为 get_running_loop()（S1，第七轮漏网之鱼）；补回归测试防止重现
- fix: wait_for_k3s 中 assert ssh_client is not None 改为显式 if guard + break，避免 -O 模式下 assert 被剥离
- fix: _fetch_geo() 静默吞掉所有异常改为 logger.debug 记录，便于调试地理位置解析失败
- fix: create_vm/delete_vm run_in_executor 加 asyncio.wait_for 超时（360s/120s），Proxmox 挂起时返回 504 而非永久阻塞线程池（B2）；补回归测试防止重现
- fix: websocket.py 静默异常（except Exception: pass）改为记录 warning（C1）；get_event_loop() 改为 get_running_loop()（S）；补回归测试防止重现
- fix: ADMIN_TOKEN 长度不足 32 字符时启动抛 RuntimeError，防止弱 token 被暴力破解（H3）；补回归测试防止重现
- fix: Cookie SameSite 从 lax 改为 strict，消除 CSRF 攻击面（I2）；补回归测试防止重现
- fix: proxmox_api INFO 日志移除 token_user/token_name，防止凭据泄漏到日志系统（A1）；补回归测试防止重现
- fix: GET /api/vms/{vm_id}/status 非归属用户返回 Forbidden 而非 "You do not own this VM"，防止 VM ID 枚举（A2）；补回归测试防止重现
- fix: VM_REGISTRY_MIRROR 未配置时记录 WARNING，提示 HTTP fallback 不安全（H1）；补回归测试防止重现
- fix: CSP 移除 'unsafe-inline' — 将三个 HTML 页面内联脚本/样式提取到外部文件（tailwind-config.js、app.css、ui-init.js、admin.js），SecurityHeadersMiddleware CSP 不再含 script-src/style-src unsafe-inline；补回归测试防止重现
- fix: health 不健康时不暴露原始异常（改为通用错误字符串），同时清理 version_info 死代码
- fix: auto_cleanup_task 中 asyncio.get_event_loop() 改为 get_running_loop()（避免 Python 3.10 废弃警告）
- fix: GET /api/vms 和 GET /api/vms/{vm_id}/status 中阻塞 Proxmox I/O 改用 run_in_executor，防止高并发时阻塞 event loop
- fix: GET /api 端点移除 version 字段，与 health 端点去指纹精神一致
- refactor: list_vms() 从 SmartLogger 改为普通 logger，避免频繁轻量调用产生大量磁盘 report 文件
- fix: config.py 启动交叉校验 — VM_TEMPLATE_ID 不得落在 VM_ID_MIN..MAX 范围内；MAX_VMS_PER_USER 不得超过 MAX_TOTAL_VMS；补 2 个回归测试防止重现
- fix: health 端点移除指纹信息 — 不再返回 version/host/node，只返回 {status, proxmox.connected}；补 1 个回归测试防止重现
- fix: vm_tracker._load_data 对非数字 key 用 isdigit() 过滤 — 文件损坏时不再崩溃；补 2 个回归测试防止重现
- fix: create_vm handler 显式校验 vm_id 范围 — 超出 VM_ID_MIN..VM_ID_MAX 返回 422；补 1 个回归测试防止重现
- fix: docs_routes 路径遍历防御 — 读实验文件前加 .resolve() + 前缀校验；补 1 个回归测试防止重现
- fix: auto_cleanup_task 每轮调用 cleanup_expired_sessions() — 过期 session 不再残留；补 1 个回归测试防止重现
- fix: RequestIDMiddleware 记录请求日志含 duration_ms + status_code；JsonFormatter 支持 extra 字段；补 2 个回归测试防止重现
- ops: CI 加 pip-audit + bandit 安全扫描门禁 — pip-audit 检测依赖 CVE，bandit -lll 阻断 HIGH 级别静态漏洞；websocket.py 已知可接受风险标注 nosec
- fix: 生产环境 500 响应泄露内部异常信息 — `APP_DEBUG=False` 时统一返回 `"Internal server error"`，细节只写入日志；补 2 个回归测试防止重现
- fix: LoginRequest.username 缺 pattern 校验 — 登录接口补齐与注册相同的 `^[a-zA-Z0-9_-]+$` 约束，特殊字符返回 422；补 1 个回归测试防止重现
- fix: vm_tracker 旧格式迁移 bug — `track_vm()` 对已有条目覆盖 `created_at` 为 `now()`，改为保留原始时间戳；补 3 个回归测试防止重现

### Security
- fix: GET /api/vms/{vm_id}/status 无认证 — 加 `get_current_user` 依赖 + 归属校验，未登录返回 401，非归属用户返回 403；补 2 个回归测试防止重现
- fix: 孤儿 VM 自动认领不检查配额 — 认领前验证 `len(user_vm_ids) < MAX_VMS_PER_USER`，配额满时记录 warning 并跳过；补 1 个回归测试防止重现
- fix: session cookie `secure` 硬编码 False — 改为读 `SESSION_COOKIE_SECURE` env var（默认 True），本地开发 .env 设 false；补 1 个回归测试防止重现
- feat: HTTP 安全响应头 — `SecurityHeadersMiddleware` 全局注入 X-Content-Type-Options/X-Frame-Options/Referrer-Policy/CSP；5 个测试覆盖
- fix: 升级 CVE 依赖 — fastapi 0.104.1→0.135.2, starlette 0.27→0.52.1, python-multipart 0.0.6→0.0.22, requests 2.31→2.33.0, pydantic 2.5→2.12.5；清除 9 个 CVE（pygments CVE-2026-4539 暂无修复版本）
- fix: 重新生成 requirements.lock 锁定所有传递依赖版本

### Fixed
- fix: httpx 从 Testing 分组移至生产依赖 — admin_routes.py 在生产中用它做 IP 地理定位
- fix: vm_manager create_vm 部分失败无回滚 — clone 成功后 pool/config/start 任一步骤失败时自动删除孤儿 VM；补 5 个回归测试覆盖各失败场景
- fix: SmartLogger 日志文件在 root logger 已有 handler 时为空 — 改用直接挂 FileHandler 到子 logger，绕过 basicConfig 的 no-op 陷阱；补回归测试防止重现
- fix: pytest-asyncio 漏加进 requirements.txt — 补声明 + 重新生成 requirements.lock；pytest.ini 加 asyncio_mode=auto 消除隐式警告

### Added
- feat: CDN SRI integrity 属性 — xterm.js / marked / highlight.js 所有 jsdelivr 资源加 sha384 哈希，防 CDN 投毒
- feat: 结构化 JSON 日志 — `backend/middleware.py` JsonFormatter，每条日志一行 JSON（timestamp/level/logger/message/exc_info）
- feat: X-Request-ID middleware — 每个 HTTP 请求自动注入 UUID4，客户端可自带，响应头回显
- feat: ruff + mypy 接入 CI — 每次推送自动 lint + 类型检查；pyproject.toml 固化规则
- fix: 修复 smart_logger/websocket/docs_routes 中 8 处 mypy 类型注解错误（Optional、union-attr）
- fix: 修复 websocket.py 3 处 bare except → except Exception
- feat: 优雅关闭 — backend/task_registry.py 追踪 in-flight VM 操作，lifespan shutdown 等待 30s 后才退出，防止孤儿 VM
- feat: pip-tools 依赖锁文件 — requirements.lock 固定所有传递依赖版本，CI 验证锁文件与 requirements.txt 同步
- feat: Prometheus metrics 端点 — /metrics 暴露请求数、延迟分布等指标（prometheus-fastapi-instrumentator）

---

## [1.1.0] - 2026-03-25

### Added
- 数据自动备份：`scripts/backup-data.sh`，每日凌晨 3 点 cron，保留 30 天
- UptimeRobot 监控接入：`/api/health` 端点（已存在，正式纳入监控体系）
- 容量管理：`MAX_TOTAL_VMS=15`，满员返回 503 + 当前人数，前端显示 amber banner
- `scripts/prep-template.sh`：模板重建脚本（预拉镜像 + 清空 etcd）
- `scripts/test-experiment.sh`：11 个实验自动化端到端验证

### Fixed
- 登录时清除同用户旧 session，防止 sessions.json 无限堆积
- Pydantic V2 兼容：`class Config` 改为 `model_config = ConfigDict`
- 删除 VM 后联动关闭终端面板
- `CreateVMRequest` 移除 `template_id`，服务端通过 `config.VM_TEMPLATE_ID` 独占控制，防止模板被绕过克隆

### Tests
- 补回归测试：登录应清除旧 session（`test_login_evicts_previous_session`）
- 补回归测试：服务端独占 template_id（`test_create_vm_always_uses_config_template_id`）

---

## [1.0.0] - 2026-03-01

### Added
- K8S NetLab 初始版本上线
- FastAPI 后端，11 个 K8s 实验文档
- Proxmox VM 克隆/销毁（linked clone，秒级创建）
- WebSocket SSH 终端（xterm.js）
- 用户注册/登录，bcrypt 密码，SHA-256 自动迁移
- 滑动窗口登录频率限制（5 次/IP/60s）
- Session cookie（HttpOnly + Secure）
- Admin 观测面板：活跃 session、VM 归属、IP 地理位置
- K3s 模板重置（QEMU agent 执行，SSH 建立前）
- Registry mirror（宿主机 `registry:2`，隔绝外网依赖）
- Proxmox Pool 权限隔离（`k8s-netlab` pool）
- flock 原子 JSON 读写（防并发损坏）
- Git 双层安全 hooks（pre-commit + pre-push）
- GitHub Actions CI（pytest 门禁，覆盖率 ≥ 75%）
- 网络隔离：vmbr1（172.16.100.0/24），跨 bridge DROP 规则

---

[Unreleased]: https://github.com/svb-devops/k8s-netlab/compare/v1.1.0...HEAD
[1.1.0]: https://github.com/svb-devops/k8s-netlab/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/svb-devops/k8s-netlab/releases/tag/v1.0.0
