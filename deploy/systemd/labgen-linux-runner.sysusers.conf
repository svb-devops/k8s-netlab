# LabGen Linux sandbox command runner — system account for privilege-separated
# execution of learner Linux commands (linux_command_executor.py).
# Deploy: install to /etc/sysusers.d/ then run `systemd-sysusers`.
# See scripts/install-labgen-linux-runner.sh for the idempotent install path.
#Type Name                 ID  GECOS                                    Home Shell
u     labgen-linux-runner  -   "LabGen Linux sandbox command runner"    -    -
